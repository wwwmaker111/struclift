===== STAGE2AB CONTROL-FLOW SOURCE VS FULL SKELETON EXPORT =====
input = /data/chaoni/WQM/datasets/C_train_o0_deepseek_coder6.7b_stage2ab_asmctx_pretty_easy16style_full_nofname_masktok_len16384_v8_full_dedup.jsonl
scanned = 172
exported = 100
requested = 100
source_control_filter = bb-start-line
min_source_controls = 1
allow_duplicates = False
include_fallback_source = False
skipped = {'no_source_control': 62, 'fallback_source_outline': 10}

========================================================================================================================
example = 0  input_line = 1  func = base_name  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/basename.c
source_bounds = 28-64
bb = 6  src_rows = 21  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/2  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.500
bb_cover = 6/6  reasons = ['same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=1, if_header=2, if_then_body=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L32
if L54

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:if_header
BB_4:if_else_body
BB_5:sequential

-- REAL SOURCE FUNCTION --
   28: {
   29:   char const *base = last_component (name);
   30:   idx_t length;
   31:   int dotslash_len;
   32:   if (*base)
   33:     {
   34:       length = base_len (base);
   35: 
   36:       /* Collapse a sequence of trailing slashes into one.  */
   37:       length += ISSLASH (base[length]);
   38: 
   39:       /* On systems with drive letters, "a/b:c" must return "./b:c" rather
   40:          than "b:c" to avoid confusion with a drive letter.  On systems
   41:          with pure POSIX semantics, this is not an issue.  */
   42:       dotslash_len = FILE_SYSTEM_PREFIX_LEN (base) != 0 ? 2 : 0;
   43:     }
   44:   else
   45:     {
   46:       /* There is no last component, so NAME is a file system root or
   47:          the empty string.  */
   48:       base = name;
   49:       length = base_len (base);
   50:       dotslash_len = 0;
   51:     }
   52: 
   53:   char *p = ximalloc (dotslash_len + length + 1);
   54:   if (dotslash_len)
   55:     {
   56:       p[0] = '.';
   57:       p[1] = '/';
   58:     }
   59: 
   60:   /* Finally, copy the basename.  */
   61:   memcpy (p + dotslash_len, base, length);
   62:   p[dotslash_len + length] = '\0';
   63:   return p;
   64: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_then_body)
 if (<FILL_COND:slot_4>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_5 (sequential)
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_4 (if_else_body)
 <FILL_STMT:slot_7>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 1  input_line = 2  func = perform_basename  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basename.c
source_bounds = 104-120
bb = 5  src_rows = 8  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.500/0.500
bb_cover = 5/5  reasons = ['extra_if:1']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L114

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:sequential
BB_4:sequential

-- REAL SOURCE FUNCTION --
  104: {
  105:   char *name = base_name (string);
  106:   strip_trailing_slashes (name);
  107: 
  108:   /* Per POSIX, 'basename // /' must return '//' on platforms with
  109:      distinct //.  On platforms with drive letters, this generalizes
  110:      to making 'basename c: :' return 'c:'.  This rule is captured by
  111:      skipping suffix stripping if base_name returned an absolute path
  112:      or a drive letter (only possible if name is a file-system
  113:      root).  */
  114:   if (suffix && IS_RELATIVE_FILE_NAME (name) && ! FILE_SYSTEM_PREFIX_LEN (name))
  115:     remove_suffix (name, suffix);
  116: 
  117:   fputs (name, stdout);
  118:   putchar (use_nuls ? '\0' : '\n');
  119:   free (name);
  120: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 2  input_line = 3  func = remove_suffix  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basename.c
source_bounds = 85-97
bb = 11  src_rows = 10  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 6/3  gaps(if,loop,switch)=(0,3,0)  sim(type,depth)=0.500/0.167
bb_cover = 11/11  reasons = ['extra_loop:3', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=1, loop_header=5, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L92
  if L93
if L95

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        if
          if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:sequential
BB_5:if_header
BB_6:loop_header
BB_7:loop_body
BB_8:if_header
BB_9:if_else_body
BB_10:sequential

-- REAL SOURCE FUNCTION --
   85: {
   86:   char *np;
   87:   char const *sp;
   88: 
   89:   np = name + strlen (name);
   90:   sp = suffix + strlen (suffix);
   91: 
   92:   while (np > name && sp > suffix)
   93:     if (*--np != *--sp)
   94:       return;
   95:   if (np > name)
   96:     *np = '\0';
   97: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 if (<FILL_COND:slot_7>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_9 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_10>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_11>;  // aligned to BB_6 (if_then_body)
 }
 }
 <FILL_STMT:slot_12>;  // aligned to BB_7 (loop_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 3  input_line = 5  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basename.c
source_bounds = 124-190
bb = 35  src_rows = 44  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 15/7  gaps(if,loop,switch)=(0,4,4)  sim(type,depth)=0.333/0.133
bb_cover = 35/35  reasons = ['extra_loop:4', 'extra_switch:4', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=17, if_header=2, if_else_body=2, loop_header=5, loop_body=2, switch_header=6, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L137
    if L141
    switch L144
if L167
if L173
if L179
    loop L181

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if
        if
          loop
            if
              loop
                loop
                  switch
                    switch
                    switch
                    switch
                    switch
                    loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:switch_header
BB_3:switch_header
BB_4:sequential
BB_5:switch_header
BB_6:sequential
BB_7:switch_header
BB_8:sequential
BB_9:switch_header
BB_10:sequential
BB_11:switch_header
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:loop_header
BB_19:loop_body
BB_20:if_header
BB_21:if_else_body
BB_22:if_header
BB_23:if_else_body
BB_24:sequential
BB_25:loop_header
BB_26:loop_header
BB_27:loop_header
BB_28:loop_body
BB_29:sequential
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  124: {
  125:   bool multiple_names = false;
  126:   bool use_nuls = false;
  127:   char const *suffix = nullptr;
  128: 
  129:   initialize_main (&argc, &argv);
  130:   set_program_name (argv[0]);
  131:   setlocale (LC_ALL, "");
  132:   bindtextdomain (PACKAGE, LOCALEDIR);
  133:   textdomain (PACKAGE);
  134: 
  135:   atexit (close_stdout);
  136: 
  137:   while (true)
  138:     {
  139:       int c = getopt_long (argc, argv, "+as:z", longopts, nullptr);
  140: 
  141:       if (c == -1)
  142:         break;
  143: 
  144:       switch (c)
  145:         {
  146:         case 's':
  147:           suffix = optarg;
  148:           /* -s implies -a, so...  */
  149:           FALLTHROUGH;
  150: 
  151:         case 'a':
  152:           multiple_names = true;
  153:           break;
  154: 
  155:         case 'z':
  156:           use_nuls = true;
  157:           break;
  158: 
  159:         case_GETOPT_HELP_CHAR;
  160:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  161: 
  162:         default:
  163:           usage (EXIT_FAILURE);
  164:         }
  165:     }
  166: 
  167:   if (argc < optind + 1)
  168:     {
  169:       error (0, 0, _("missing operand"));
  170:       usage (EXIT_FAILURE);
  171:     }
  172: 
  173:   if (!multiple_names && optind + 2 < argc)
  174:     {
  175:       error (0, 0, _("extra operand %s"), quote (argv[optind + 2]));
  176:       usage (EXIT_FAILURE);
  177:     }
  178: 
  179:   if (multiple_names)
  180:     {
  181:       for (; optind < argc; optind++)
  182:         perform_basename (argv[optind], suffix, use_nuls);
  183:     }
  184:   else
  185:     perform_basename (argv[optind],
  186:                       optind + 2 == argc ? argv[optind + 1] : nullptr,
  187:                       use_nuls);
  188: 
  189:   return EXIT_SUCCESS;
  190: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 if (<FILL_COND:slot_5>) {  // aligned to BB_20 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_21 (sequential)
 if (<FILL_COND:slot_7>) {  // aligned to BB_22 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_23 (if_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_24 (loop_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_25 (if_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_26 (loop_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_27 (loop_header)
 <FILL_STMT:slot_13>;  // aligned to BB_28 (loop_body)
 <FILL_STMT:slot_14>;  // aligned to BB_29 (sequential)
 <FILL_EXPR:slot_15>;  // aligned to BB_34 (func_prologue_epilogue)
 <FILL_STMT:slot_16>;  // aligned to BB_33 (sequential)
 <FILL_STMT:slot_17>;  // aligned to BB_32 (sequential)
 switch (<FILL_COND:slot_18>) {  // aligned to BB_3 (switch_header)
 <FILL_STMT:slot_19>;  // aligned to BB_4 (sequential)
 switch (<FILL_COND:slot_20>) {  // aligned to BB_5 (switch_header)
 <FILL_STMT:slot_21>;  // aligned to BB_6 (sequential)
 switch (<FILL_COND:slot_22>) {  // aligned to BB_7 (switch_header)
 <FILL_STMT:slot_23>;  // aligned to BB_8 (sequential)
 switch (<FILL_COND:slot_24>) {  // aligned to BB_9 (switch_header)
 <FILL_STMT:slot_25>;  // aligned to BB_10 (sequential)
 switch (<FILL_COND:slot_26>) {  // aligned to BB_11 (switch_header)
 <FILL_STMT:slot_27>;  // aligned to BB_12 (sequential)
 while (<FILL_COND:slot_28>) {  // aligned to BB_18 (loop_header)
 <FILL_STMT:slot_29>;  // aligned to BB_19 (loop_body)
 <FILL_STMT:slot_30>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_32>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_33>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_34>;  // aligned to BB_17 (sequential)
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_35>;  // aligned to BB_30 (if_else_body)
 <FILL_STMT:slot_36>;  // aligned to BB_31 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 4  input_line = 10  func = base64url_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 307-319
bb = 9  src_rows = 10  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(-1,2,0)  sim(type,depth)=0.500/0.500
bb_cover = 9/9  reasons = ['missing_if:1', 'extra_loop:2']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=2, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L311
    if L313
        if L315

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:if_header
BB_3:if_else_body
BB_4:if_header
BB_5:if_else_body
BB_6:sequential
BB_7:loop_body
BB_8:sequential

-- REAL SOURCE FUNCTION --
  307: {
  308:   base64_encode (in, inlen, out, outlen);
  309:   /* translate 62nd and 63rd characters */
  310:   char *p = out;
  311:   while (outlen--)
  312:     {
  313:       if (*p == '+')
  314:         *p = '-';
  315:       else if (*p == '/')
  316:         *p = '_';
  317:       ++p;
  318:     }
  319: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_6 (loop_header)
 <FILL_STMT:slot_9>;  // aligned to BB_8 (sequential)
 }
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 }
 <FILL_STMT:slot_10>;  // aligned to BB_7 (loop_body)
 }
 }
}

========================================================================================================================
example = 5  input_line = 12  func = base64url_decode_ctx_wrapper  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 340-366
bb = 14  src_rows = 19  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 7/4  gaps(if,loop,switch)=(0,3,0)  sim(type,depth)=0.571/0.429
bb_cover = 14/14  reasons = ['extra_loop:3']
10class_pattern_label_counts = sequential=5, if_header=3, if_else_body=3, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L347
    if L349
        if L354
            if L356

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if
        if
          loop
            loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:if_header
BB_3:if_else_body
BB_4:sequential
BB_5:if_header
BB_6:if_else_body
BB_7:if_header
BB_8:if_else_body
BB_9:sequential
BB_10:sequential
BB_11:loop_body
BB_12:sequential
BB_13:sequential

-- REAL SOURCE FUNCTION --
  340: {
  341:   prepare_inbuf (ctx, inlen);
  342:   memcpy (ctx->inbuf, in, inlen);
  343: 
  344:   /* translate 62nd and 63rd characters */
  345:   idx_t i = inlen;
  346:   char *p = ctx->inbuf;
  347:   while (i--)
  348:     {
  349:       if (*p == '+' || *p == '/')
  350:         {
  351:           *outlen = 0;
  352:           return false; /* reject base64 input */
  353:         }
  354:       else if (*p == '-')
  355:         *p = '+';
  356:       else if (*p == '_')
  357:         *p = '/';
  358:       ++p;
  359:     }
  360: 
  361:   bool b = base64_decode_ctx (&ctx->ctx.base64, ctx->inbuf, inlen,
  362:                               out, outlen);
  363:   ctx->i = ctx->ctx.base64.i;
  364: 
  365:   return b;
  366: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_13 (sequential)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_10 (loop_header)
 <FILL_STMT:slot_11>;  // aligned to BB_11 (loop_body)
 while (<FILL_COND:slot_12>) {  // aligned to BB_9 (loop_header)
 <FILL_STMT:slot_13>;  // aligned to BB_12 (sequential)
 }
 }
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_6 (if_else_body)
 <FILL_STMT:slot_14>;  // aligned to BB_7 (if_else_body)
 <FILL_STMT:slot_15>;  // aligned to BB_8 (if_else_body)
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 6  input_line = 17  func = base32hex_encode  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 444-452
bb = 8  src_rows = 7  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.500/0.500
bb_cover = 8/8  reasons = ['extra_if:1']
10class_pattern_label_counts = sequential=6, loop_header=1, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L447

-- AB SKELETON CONTROL OUTLINE --
loop
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:sequential
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:loop_body
BB_7:sequential

-- REAL SOURCE FUNCTION --
  444: {
  445:   base32_encode (in, inlen, out, outlen);
  446: 
  447:   for (char *p = out; outlen--; p++)
  448:     {
  449:       affirm (0x32 <= *p && *p <= 0x5a);          /* LCOV_EXCL_LINE */
  450:       *p = base32_norm_to_hex[*p - 0x32];
  451:     }
  452: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_7 (sequential)
 }
 <FILL_STMT:slot_9>;  // aligned to BB_6 (loop_body)
 }
}

========================================================================================================================
example = 7  input_line = 19  func = base32hex_decode_ctx_wrapper  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 467-487
bb = 8  src_rows = 14  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 3/2  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.667/0.667
bb_cover = 8/8  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=2, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L472
    if L474

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:if_header
BB_3:if_else_body
BB_4:if_else_body
BB_5:sequential
BB_6:loop_body
BB_7:sequential

-- REAL SOURCE FUNCTION --
  467: {
  468:   prepare_inbuf (ctx, inlen);
  469: 
  470:   idx_t i = inlen;
  471:   char *p = ctx->inbuf;
  472:   while (i--)
  473:     {
  474:       if (isbase32hex (*in))
  475:         *p = base32_hex_to_norm[ (int)*in - 0x30];
  476:       else
  477:         *p = *in;
  478:       ++p;
  479:       ++in;
  480:     }
  481: 
  482:   bool b = base32_decode_ctx (&ctx->ctx.base32, ctx->inbuf, inlen,
  483:                               out, outlen);
  484:   ctx->i = ctx->ctx.base32.i;
  485: 
  486:   return b;
  487: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_then_body)
 <FILL_STMT:slot_8>;  // aligned to BB_7 (sequential)
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_5 (if_else_body)
 }
 <FILL_STMT:slot_9>;  // aligned to BB_6 (loop_body)
 }
 }
}

========================================================================================================================
example = 8  input_line = 22  func = base16_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 507-515
bb = 4  src_rows = 7  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L508

-- AB SKELETON CONTROL OUTLINE --
loop
  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_body
BB_3:sequential

-- REAL SOURCE FUNCTION --
  507: {
  508:   while (inlen--)
  509:     {
  510:       unsigned char c = *in;
  511:       *out++ = base16[c >> 4];
  512:       *out++ = base16[c & 0x0F];
  513:       ++in;
  514:     }
  515: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (loop_body)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 }
 }
}

========================================================================================================================
example = 9  input_line = 24  func = base16_decode_ctx  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 531-572
bb = 21  src_rows = 25  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 10/6  gaps(if,loop,switch)=(2,2,0)  sim(type,depth)=0.600/0.200
bb_cover = 21/21  reasons = ['extra_if:2', 'extra_loop:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=6, if_header=5, if_then_body=1, if_else_body=4, loop_header=3, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L539
loop L542
    if L544
    if L551
        if L553
    if L558

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        if
          if
            if
              if
                if
                  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:if_header
BB_5:if_else_body
BB_6:loop_body
BB_7:if_header
BB_8:if_else_body
BB_9:sequential
BB_10:if_header
BB_11:if_else_body
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:if_header
BB_16:if_then_body
BB_17:if_else_body
BB_18:loop_body
BB_19:sequential
BB_20:sequential

-- REAL SOURCE FUNCTION --
  531: {
  532:   bool ignore_lines = true;  /* for now, always ignore them */
  533: 
  534:   *outlen = 0;
  535: 
  536:   /* inlen==0 is request to flush output.
  537:      if there is a dangling high nibble - we are missing the low nibble,
  538:      so return false - indicating an invalid input.  */
  539:   if (inlen == 0)
  540:     return !ctx->ctx.base16.have_nibble;
  541: 
  542:   while (inlen--)
  543:     {
  544:       if (ignore_lines && *in == '\n')
  545:         {
  546:           ++in;
  547:           continue;
  548:         }
  549: 
  550:       int nib = *in++;
  551:       if ('0' <= nib && nib <= '9')
  552:         nib -= '0';
  553:       else if ('A' <= nib && nib <= 'F')
  554:         nib -= 'A' - 10;
  555:       else
  556:         return false; /* garbage - return false */
  557: 
  558:       if (ctx->ctx.base16.have_nibble)
  559:         {
  560:           /* have both nibbles, write octet */
  561:           *out++ = (ctx->ctx.base16.nibble << 4) + nib;
  562:           ++(*outlen);
  563:         }
  564:       else
  565:         {
  566:           /* Store higher nibble until next one arrives */
  567:           ctx->ctx.base16.nibble = nib;
  568:         }
  569:       ctx->ctx.base16.have_nibble = !ctx->ctx.base16.have_nibble;
  570:     }
  571:   return true;
  572: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_20 (sequential)
 while (<FILL_COND:slot_5>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 if (<FILL_COND:slot_12>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_10 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_12 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_19>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_19 (sequential)
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_11 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_13>;  // aligned to BB_16 (if_else_body)
 <FILL_STMT:slot_14>;  // aligned to BB_17 (if_else_body)
 }
 }
 }
 }
 }
 <FILL_STMT:slot_21>;  // aligned to BB_6 (loop_body)
 <FILL_STMT:slot_22>;  // aligned to BB_18 (loop_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 10  input_line = 27  func = base2msbf_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 783-795
bb = 7  src_rows = 11  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/2  gaps(if,loop,switch)=(0,2,0)  sim(type,depth)=0.500/0.500
bb_cover = 7/7  reasons = ['extra_loop:2']
10class_pattern_label_counts = sequential=2, loop_header=3, loop_body=2

-- SOURCE CONTROL OUTLINE --
loop L784
    loop L787

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:sequential
BB_3:loop_header
BB_4:loop_body
BB_5:loop_body
BB_6:sequential

-- REAL SOURCE FUNCTION --
  783: {
  784:   while (inlen--)
  785:     {
  786:       unsigned char c = *in;
  787:       for (int i = 0; i < 8; i++)
  788:         {
  789:           *out++ = c & 0x80 ? '1' : '0';
  790:           c <<= 1;
  791:         }
  792:       outlen -= 8;
  793:       ++in;
  794:     }
  795: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (loop_body)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (loop_body)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (sequential)
 }
 }
 }
 }
}

========================================================================================================================
example = 11  input_line = 29  func = base2msbf_decode_ctx  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 872-912
bb = 16  src_rows = 26  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 9/6  gaps(if,loop,switch)=(1,2,0)  sim(type,depth)=0.667/0.111
bb_cover = 16/16  reasons = ['extra_if:1', 'extra_loop:2']
10class_pattern_label_counts = sequential=2, if_header=5, if_then_body=1, if_else_body=3, loop_header=3, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L880
loop L883
    if L885
    if L891
    if L895
    if L900

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        if
          if
            if
              if
                if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:if_header
BB_5:if_else_body
BB_6:loop_body
BB_7:if_header
BB_8:if_then_body
BB_9:if_header
BB_10:if_else_body
BB_11:if_header
BB_12:if_else_body
BB_13:loop_body
BB_14:sequential
BB_15:sequential

-- REAL SOURCE FUNCTION --
  872: {
  873:   bool ignore_lines = true;  /* for now, always ignore them */
  874: 
  875:   *outlen = 0;
  876: 
  877:   /* inlen==0 is request to flush output.
  878:      if there is a dangling bit - we are missing some bits,
  879:      so return false - indicating an invalid input.  */
  880:   if (inlen == 0)
  881:     return ctx->i == 0;
  882: 
  883:   while (inlen--)
  884:     {
  885:       if (ignore_lines && *in == '\n')
  886:         {
  887:           ++in;
  888:           continue;
  889:         }
  890: 
  891:       if (!isbase2 (*in))
  892:         return false;
  893: 
  894:       bool bit = (*in == '1');
  895:       if (ctx->i == 0)
  896:         ctx->i = 8;
  897:       --ctx->i;
  898:       ctx->ctx.base2.octet |= bit << ctx->i;
  899: 
  900:       if (ctx->i == 0)
  901:         {
  902:           *out++ = ctx->ctx.base2.octet;
  903:           ctx->ctx.base2.octet = 0;
  904:           ++*outlen;
  905:           ctx->i = 0;
  906:         }
  907: 
  908:       ++in;
  909:     }
  910: 
  911:   return true;
  912: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_15 (sequential)
 while (<FILL_COND:slot_5>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (if_then_body)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_14 (sequential)
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_12 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_10 (if_else_body)
 }
 }
 }
 }
 <FILL_STMT:slot_16>;  // aligned to BB_6 (loop_body)
 <FILL_STMT:slot_17>;  // aligned to BB_13 (loop_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 12  input_line = 30  func = base2lsbf_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 800-812
bb = 7  src_rows = 11  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/2  gaps(if,loop,switch)=(0,2,0)  sim(type,depth)=0.500/0.500
bb_cover = 7/7  reasons = ['extra_loop:2']
10class_pattern_label_counts = sequential=2, loop_header=3, loop_body=2

-- SOURCE CONTROL OUTLINE --
loop L801
    loop L804

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:sequential
BB_3:loop_header
BB_4:loop_body
BB_5:loop_body
BB_6:sequential

-- REAL SOURCE FUNCTION --
  800: {
  801:   while (inlen--)
  802:     {
  803:       unsigned char c = *in;
  804:       for (int i = 0; i < 8; i++)
  805:         {
  806:           *out++ = c & 0x01 ? '1' : '0';
  807:           c >>= 1;
  808:         }
  809:       outlen -= 8;
  810:       ++in;
  811:     }
  812: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (loop_body)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (loop_body)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (sequential)
 }
 }
 }
 }
}

========================================================================================================================
example = 13  input_line = 31  func = base2lsbf_decode_ctx  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 828-866
bb = 14  src_rows = 24  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 8/5  gaps(if,loop,switch)=(1,2,0)  sim(type,depth)=0.625/0.125
bb_cover = 14/14  reasons = ['extra_if:1', 'extra_loop:2']
10class_pattern_label_counts = sequential=2, if_header=4, if_then_body=1, if_else_body=2, loop_header=3, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L836
loop L839
    if L841
    if L847
    if L854

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        if
          if
            if
              if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:if_header
BB_5:if_else_body
BB_6:loop_body
BB_7:if_header
BB_8:if_then_body
BB_9:if_header
BB_10:if_else_body
BB_11:loop_body
BB_12:sequential
BB_13:sequential

-- REAL SOURCE FUNCTION --
  828: {
  829:   bool ignore_lines = true;  /* for now, always ignore them */
  830: 
  831:   *outlen = 0;
  832: 
  833:   /* inlen==0 is request to flush output.
  834:      if there is a dangling bit - we are missing some bits,
  835:      so return false - indicating an invalid input.  */
  836:   if (inlen == 0)
  837:     return ctx->i == 0;
  838: 
  839:   while (inlen--)
  840:     {
  841:       if (ignore_lines && *in == '\n')
  842:         {
  843:           ++in;
  844:           continue;
  845:         }
  846: 
  847:       if (!isbase2 (*in))
  848:         return false;
  849: 
  850:       bool bit = (*in == '1');
  851:       ctx->ctx.base2.octet |= bit << ctx->i;
  852:       ++ctx->i;
  853: 
  854:       if (ctx->i == 8)
  855:         {
  856:           *out++ = ctx->ctx.base2.octet;
  857:           ctx->ctx.base2.octet = 0;
  858:           ++*outlen;
  859:           ctx->i = 0;
  860:         }
  861: 
  862:       ++in;
  863:     }
  864: 
  865:   return true;
  866: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_13 (sequential)
 while (<FILL_COND:slot_5>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (if_then_body)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_12 (sequential)
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_10 (if_else_body)
 }
 }
 }
 }
 <FILL_STMT:slot_14>;  // aligned to BB_6 (loop_body)
 <FILL_STMT:slot_15>;  // aligned to BB_11 (loop_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 14  input_line = 34  func = z85_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 600-645
bb = 14  src_rows = 28  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 10/6  gaps(if,loop,switch)=(-1,5,0)  sim(type,depth)=0.500/0.300
bb_cover = 14/14  reasons = ['missing_if:1', 'extra_loop:5']
10class_pattern_label_counts = sequential=1, if_header=3, if_else_body=2, loop_header=5, loop_body=2, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L605
    if L607
        if L610
    if L624
        loop L629
            if L638

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      loop
        if
          loop
            loop
              if
                loop
                  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:if_header
BB_3:function_prologue_epilogue
BB_4:if_else_body
BB_5:if_header
BB_6:if_else_body
BB_7:loop_header
BB_8:if_header
BB_9:loop_header
BB_10:loop_header
BB_11:loop_body
BB_12:loop_header
BB_13:loop_body

-- REAL SOURCE FUNCTION --
  600: {
  601:   int i = 0;
  602:   unsigned char quad[4];
  603:   idx_t outidx = 0;
  604: 
  605:   while (true)
  606:     {
  607:       if (inlen == 0)
  608:         {
  609:           /* no more input, exactly on 4 octet boundary. */
  610:           if (i == 0)
  611:             return;
  612: 
  613:           /* currently, there's no way to return an error in encoding.  */
  614:           error (EXIT_FAILURE, 0,
  615:                  _("invalid input (length must be multiple of 4 characters)"));
  616:         }
  617:       else
  618:         {
  619:           quad[i++] = *in++;
  620:           --inlen;
  621:         }
  622: 
  623:       /* Got a quad, encode it */
  624:       if (i == 4)
  625:         {
  626:           int_fast64_t val = quad[0];
  627:           val = (val << 24) + (quad[1] << 16) + (quad[2] << 8) + quad[3];
  628: 
  629:           for (int j = 4; j >= 0; --j)
  630:             {
  631:               int c = val % 85;
  632:               val /= 85;
  633: 
  634:               /* NOTE: if there is padding (which is trimmed by z85
  635:                  before outputting the result), the output buffer 'out'
  636:                  might not include enough allocated bytes for the padding,
  637:                  so don't store them. */
  638:               if (outidx + j < outlen)
  639:                 out[j] = z85_encoding[c];
  640:             }
  641:           out += 5;
  642:           outidx += 5;
  643:           i = 0;
  644:         }
  645:     }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 while (<FILL_COND:slot_6>) {  // aligned to BB_4 (loop_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_5 (if_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_6 (loop_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_7 (loop_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_8 (if_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_9 (loop_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_10 (loop_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (loop_body)
 <FILL_STMT:slot_14>;  // aligned to BB_13 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_15>;  // aligned to BB_12 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 15  input_line = 36  func = z85_decode_ctx  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 697-764
bb = 22  src_rows = 39  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 12/8  gaps(if,loop,switch)=(2,2,0)  sim(type,depth)=0.667/0.083
bb_cover = 22/22  reasons = ['extra_if:2', 'extra_loop:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=3, if_header=7, if_then_body=3, if_else_body=3, loop_header=4, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L705
    if L707
loop L716
    if L718
    if L727
        if L730
    if L740
        if L749

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop
        if
          if
            if
              if
                if
                  if
                    if
                    loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_then_body
BB_3:loop_header
BB_4:loop_header
BB_5:loop_header
BB_6:if_header
BB_7:if_else_body
BB_8:loop_body
BB_9:if_header
BB_10:if_else_body
BB_11:if_header
BB_12:if_then_body
BB_13:if_else_body
BB_14:sequential
BB_15:if_header
BB_16:if_header
BB_17:if_then_body
BB_18:loop_header
BB_19:loop_body
BB_20:sequential
BB_21:sequential

-- REAL SOURCE FUNCTION --
  697: {
  698:   bool ignore_lines = true;  /* for now, always ignore them */
  699: 
  700:   *outlen = 0;
  701: 
  702:   /* inlen==0 is request to flush output.
  703:      if there are dangling values - we are missing entries,
  704:      so return false - indicating an invalid input.  */
  705:   if (inlen == 0)
  706:     {
  707:       if (ctx->ctx.z85.i > 0)
  708:         {
  709:           /* Z85 variant does not allow padding - input must
  710:              be a multiple of 5 - so return error.  */
  711:           return false;
  712:         }
  713:       return true;
  714:     }
  715: 
  716:   while (inlen--)
  717:     {
  718:       if (ignore_lines && *in == '\n')
  719:         {
  720:           ++in;
  721:           continue;
  722:         }
  723: 
  724:       /* z85 decoding */
  725:       unsigned char c = *in;
  726: 
  727:       if (c >= 33 && c <= 125)
  728:         {
  729:           signed char ch = z85_decoding[c - 33];
  730:           if (ch < 0)
  731:             return false; /* garbage - return false */
  732:           c = ch;
  733:         }
  734:       else
  735:         return false; /* garbage - return false */
  736: 
  737:       ++in;
  738: 
  739:       ctx->ctx.z85.octets[ctx->ctx.z85.i++] = c;
  740:       if (ctx->ctx.z85.i == 5)
  741:         {
  742:           /* decode the lowest 4 octets, then check for overflows.  */
  743:           int_fast64_t val = Z85_LO_CTX_TO_32BIT_VAL (ctx);
  744: 
  745:           /* The Z85 spec and the reference implementation say nothing
  746:              about overflows. To be on the safe side, reject them.  */
  747: 
  748:           val += Z85_HI_CTX_TO_32BIT_VAL (ctx);
  749:           if ((val >> 24) & ~0xFF)
  750:             return false;
  751: 
  752:           *out++ = val >> 24;
  753:           *out++ = (val >> 16) & 0xFF;
  754:           *out++ = (val >> 8) & 0xFF;
  755:           *out++ = val & 0xFF;
  756: 
  757:           *outlen += 4;
  758: 
  759:           ctx->ctx.z85.i = 0;
  760:         }
  761:     }
  762:   ctx->i = ctx->ctx.z85.i;
  763:   return true;
  764: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_21 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (sequential)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_12 (if_then_body)
 if (<FILL_COND:slot_16>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_16 (if_header)
 <FILL_STMT:slot_18>;  // aligned to BB_17 (if_then_body)
 while (<FILL_COND:slot_19>) {  // aligned to BB_18 (loop_header)
 <FILL_STMT:slot_20>;  // aligned to BB_19 (loop_body)
 <FILL_STMT:slot_21>;  // aligned to BB_14 (sequential)
 }
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_20 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_15>;  // aligned to BB_13 (if_else_body)
 }
 }
 }
 }
 }
 <FILL_STMT:slot_23>;  // aligned to BB_8 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 16  input_line = 37  func = do_decode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 1009-1071
bb = 37  src_rows = 38  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 22/8  gaps(if,loop,switch)=(1,13,0)  sim(type,depth)=0.364/0.091
bb_cover = 37/37  reasons = ['extra_if:1', 'extra_loop:13', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=14, if_header=5, if_else_body=3, loop_header=11, loop_body=4

-- SOURCE CONTROL OUTLINE --
    if L1032
        loop L1034
            if L1036
    if L1045
loop L1054
    if L1056
    if L1061
    if L1064

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        loop
          loop
            if
              loop
                loop
                  loop
                    loop
                    loop
                    loop
                    if
                    if
                    loop
                    if
                    if
                    loop
                    loop
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:loop_header
BB_3:sequential
BB_4:loop_header
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:if_header
BB_9:if_else_body
BB_10:sequential
BB_11:sequential
BB_12:loop_body
BB_13:sequential
BB_14:if_header
BB_15:loop_header
BB_16:loop_header
BB_17:loop_header
BB_18:loop_header
BB_19:loop_body
BB_20:sequential
BB_21:loop_header
BB_22:if_header
BB_23:if_else_body
BB_24:sequential
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:if_header
BB_29:if_else_body
BB_30:if_header
BB_31:loop_header
BB_32:loop_header
BB_33:loop_body
BB_34:loop_header
BB_35:loop_body
BB_36:sequential

-- REAL SOURCE FUNCTION --
 1009: {
 1010:   char *inbuf, *outbuf;
 1011:   idx_t sum;
 1012:   struct base_decode_context ctx;
 1013: 
 1014:   inbuf = xmalloc (BASE_LENGTH (DEC_BLOCKSIZE));
 1015:   outbuf = xmalloc (DEC_BLOCKSIZE);
 1016: 
 1017: #if BASE_TYPE == 42
 1018:   ctx.inbuf = nullptr;
 1019: #endif
 1020:   base_decode_ctx_init (&ctx);
 1021: 
 1022:   do
 1023:     {
 1024:       bool ok;
 1025: 
 1026:       sum = 0;
 1027:       do
 1028:         {
 1029:           idx_t n = fread (inbuf + sum,
 1030:                            1, BASE_LENGTH (DEC_BLOCKSIZE) - sum, in);
 1031: 
 1032:           if (ignore_garbage)
 1033:             {
 1034:               for (idx_t i = 0; n > 0 && i < n;)
 1035:                 {
 1036:                   if (isbase (inbuf[sum + i]) || inbuf[sum + i] == '=')
 1037:                     i++;
 1038:                   else
 1039:                     memmove (inbuf + sum + i, inbuf + sum + i + 1, --n - i);
 1040:                 }
 1041:             }
 1042: 
 1043:           sum += n;
 1044: 
 1045:           if (ferror (in))
 1046:             error (EXIT_FAILURE, errno, _("read error"));
 1047:         }
 1048:       while (sum < BASE_LENGTH (DEC_BLOCKSIZE) && !feof (in));
 1049: 
 1050:       /* The following "loop" is usually iterated just once.
 1051:          However, when it processes the final input buffer, we want
 1052:          to iterate it one additional time, but with an indicator
 1053:          telling it to flush what is in CTX.  */
 1054:       for (int k = 0; k < 1 + !!feof (in); k++)
 1055:         {
 1056:           if (k == 1 && ctx.i == 0)
 1057:             break;
 1058:           idx_t n = DEC_BLOCKSIZE;
 1059:           ok = base_decode_ctx (&ctx, inbuf, (k == 0 ? sum : 0), outbuf, &n);
 1060: 
 1061:           if (fwrite (outbuf, 1, n, out) < n)
 1062:             write_error ();
 1063: 
 1064:           if (!ok)
 1065:             error (EXIT_FAILURE, 0, _("invalid input"));
 1066:         }
 1067:     }
 1068:   while (!feof (in));
 1069: 
 1070:   finish_and_exit (in, infile);
 1071: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_7>) {  // aligned to BB_5 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_6 (loop_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_10>;  // aligned to BB_13 (sequential)
 if (<FILL_COND:slot_11>) {  // aligned to BB_14 (if_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_15 (loop_header)
 while (<FILL_COND:slot_13>) {  // aligned to BB_16 (loop_header)
 while (<FILL_COND:slot_14>) {  // aligned to BB_17 (loop_header)
 while (<FILL_COND:slot_15>) {  // aligned to BB_18 (loop_header)
 <FILL_STMT:slot_16>;  // aligned to BB_19 (loop_body)
 while (<FILL_COND:slot_17>) {  // aligned to BB_20 (loop_header)
 while (<FILL_COND:slot_18>) {  // aligned to BB_21 (loop_header)
 if (<FILL_COND:slot_19>) {  // aligned to BB_22 (if_header)
 if (<FILL_COND:slot_20>) {  // aligned to BB_23 (if_header)
 while (<FILL_COND:slot_21>) {  // aligned to BB_24 (loop_header)
 <FILL_STMT:slot_22>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_23>;  // aligned to BB_35 (loop_body)
 <FILL_STMT:slot_24>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_25>;  // aligned to BB_25 (sequential)
 <FILL_STMT:slot_26>;  // aligned to BB_26 (sequential)
 if (<FILL_COND:slot_27>) {  // aligned to BB_28 (if_header)
 if (<FILL_COND:slot_29>) {  // aligned to BB_30 (if_header)
 while (<FILL_COND:slot_30>) {  // aligned to BB_31 (loop_header)
 while (<FILL_COND:slot_31>) {  // aligned to BB_32 (loop_header)
 <FILL_STMT:slot_32>;  // aligned to BB_33 (loop_body)
 <FILL_STMT:slot_33>;  // aligned to BB_27 (sequential)
 if (<FILL_COND:slot_34>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_35>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_36>;  // aligned to BB_10 (sequential)
 } else {
 <FILL_STMT:slot_37>;  // aligned to BB_11 (if_else_body)
 }
 }
 <FILL_STMT:slot_38>;  // aligned to BB_12 (loop_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_28>;  // aligned to BB_29 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 17  input_line = 38  func = do_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 965-1005
bb = 20  src_rows = 23  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 13/3  gaps(if,loop,switch)=(-1,11,0)  sim(type,depth)=0.154/0.000
bb_cover = 20/20  reasons = ['missing_if:1', 'extra_loop:11']
10class_pattern_label_counts = sequential=3, if_header=4, if_else_body=2, loop_header=9, loop_body=2

-- SOURCE CONTROL OUTLINE --
    if L985
if L998
if L1001

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        loop
          loop
            loop
              loop
                loop
                  loop
                    loop
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:loop_header
BB_5:loop_body
BB_6:if_header
BB_7:loop_header
BB_8:loop_header
BB_9:loop_header
BB_10:loop_header
BB_11:loop_header
BB_12:loop_body
BB_13:if_header
BB_14:if_header
BB_15:if_else_body
BB_16:sequential
BB_17:if_header
BB_18:if_else_body
BB_19:sequential

-- REAL SOURCE FUNCTION --
  965: {
  966:   idx_t current_column = 0;
  967:   char *inbuf, *outbuf;
  968:   idx_t sum;
  969: 
  970:   inbuf = xmalloc (ENC_BLOCKSIZE);
  971:   outbuf = xmalloc (BASE_LENGTH (ENC_BLOCKSIZE));
  972: 
  973:   do
  974:     {
  975:       idx_t n;
  976: 
  977:       sum = 0;
  978:       do
  979:         {
  980:           n = fread (inbuf + sum, 1, ENC_BLOCKSIZE - sum, in);
  981:           sum += n;
  982:         }
  983:       while (!feof (in) && !ferror (in) && sum < ENC_BLOCKSIZE);
  984: 
  985:       if (sum > 0)
  986:         {
  987:           /* Process input one block at a time.  Note that ENC_BLOCKSIZE
  988:              is sized so that no pad chars will appear in output. */
  989:           base_encode (inbuf, sum, outbuf, BASE_LENGTH (sum));
  990: 
  991:           wrap_write (outbuf, BASE_LENGTH (sum), wrap_column,
  992:                       &current_column, out);
  993:         }
  994:     }
  995:   while (!feof (in) && !ferror (in) && sum == ENC_BLOCKSIZE);
  996: 
  997:   /* When wrapping, terminate last line. */
  998:   if (wrap_column && current_column > 0 && fputc ('\n', out) == EOF)
  999:     write_error ();
 1000: 
 1001:   if (ferror (in))
 1002:     error (EXIT_FAILURE, errno, _("read error"));
 1003: 
 1004:   finish_and_exit (in, infile);
 1005: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_4 (loop_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (loop_body)
 while (<FILL_COND:slot_8>) {  // aligned to BB_6 (loop_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_7 (loop_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_8 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_9 (loop_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_10 (loop_header)
 while (<FILL_COND:slot_13>) {  // aligned to BB_11 (loop_header)
 <FILL_STMT:slot_14>;  // aligned to BB_12 (loop_body)
 if (<FILL_COND:slot_15>) {  // aligned to BB_13 (if_header)
 if (<FILL_COND:slot_19>) {  // aligned to BB_17 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_19 (sequential)
 } else {
 <FILL_STMT:slot_20>;  // aligned to BB_18 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_14 (if_else_body)
 <FILL_STMT:slot_17>;  // aligned to BB_15 (if_else_body)
 <FILL_STMT:slot_18>;  // aligned to BB_16 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 18  input_line = 40  func = prepare_inbuf  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 295-301
bb = 3  src_rows = 5  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 3/3  reasons = []
10class_pattern_label_counts = sequential=1, if_header=1, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L296

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential

-- REAL SOURCE FUNCTION --
  295: {
  296:   if (ctx->bufsize < inlen)
  297:     {
  298:       ctx->bufsize = inlen * 2;
  299:       ctx->inbuf = xnrealloc (ctx->inbuf, ctx->bufsize, sizeof (char));
  300:     }
  301: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 19  input_line = 41  func = finish_and_exit  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 951-961
bb = 5  src_rows = 7  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/2  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.500
bb_cover = 5/5  reasons = ['same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L952
    if L954

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:sequential
BB_4:sequential

-- REAL SOURCE FUNCTION --
  951: {
  952:   if (fclose (in) != 0)
  953:     {
  954:       if (STREQ (infile, "-"))
  955:         error (EXIT_FAILURE, errno, _("closing standard input"));
  956:       else
  957:         error (EXIT_FAILURE, errno, "%s", quotef (infile));
  958:     }
  959: 
  960:   exit (EXIT_SUCCESS);
  961: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 20  input_line = 42  func = wrap_write  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 921-947
bb = 19  src_rows = 19  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 7/6  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.857/0.429
bb_cover = 19/19  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=8, if_header=5, if_else_body=4, loop_header=1, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L922
    if L925
    loop L929
        if L933
            if L935
              if L941

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop
        if
          if
            if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:sequential
BB_4:if_else_body
BB_5:loop_header
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:if_header
BB_10:if_header
BB_11:if_else_body
BB_12:sequential
BB_13:if_header
BB_14:if_else_body
BB_15:sequential
BB_16:loop_body
BB_17:sequential
BB_18:sequential

-- REAL SOURCE FUNCTION --
  921: {
  922:   if (wrap_column == 0)
  923:     {
  924:       /* Simple write. */
  925:       if (fwrite (buffer, 1, len, stdout) < len)
  926:         write_error ();
  927:     }
  928:   else
  929:     for (idx_t written = 0; written < len; )
  930:       {
  931:         idx_t to_write = MIN (wrap_column - *current_column, len - written);
  932: 
  933:         if (to_write == 0)
  934:           {
  935:             if (fputc ('\n', out) == EOF)
  936:               write_error ();
  937:             *current_column = 0;
  938:           }
  939:         else
  940:           {
  941:             if (fwrite (buffer + written, 1, to_write, stdout) < to_write)
  942:               write_error ();
  943:             *current_column += to_write;
  944:             written += to_write;
  945:           }
  946:       }
  947: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_18 (sequential)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 if (<FILL_COND:slot_10>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_10 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_12 (sequential)
 if (<FILL_COND:slot_14>) {  // aligned to BB_13 (if_header)
 <FILL_STMT:slot_16>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_17>;  // aligned to BB_8 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_17 (sequential)
 } else {
 <FILL_STMT:slot_15>;  // aligned to BB_14 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_11 (if_else_body)
 }
 }
 <FILL_STMT:slot_19>;  // aligned to BB_16 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 <FILL_STMT:slot_20>;  // aligned to BB_6 (if_else_body)
 }
 }
}

========================================================================================================================
example = 21  input_line = 44  func = decode_8  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base32.c
source_bounds = 361-448
bb = 61  src_rows = 53  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 24/18  gaps(if,loop,switch)=(6,0,0)  sim(type,depth)=0.750/0.125
bb_cover = 61/61  reasons = ['extra_if:6', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=21, if_header=23, if_then_body=3, if_else_body=14

-- SOURCE CONTROL OUTLINE --
if L363
if L366
if L369
if L376
    if L378
      if L384
      if L387
      if L395
          if L397
            if L402
            if L405
            if L412
                if L414
                  if L419
                  if L422
                  if L430
                    if L432
                    if L435

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_then_body
BB_4:if_else_body
BB_5:sequential
BB_6:if_header
BB_7:if_else_body
BB_8:if_header
BB_9:if_header
BB_10:if_header
BB_11:if_header
BB_12:if_header
BB_13:if_header
BB_14:sequential
BB_15:sequential
BB_16:if_else_body
BB_17:if_header
BB_18:if_then_body
BB_19:if_else_body
BB_20:sequential
BB_21:sequential
BB_22:if_header
BB_23:if_else_body
BB_24:if_header
BB_25:if_header
BB_26:if_header
BB_27:if_else_body
BB_28:sequential
BB_29:sequential
BB_30:sequential
BB_31:if_header
BB_32:if_else_body
BB_33:sequential
BB_34:if_header
BB_35:if_else_body
BB_36:if_header
BB_37:if_header
BB_38:if_else_body
BB_39:sequential
BB_40:sequential
BB_41:sequential
BB_42:if_header
BB_43:if_then_body
BB_44:if_else_body
BB_45:sequential
BB_46:sequential
BB_47:if_header
BB_48:if_else_body
BB_49:if_header
BB_50:if_header
BB_51:if_else_body
BB_52:sequential
BB_53:if_header
BB_54:if_else_body
BB_55:sequential
BB_56:sequential
BB_57:sequential
BB_58:sequential
BB_59:sequential
BB_60:sequential

-- REAL SOURCE FUNCTION --
  361: {
  362:   char *out = *outp;
  363:   if (inlen < 8)
  364:     return false;
  365: 
  366:   if (!isbase32 (in[0]) || !isbase32 (in[1]))
  367:     return false;
  368: 
  369:   if (*outleft)
  370:     {
  371:       *out++ = ((b32[to_uchar (in[0])] << 3)
  372:                 | (b32[to_uchar (in[1])] >> 2));
  373:       --*outleft;
  374:     }
  375: 
  376:   if (in[2] == '=')
  377:     {
  378:       if (in[3] != '=' || in[4] != '=' || in[5] != '='
  379:           || in[6] != '=' || in[7] != '=')
  380:         return_false;
  381:     }
  382:   else
  383:     {
  384:       if (!isbase32 (in[2]) || !isbase32 (in[3]))
  385:         return_false;
  386: 
  387:       if (*outleft)
  388:         {
  389:           *out++ = ((b32[to_uchar (in[1])] << 6)
  390:                     | (b32[to_uchar (in[2])] << 1)
  391:                     | (b32[to_uchar (in[3])] >> 4));
  392:           --*outleft;
  393:         }
  394: 
  395:       if (in[4] == '=')
  396:         {
  397:           if (in[5] != '=' || in[6] != '=' || in[7] != '=')
  398:             return_false;
  399:         }
  400:       else
  401:         {
  402:           if (!isbase32 (in[4]))
  403:             return_false;
  404: 
  405:           if (*outleft)
  406:             {
  407:               *out++ = ((b32[to_uchar (in[3])] << 4)
  408:                         | (b32[to_uchar (in[4])] >> 1));
  409:               --*outleft;
  410:             }
  411: 
  412:           if (in[5] == '=')
  413:             {
  414:               if (in[6] != '=' || in[7] != '=')
  415:                 return_false;
  416:             }
  417:           else
  418:             {
  419:               if (!isbase32 (in[5]) || !isbase32 (in[6]))
  420:                 return_false;
  421: 
  422:               if (*outleft)
  423:                 {
  424:                   *out++ = ((b32[to_uchar (in[4])] << 7)
  425:                             | (b32[to_uchar (in[5])] << 2)
  426:                             | (b32[to_uchar (in[6])] >> 3));
  427:                   --*outleft;
  428:                 }
  429: 
  430:               if (in[7] != '=')
  431:                 {
  432:                   if (!isbase32 (in[7]))
  433:                     return_false;
  434: 
  435:                   if (*outleft)
  436:                     {
  437:                       *out++ = ((b32[to_uchar (in[6])] << 5)
  438:                                 | (b32[to_uchar (in[7])]));
  439:                       --*outleft;
  440:                     }
  441:                 }
  442:             }
  443:         }
  444:     }
  445: 
  446:   *outp = out;
  447:   return true;
  448: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_60 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_14>) {  // aligned to BB_11 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_13 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_59 (sequential)
 if (<FILL_COND:slot_21>) {  // aligned to BB_17 (if_header)
 <FILL_STMT:slot_23>;  // aligned to BB_20 (sequential)
 <FILL_STMT:slot_24>;  // aligned to BB_21 (sequential)
 if (<FILL_COND:slot_25>) {  // aligned to BB_19 (if_header)
 if (<FILL_COND:slot_26>) {  // aligned to BB_22 (if_header)
 if (<FILL_COND:slot_28>) {  // aligned to BB_24 (if_header)
 if (<FILL_COND:slot_29>) {  // aligned to BB_25 (if_header)
 if (<FILL_COND:slot_30>) {  // aligned to BB_26 (if_header)
 <FILL_STMT:slot_32>;  // aligned to BB_28 (sequential)
 <FILL_STMT:slot_33>;  // aligned to BB_29 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_58 (sequential)
 if (<FILL_COND:slot_36>) {  // aligned to BB_31 (if_header)
 <FILL_STMT:slot_37>;  // aligned to BB_32 (sequential)
 <FILL_STMT:slot_38>;  // aligned to BB_33 (sequential)
 if (<FILL_COND:slot_39>) {  // aligned to BB_34 (if_header)
 if (<FILL_COND:slot_41>) {  // aligned to BB_36 (if_header)
 if (<FILL_COND:slot_42>) {  // aligned to BB_37 (if_header)
 <FILL_STMT:slot_44>;  // aligned to BB_39 (sequential)
 <FILL_STMT:slot_45>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_41 (sequential)
 <FILL_STMT:slot_47>;  // aligned to BB_57 (sequential)
 if (<FILL_COND:slot_48>) {  // aligned to BB_42 (if_header)
 <FILL_STMT:slot_50>;  // aligned to BB_45 (sequential)
 <FILL_STMT:slot_51>;  // aligned to BB_46 (sequential)
 if (<FILL_COND:slot_52>) {  // aligned to BB_44 (if_header)
 if (<FILL_COND:slot_53>) {  // aligned to BB_47 (if_header)
 if (<FILL_COND:slot_55>) {  // aligned to BB_49 (if_header)
 if (<FILL_COND:slot_56>) {  // aligned to BB_50 (if_header)
 <FILL_STMT:slot_58>;  // aligned to BB_52 (sequential)
 if (<FILL_COND:slot_59>) {  // aligned to BB_53 (if_header)
 <FILL_STMT:slot_61>;  // aligned to BB_55 (sequential)
 <FILL_STMT:slot_62>;  // aligned to BB_56 (sequential)
 } else {
 <FILL_STMT:slot_60>;  // aligned to BB_54 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_57>;  // aligned to BB_51 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_54>;  // aligned to BB_48 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_49>;  // aligned to BB_43 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_43>;  // aligned to BB_38 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_40>;  // aligned to BB_35 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_31>;  // aligned to BB_27 (if_else_body)
 <FILL_STMT:slot_34>;  // aligned to BB_30 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_27>;  // aligned to BB_23 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_18 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_16 (if_else_body)
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_7 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_5>;  // aligned to BB_2 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 <FILL_STMT:slot_8>;  // aligned to BB_4 (if_else_body)
 }
}

========================================================================================================================
example = 22  input_line = 45  func = get_8  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base32.c
source_bounds = 308-342
bb = 16  src_rows = 24  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 9/5  gaps(if,loop,switch)=(2,2,0)  sim(type,depth)=0.556/0.222
bb_cover = 16/16  reasons = ['extra_if:2', 'extra_loop:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=4, if_header=5, if_else_body=4, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L312
    if L315
  loop L327
      if L330
          if L333

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        loop
          loop
            loop
              if
                if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_header
BB_4:if_else_body
BB_5:sequential
BB_6:sequential
BB_7:loop_header
BB_8:loop_header
BB_9:if_header
BB_10:if_header
BB_11:if_else_body
BB_12:if_else_body
BB_13:loop_body
BB_14:sequential
BB_15:sequential

-- REAL SOURCE FUNCTION --
  308: {
  309:   if (ctx->i == 8)
  310:     ctx->i = 0;
  311: 
  312:   if (ctx->i == 0)
  313:     {
  314:       char const *t = *in;
  315:       if (8 <= in_end - *in && memchr (t, '\n', 8) == NULL)
  316:         {
  317:           /* This is the common case: no newline.  */
  318:           *in += 8;
  319:           *n_non_newline = 8;
  320:           return (char *) t;
  321:         }
  322:     }
  323: 
  324:   {
  325:     /* Copy non-newline bytes into BUF.  */
  326:     char const *p = *in;
  327:     while (p < in_end)
  328:       {
  329:         char c = *p++;
  330:         if (c != '\n')
  331:           {
  332:             ctx->buf[ctx->i++] = c;
  333:             if (ctx->i == 8)
  334:               break;
  335:           }
  336:       }
  337: 
  338:     *in = p;
  339:     *n_non_newline = ctx->i;
  340:     return ctx->buf;
  341:   }
  342: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_4 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_15 (sequential)
 while (<FILL_COND:slot_9>) {  // aligned to BB_6 (loop_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_7 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_8 (loop_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_10 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_14 (sequential)
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_11 (if_else_body)
 <FILL_STMT:slot_16>;  // aligned to BB_12 (if_else_body)
 }
 }
 <FILL_STMT:slot_17>;  // aligned to BB_13 (loop_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 23  input_line = 50  func = base32_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base32.c
source_bounds = 69-130
bb = 59  src_rows = 34  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 19/11  gaps(if,loop,switch)=(3,5,0)  sim(type,depth)=0.579/0.053
bb_cover = 59/59  reasons = ['extra_if:3', 'extra_loop:5']
10class_pattern_label_counts = sequential=26, if_header=11, if_else_body=17, loop_header=4, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L73
    if L76
    if L81
    if L87
    if L95
    if L103
    if L109
    if L117
    if L122
    if L124
if L128

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    if
                    loop
                    if
                    loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:sequential
BB_3:loop_header
BB_4:sequential
BB_5:if_header
BB_6:if_else_body
BB_7:if_else_body
BB_8:sequential
BB_9:sequential
BB_10:if_header
BB_11:if_else_body
BB_12:if_else_body
BB_13:sequential
BB_14:sequential
BB_15:if_header
BB_16:if_else_body
BB_17:if_else_body
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:sequential
BB_22:sequential
BB_23:if_header
BB_24:if_else_body
BB_25:if_else_body
BB_26:sequential
BB_27:sequential
BB_28:sequential
BB_29:sequential
BB_30:sequential
BB_31:if_header
BB_32:if_else_body
BB_33:if_else_body
BB_34:sequential
BB_35:sequential
BB_36:if_header
BB_37:if_else_body
BB_38:if_else_body
BB_39:sequential
BB_40:sequential
BB_41:sequential
BB_42:sequential
BB_43:sequential
BB_44:if_header
BB_45:if_else_body
BB_46:if_else_body
BB_47:sequential
BB_48:sequential
BB_49:if_header
BB_50:if_else_body
BB_51:if_header
BB_52:if_else_body
BB_53:if_header
BB_54:loop_header
BB_55:loop_body
BB_56:if_header
BB_57:if_else_body
BB_58:sequential

-- REAL SOURCE FUNCTION --
   69: {
   70:   static const char b32str[32] =
   71:     "ABCDEFGHIJKLMNOPQRSTUVWXYZ234567";
   72: 
   73:   while (inlen && outlen)
   74:     {
   75:       *out++ = b32str[(to_uchar (in[0]) >> 3) & 0x1f];
   76:       if (!--outlen)
   77:         break;
   78:       *out++ = b32str[((to_uchar (in[0]) << 2)
   79:                        + (--inlen ? to_uchar (in[1]) >> 6 : 0))
   80:                       & 0x1f];
   81:       if (!--outlen)
   82:         break;
   83:       *out++ =
   84:         (inlen
   85:          ? b32str[(to_uchar (in[1]) >> 1) & 0x1f]
   86:          : '=');
   87:       if (!--outlen)
   88:         break;
   89:       *out++ =
   90:         (inlen
   91:          ? b32str[((to_uchar (in[1]) << 4)
   92:                    + (--inlen ? to_uchar (in[2]) >> 4 : 0))
   93:                   & 0x1f]
   94:          : '=');
   95:       if (!--outlen)
   96:         break;
   97:       *out++ =
   98:         (inlen
   99:          ? b32str[((to_uchar (in[2]) << 1)
  100:                    + (--inlen ? to_uchar (in[3]) >> 7 : 0))
  101:                   & 0x1f]
  102:          : '=');
  103:       if (!--outlen)
  104:         break;
  105:       *out++ =
  106:         (inlen
  107:          ? b32str[(to_uchar (in[3]) >> 2) & 0x1f]
  108:          : '=');
  109:       if (!--outlen)
  110:         break;
  111:       *out++ =
  112:         (inlen
  113:          ? b32str[((to_uchar (in[3]) << 3)
  114:                    + (--inlen ? to_uchar (in[4]) >> 5 : 0))
  115:                   & 0x1f]
  116:          : '=');
  117:       if (!--outlen)
  118:         break;
  119:       *out++ = inlen ? b32str[to_uchar (in[4]) & 0x1f] : '=';
  120:       if (!--outlen)
  121:         break;
  122:       if (inlen)
  123:         inlen--;
  124:       if (inlen)
  125:         in += 5;
  126:     }
  127: 
  128:   if (outlen)
  129:     *out = '\0';
  130: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 if (<FILL_COND:slot_7>) {  // aligned to BB_56 (if_header)
 <FILL_STMT:slot_9>;  // aligned to BB_58 (sequential)
 if (<FILL_COND:slot_10>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_8 (sequential)
 if (<FILL_COND:slot_14>) {  // aligned to BB_10 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_13 (sequential)
 if (<FILL_COND:slot_18>) {  // aligned to BB_15 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_22>;  // aligned to BB_19 (sequential)
 <FILL_STMT:slot_23>;  // aligned to BB_21 (sequential)
 if (<FILL_COND:slot_24>) {  // aligned to BB_23 (if_header)
 if (<FILL_COND:slot_26>) {  // aligned to BB_25 (if_header)
 <FILL_STMT:slot_28>;  // aligned to BB_27 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_29 (sequential)
 if (<FILL_COND:slot_30>) {  // aligned to BB_31 (if_header)
 <FILL_STMT:slot_33>;  // aligned to BB_34 (sequential)
 if (<FILL_COND:slot_34>) {  // aligned to BB_36 (if_header)
 <FILL_STMT:slot_35>;  // aligned to BB_37 (sequential)
 if (<FILL_COND:slot_36>) {  // aligned to BB_38 (if_header)
 <FILL_STMT:slot_38>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_39>;  // aligned to BB_42 (sequential)
 if (<FILL_COND:slot_40>) {  // aligned to BB_44 (if_header)
 <FILL_STMT:slot_43>;  // aligned to BB_47 (sequential)
 if (<FILL_COND:slot_44>) {  // aligned to BB_49 (if_header)
 <FILL_STMT:slot_45>;  // aligned to BB_50 (sequential)
 if (<FILL_COND:slot_46>) {  // aligned to BB_51 (if_header)
 while (<FILL_COND:slot_47>) {  // aligned to BB_52 (loop_header)
 if (<FILL_COND:slot_48>) {  // aligned to BB_53 (if_header)
 while (<FILL_COND:slot_49>) {  // aligned to BB_54 (loop_header)
 <FILL_STMT:slot_50>;  // aligned to BB_55 (loop_body)
 <FILL_STMT:slot_51>;  // aligned to BB_48 (sequential)
 <FILL_STMT:slot_52>;  // aligned to BB_41 (sequential)
 <FILL_STMT:slot_53>;  // aligned to BB_43 (sequential)
 <FILL_STMT:slot_54>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_55>;  // aligned to BB_28 (sequential)
 <FILL_STMT:slot_56>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_57>;  // aligned to BB_20 (sequential)
 <FILL_STMT:slot_58>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_59>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_60>;  // aligned to BB_9 (sequential)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_41>;  // aligned to BB_45 (if_else_body)
 <FILL_STMT:slot_42>;  // aligned to BB_46 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_37>;  // aligned to BB_39 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_31>;  // aligned to BB_32 (if_else_body)
 <FILL_STMT:slot_32>;  // aligned to BB_33 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_27>;  // aligned to BB_26 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_25>;  // aligned to BB_24 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_16 (if_else_body)
 <FILL_STMT:slot_20>;  // aligned to BB_17 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_15>;  // aligned to BB_11 (if_else_body)
 <FILL_STMT:slot_16>;  // aligned to BB_12 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_11>;  // aligned to BB_6 (if_else_body)
 <FILL_STMT:slot_12>;  // aligned to BB_7 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_8>;  // aligned to BB_57 (if_else_body)
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 24  input_line = 51  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 1075-1245
bb = 53  src_rows = 126  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 18/9  gaps(if,loop,switch)=(2,2,5)  sim(type,depth)=0.500/0.056
bb_cover = 53/53  reasons = ['extra_if:2', 'extra_loop:2', 'extra_switch:5', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=29, if_header=6, if_then_body=1, if_else_body=6, loop_header=3, loop_body=1, switch_header=7

-- SOURCE CONTROL OUTLINE --
loop L1099
  switch L1100
          if L1110
  switch L1144
if L1216
if L1222
if L1227
      if L1235
if L1241

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      switch
        switch
          switch
            switch
              switch
                loop
                  if
                    if
                    if
                    switch
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:switch_header
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:switch_header
BB_13:sequential
BB_14:sequential
BB_15:if_header
BB_16:if_else_body
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:sequential
BB_22:sequential
BB_23:sequential
BB_24:sequential
BB_25:sequential
BB_26:sequential
BB_27:loop_header
BB_28:loop_body
BB_29:switch_header
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:sequential
BB_39:sequential
BB_40:if_header
BB_41:if_else_body
BB_42:if_header
BB_43:if_else_body
BB_44:if_else_body
BB_45:if_header
BB_46:if_then_body
BB_47:if_header
BB_48:if_else_body
BB_49:sequential
BB_50:if_header
BB_51:if_else_body
BB_52:sequential

-- REAL SOURCE FUNCTION --
 1075: {
 1076:   int opt;
 1077:   FILE *input_fh;
 1078:   char const *infile;
 1079: 
 1080:   /* True if --decode has been given and we should decode data. */
 1081:   bool decode = false;
 1082:   /* True if we should ignore non-base-alphabetic characters. */
 1083:   bool ignore_garbage = false;
 1084:   /* Wrap encoded data around the 76th column, by default. */
 1085:   idx_t wrap_column = 76;
 1086: 
 1087: #if BASE_TYPE == 42
 1088:   int base_type = 0;
 1089: #endif
 1090: 
 1091:   initialize_main (&argc, &argv);
 1092:   set_program_name (argv[0]);
 1093:   setlocale (LC_ALL, "");
 1094:   bindtextdomain (PACKAGE, LOCALEDIR);
 1095:   textdomain (PACKAGE);
 1096: 
 1097:   atexit (close_stdout);
 1098: 
 1099:   while ((opt = getopt_long (argc, argv, "diw:", long_options, nullptr)) != -1)
 1100:     switch (opt)
 1101:       {
 1102:       case 'd':
 1103:         decode = true;
 1104:         break;
 1105: 
 1106:       case 'w':
 1107:         {
 1108:           intmax_t w;
 1109:           strtol_error s_err = xstrtoimax (optarg, nullptr, 10, &w, "");
 1110:           if (LONGINT_OVERFLOW < s_err || w < 0)
 1111:             error (EXIT_FAILURE, 0, "%s: %s",
 1112:                    _("invalid wrap size"), quote (optarg));
 1113:           wrap_column = s_err == LONGINT_OVERFLOW || IDX_MAX < w ? 0 : w;
 1114:         }
 1115:         break;
 1116: 
 1117:       case 'i':
 1118:         ignore_garbage = true;
 1119:         break;
 1120: 
 1121: #if BASE_TYPE == 42
 1122:       case BASE64_OPTION:
 1123:       case BASE64URL_OPTION:
 1124:       case BASE32_OPTION:
 1125:       case BASE32HEX_OPTION:
 1126:       case BASE16_OPTION:
 1127:       case BASE2MSBF_OPTION:
 1128:       case BASE2LSBF_OPTION:
 1129:       case Z85_OPTION:
 1130:         base_type = opt;
 1131:         break;
 1132: #endif
 1133: 
 1134:       case_GETOPT_HELP_CHAR;
 1135: 
 1136:       case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
 1137: 
 1138:       default:
 1139:         usage (EXIT_FAILURE);
 1140:         break;
 1141:       }
 1142: 
 1143: #if BASE_TYPE == 42
 1144:   switch (base_type)
 1145:     {
 1146:     case BASE64_OPTION:
 1147:       base_length = base64_length_wrapper;
 1148:       isbase = isbase64;
 1149:       base_encode = base64_encode;
 1150:       base_decode_ctx_init = base64_decode_ctx_init_wrapper;
 1151:       base_decode_ctx = base64_decode_ctx_wrapper;
 1152:       break;
 1153: 
 1154:     case BASE64URL_OPTION:
 1155:       base_length = base64_length_wrapper;
 1156:       isbase = isbase64url;
 1157:       base_encode = base64url_encode;
 1158:       base_decode_ctx_init = base64url_decode_ctx_init_wrapper;
 1159:       base_decode_ctx = base64url_decode_ctx_wrapper;
 1160:       break;
 1161: 
 1162:     case BASE32_OPTION:
 1163:       base_length = base32_length_wrapper;
 1164:       isbase = isbase32;
 1165:       base_encode = base32_encode;
 1166:       base_decode_ctx_init = base32_decode_ctx_init_wrapper;
 1167:       base_decode_ctx = base32_decode_ctx_wrapper;
 1168:       break;
 1169: 
 1170:     case BASE32HEX_OPTION:
 1171:       base_length = base32_length_wrapper;
 1172:       isbase = isbase32hex;
 1173:       base_encode = base32hex_encode;
 1174:       base_decode_ctx_init = base32hex_decode_ctx_init_wrapper;
 1175:       base_decode_ctx = base32hex_decode_ctx_wrapper;
 1176:       break;
 1177: 
 1178:     case BASE16_OPTION:
 1179:       base_length = base16_length;
 1180:       isbase = isbase16;
 1181:       base_encode = base16_encode;
 1182:       base_decode_ctx_init = base16_decode_ctx_init;
 1183:       base_decode_ctx = base16_decode_ctx;
 1184:       break;
 1185: 
 1186:     case BASE2MSBF_OPTION:
 1187:       base_length = base2_length;
 1188:       isbase = isbase2;
 1189:       base_encode = base2msbf_encode;
 1190:       base_decode_ctx_init = base2_decode_ctx_init;
 1191:       base_decode_ctx = base2msbf_decode_ctx;
 1192:       break;
 1193: 
 1194:     case BASE2LSBF_OPTION:
 1195:       base_length = base2_length;
 1196:       isbase = isbase2;
 1197:       base_encode = base2lsbf_encode;
 1198:       base_decode_ctx_init = base2_decode_ctx_init;
 1199:       base_decode_ctx = base2lsbf_decode_ctx;
 1200:       break;
 1201: 
 1202:     case Z85_OPTION:
 1203:       base_length = z85_length;
 1204:       isbase = isz85;
 1205:       base_encode = z85_encode;
 1206:       base_decode_ctx_init = z85_decode_ctx_init;
 1207:       base_decode_ctx = z85_decode_ctx;
 1208:       break;
 1209: 
 1210:     default:
 1211:       error (0, 0, _("missing encoding type"));
 1212:       usage (EXIT_FAILURE);
 1213:     }
 1214: #endif
 1215: 
 1216:   if (argc - optind > 1)
 1217:     {
 1218:       error (0, 0, _("extra operand %s"), quote (argv[optind + 1]));
 1219:       usage (EXIT_FAILURE);
 1220:     }
 1221: 
 1222:   if (optind < argc)
 1223:     infile = argv[optind];
 1224:   else
 1225:     infile = "-";
 1226: 
 1227:   if (STREQ (infile, "-"))
 1228:     {
 1229:       xset_binary_mode (STDIN_FILENO, O_BINARY);
 1230:       input_fh = stdin;
 1231:     }
 1232:   else
 1233:     {
 1234:       input_fh = fopen (infile, "rb");
 1235:       if (input_fh == nullptr)
 1236:         error (EXIT_FAILURE, errno, "%s", quotef (infile));
 1237:     }
 1238: 
 1239:   fadvise (input_fh, FADVISE_SEQUENTIAL);
 1240: 
 1241:   if (decode)
 1242:     do_decode (input_fh, infile, stdout, ignore_garbage);
 1243:   else
 1244:     do_encode (input_fh, infile, stdout, wrap_column);
 1245: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 switch (<FILL_COND:slot_8>) {  // aligned to BB_6 (switch_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 switch (<FILL_COND:slot_10>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_12>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 switch (<FILL_COND:slot_14>) {  // aligned to BB_12 (switch_header)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 while (<FILL_COND:slot_16>) {  // aligned to BB_27 (loop_header)
 <FILL_STMT:slot_17>;  // aligned to BB_28 (loop_body)
 <FILL_STMT:slot_18>;  // aligned to BB_24 (sequential)
 if (<FILL_COND:slot_19>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_20>) {  // aligned to BB_16 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_17 (sequential)
 if (<FILL_COND:slot_22>) {  // aligned to BB_18 (if_header)
 <FILL_STMT:slot_24>;  // aligned to BB_20 (sequential)
 <FILL_STMT:slot_25>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_26>;  // aligned to BB_21 (sequential)
 <FILL_STMT:slot_27>;  // aligned to BB_23 (sequential)
 <FILL_STMT:slot_28>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_25 (sequential)
 <FILL_STMT:slot_30>;  // aligned to BB_26 (sequential)
 switch (<FILL_COND:slot_31>) {  // aligned to BB_29 (switch_header)
 <FILL_STMT:slot_32>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_33>;  // aligned to BB_39 (sequential)
 if (<FILL_COND:slot_34>) {  // aligned to BB_40 (if_header)
 if (<FILL_COND:slot_36>) {  // aligned to BB_42 (if_header)
 if (<FILL_COND:slot_38>) {  // aligned to BB_45 (if_header)
 if (<FILL_COND:slot_40>) {  // aligned to BB_50 (if_header)
 <FILL_STMT:slot_42>;  // aligned to BB_52 (sequential)
 if (<FILL_COND:slot_43>) {  // aligned to BB_47 (if_header)
 <FILL_STMT:slot_45>;  // aligned to BB_49 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_44 (sequential)
 <FILL_STMT:slot_47>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_48>;  // aligned to BB_32 (sequential)
 <FILL_STMT:slot_49>;  // aligned to BB_33 (sequential)
 <FILL_STMT:slot_50>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_51>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_52>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_53>;  // aligned to BB_37 (sequential)
 <FILL_STMT:slot_54>;  // aligned to BB_38 (sequential)
 } else {
 <FILL_STMT:slot_44>;  // aligned to BB_48 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_41>;  // aligned to BB_51 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_39>;  // aligned to BB_46 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_37>;  // aligned to BB_43 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_35>;  // aligned to BB_41 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_19 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 25  input_line = 52  func = usage  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/basenc.c
source_bounds = 94-173
bb = 5  src_rows = 24  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 5/5  reasons = []
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L95

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_else_body
BB_4:sequential

-- REAL SOURCE FUNCTION --
   94: {
   95:   if (status != EXIT_SUCCESS)
   96:     emit_try_help ();
   97:   else
   98:     {
   99:       printf (_("\
  100: Usage: %s [OPTION]... [FILE]\n\
  101: "), program_name);
  102: 
  103: #if BASE_TYPE == 42
  104:       fputs (_("\
  105: basenc encode or decode FILE, or standard input, to standard output.\n\
  106: "), stdout);
  107: #else
  108:       printf (_("\
  109: Base%d encode or decode FILE, or standard input, to standard output.\n\
  110: "), BASE_TYPE);
  111: #endif
  112: 
  113:       emit_stdin_note ();
  114:       emit_mandatory_arg_note ();
  115: #if BASE_TYPE == 42
  116:       fputs (_("\
  117:       --base64          same as 'base64' program (RFC4648 section 4)\n\
  118: "), stdout);
  119:       fputs (_("\
  120:       --base64url       file- and url-safe base64 (RFC4648 section 5)\n\
  121: "), stdout);
  122:       fputs (_("\
  123:       --base32          same as 'base32' program (RFC4648 section 6)\n\
  124: "), stdout);
  125:       fputs (_("\
  126:       --base32hex       extended hex alphabet base32 (RFC4648 section 7)\n\
  127: "), stdout);
  128:       fputs (_("\
  129:       --base16          hex encoding (RFC4648 section 8)\n\
  130: "), stdout);
  131:       fputs (_("\
  132:       --base2msbf       bit string with most significant bit (msb) first\n\
  133: "), stdout);
  134:       fputs (_("\
  135:       --base2lsbf       bit string with least significant bit (lsb) first\n\
  136: "), stdout);
  137: #endif
  138:       fputs (_("\
  139:   -d, --decode          decode data\n\
  140:   -i, --ignore-garbage  when decoding, ignore non-alphabet characters\n\
  141:   -w, --wrap=COLS       wrap encoded lines after COLS character (default 76).\n\
  142:                           Use 0 to disable line wrapping\n\
  143: "), stdout);
  144: #if BASE_TYPE == 42
  145:       fputs (_("\
  146:       --z85             ascii85-like encoding (ZeroMQ spec:32/Z85);\n\
  147:                         when encoding, input length must be a multiple of 4;\n\
  148:                         when decoding, input length must be a multiple of 5\n\
  149: "), stdout);
  150: #endif
  151:       fputs (HELP_OPTION_DESCRIPTION, stdout);
  152:       fputs (VERSION_OPTION_DESCRIPTION, stdout);
  153: #if BASE_TYPE == 42
  154:       fputs (_("\
  155: \n\
  156: When decoding, the input may contain newlines in addition to the bytes of\n\
  157: the formal alphabet.  Use --ignore-garbage to attempt to recover\n\
  158: from any other non-alphabet bytes in the encoded stream.\n\
  159: "), stdout);
  160: #else
  161:       printf (_("\
  162: \n\
  163: The data are encoded as described for the %s alphabet in RFC 4648.\n\
  164: When decoding, the input may contain newlines in addition to the bytes of\n\
  165: the formal %s alphabet.  Use --ignore-garbage to attempt to recover\n\
  166: from any other non-alphabet bytes in the encoded stream.\n"),
  167:               PROGRAM_NAME, PROGRAM_NAME);
  168: #endif
  169:       emit_ancillary_info (PROGRAM_NAME);
  170:     }
  171: 
  172:   exit (status);
  173: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 }
}

========================================================================================================================
example = 26  input_line = 53  func = base32_encode_alloc  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base32.c
source_bounds = 143-162
bb = 58  src_rows = 12  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 19/2  gaps(if,loop,switch)=(17,0,0)  sim(type,depth)=0.105/0.053
bb_cover = 58/58  reasons = ['extra_if:17']
10class_pattern_label_counts = sequential=42, if_header=11, if_then_body=4, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L148
if L156

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_header
BB_3:if_then_body
BB_4:if_header
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:sequential
BB_10:sequential
BB_11:sequential
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:sequential
BB_22:sequential
BB_23:sequential
BB_24:sequential
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:if_header
BB_29:if_then_body
BB_30:if_header
BB_31:sequential
BB_32:if_header
BB_33:if_header
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:sequential
BB_39:sequential
BB_40:sequential
BB_41:sequential
BB_42:sequential
BB_43:sequential
BB_44:sequential
BB_45:sequential
BB_46:sequential
BB_47:sequential
BB_48:if_header
BB_49:if_header
BB_50:sequential
BB_51:if_header
BB_52:sequential
BB_53:sequential
BB_54:if_header
BB_55:if_then_body
BB_56:if_else_body
BB_57:sequential

-- REAL SOURCE FUNCTION --
  143: {
  144:   /* Check for overflow in outlen computation.
  145:      Treat negative INLEN as overflow, for better compatibility with
  146:      pre-2021-08-27 API, which used size_t.  */
  147:   idx_t in_over_5 = inlen / 5 + (inlen % 5 != 0), outlen;
  148:   if (! INT_MULTIPLY_OK (in_over_5, 8, &outlen) || inlen < 0)
  149:     {
  150:       *out = NULL;
  151:       return 0;
  152:     }
  153:   outlen++;
  154: 
  155:   *out = imalloc (outlen);
  156:   if (!*out)
  157:     return outlen;
  158: 
  159:   base32_encode (in, inlen, *out, outlen);
  160: 
  161:   return outlen - 1;
  162: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (sequential)
 if (<FILL_COND:slot_4>) {  // aligned to BB_51 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_52 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_53 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_57 (sequential)
 if (<FILL_COND:slot_8>) {  // aligned to BB_54 (if_header)
 <FILL_STMT:slot_9>;  // aligned to BB_55 (sequential)
 if (<FILL_COND:slot_11>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_12>;  // aligned to BB_3 (if_then_body)
 if (<FILL_COND:slot_13>) {  // aligned to BB_28 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_29 (if_then_body)
 if (<FILL_COND:slot_15>) {  // aligned to BB_32 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_33 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_37 (sequential)
 <FILL_STMT:slot_19>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_20>) {  // aligned to BB_46 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_47 (sequential)
 if (<FILL_COND:slot_22>) {  // aligned to BB_49 (if_header)
 if (<FILL_COND:slot_24>) {  // aligned to BB_39 (if_header)
 <FILL_STMT:slot_25>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_26>;  // aligned to BB_41 (sequential)
 <FILL_STMT:slot_27>;  // aligned to BB_44 (sequential)
 <FILL_STMT:slot_28>;  // aligned to BB_45 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_42 (sequential)
 <FILL_STMT:slot_30>;  // aligned to BB_43 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_32>;  // aligned to BB_36 (sequential)
 if (<FILL_COND:slot_33>) {  // aligned to BB_48 (if_header)
 <FILL_STMT:slot_34>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_31 (sequential)
 if (<FILL_COND:slot_36>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_37>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_38>;  // aligned to BB_6 (sequential)
 if (<FILL_COND:slot_39>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_40>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_41>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_42>;  // aligned to BB_8 (sequential)
 <FILL_STMT:slot_43>;  // aligned to BB_11 (sequential)
 <FILL_STMT:slot_44>;  // aligned to BB_12 (sequential)
 <FILL_STMT:slot_45>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_16 (sequential)
 if (<FILL_COND:slot_47>) {  // aligned to BB_26 (if_header)
 <FILL_STMT:slot_48>;  // aligned to BB_27 (sequential)
 <FILL_STMT:slot_49>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_50>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_51>;  // aligned to BB_19 (sequential)
 <FILL_STMT:slot_52>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_53>) {  // aligned to BB_23 (if_header)
 <FILL_STMT:slot_54>;  // aligned to BB_24 (sequential)
 <FILL_STMT:slot_55>;  // aligned to BB_25 (sequential)
 if (<FILL_COND:slot_56>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_57>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_58>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_59>;  // aligned to BB_14 (sequential)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_50 (if_else_body)
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_56 (if_else_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 27  input_line = 54  func = copy_cat  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 506-533
bb = 18  src_rows = 13  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 10/3  gaps(if,loop,switch)=(7,1,-1)  sim(type,depth)=0.200/0.200
bb_cover = 18/18  reasons = ['extra_if:7', 'extra_loop:1', 'missing_switch:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=5, if_header=7, loop_header=1, loop_body=1, switch_header=4

-- SOURCE CONTROL OUTLINE --
loop L518
  switch L519
        if L526

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if
        if
          if
            if
              if
                if
                  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:switch_header
BB_1:loop_header
BB_2:sequential
BB_3:switch_header
BB_4:sequential
BB_5:sequential
BB_6:if_header
BB_7:if_header
BB_8:if_header
BB_9:if_header
BB_10:if_header
BB_11:if_header
BB_12:if_header
BB_13:sequential
BB_14:switch_header
BB_15:switch_header
BB_16:loop_body
BB_17:sequential

-- REAL SOURCE FUNCTION --
  506: {
  507:   /* Copy at most COPY_MAX bytes at a time; this is min
  508:      (SSIZE_MAX, SIZE_MAX) truncated to a value that is
  509:      surely aligned well.  */
  510:   ssize_t copy_max = MIN (SSIZE_MAX, SIZE_MAX) >> 30 << 30;
  511: 
  512:   /* copy_file_range does not support some cases, and it
  513:      incorrectly returns 0 when reading from the proc file
  514:      system on the Linux kernel through at least 5.6.19 (2020),
  515:      so fall back on read+write if the copy_file_range is
  516:      unsupported or the input file seems empty.  */
  517: 
  518:   for (bool some_copied = false; ; some_copied = true)
  519:     switch (copy_file_range (input_desc, nullptr, STDOUT_FILENO, nullptr,
  520:                              copy_max, 0))
  521:       {
  522:       case 0:
  523:         return some_copied;
  524: 
  525:       case -1:
  526:         if (errno == ENOSYS || is_ENOTSUP (errno) || errno == EINVAL
  527:             || errno == EBADF || errno == EXDEV || errno == ETXTBSY
  528:             || errno == EPERM)
  529:           return 0;
  530:         error (0, errno, "%s", quotef (infile));
  531:         return -1;
  532:       }
  533: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_then_body)
 <FILL_STMT:slot_8>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_15 (sequential)
 if (<FILL_COND:slot_10>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_14>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_11 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_12 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_13 (sequential)
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_14 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 }
 <FILL_STMT:slot_19>;  // aligned to BB_16 (loop_body)
 }
 }
}

========================================================================================================================
example = 28  input_line = 55  func = simple_cat  verdict = NEEDS_PATH_AUDIT
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 158-182
bb = 9  src_rows = 12  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 3/4  gaps(if,loop,switch)=(-1,0,0)  sim(type,depth)=0.750/0.500
bb_cover = 9/9  reasons = ['missing_if:1', 'small_if_count_gap']
10class_pattern_label_counts = sequential=3, if_header=2, if_else_body=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L161
    if L166
    if L174
    if L179

-- AB SKELETON CONTROL OUTLINE --
loop
  if
    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:sequential
BB_3:if_header
BB_4:if_else_body
BB_5:if_header
BB_6:loop_header
BB_7:loop_body
BB_8:sequential

-- REAL SOURCE FUNCTION --
  158: {
  159:   /* Loop until the end of the file.  */
  160: 
  161:   while (true)
  162:     {
  163:       /* Read a block of input.  */
  164: 
  165:       size_t n_read = safe_read (input_desc, buf, bufsize);
  166:       if (n_read == SAFE_READ_ERROR)
  167:         {
  168:           error (0, errno, "%s", quotef (infile));
  169:           return false;
  170:         }
  171: 
  172:       /* End of this file?  */
  173: 
  174:       if (n_read == 0)
  175:         return true;
  176: 
  177:       /* Write this block out.  */
  178: 
  179:       if (full_write (STDOUT_FILENO, buf, n_read) != n_read)
  180:         write_error ();
  181:     }
  182: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 <FILL_STMT:slot_5>;  // aligned to BB_8 (sequential)
 if (<FILL_COND:slot_6>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_then_body)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)

 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_6 (if_else_body)
 }
 }
 <FILL_STMT:slot_10>;  // aligned to BB_7 (loop_body)
 }
}

========================================================================================================================
example = 29  input_line = 56  func = cat  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 216-498
bb = 94  src_rows = 142  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 47/29  gaps(if,loop,switch)=(10,8,0)  sim(type,depth)=0.617/0.106
bb_cover = 94/94  reasons = ['extra_if:10', 'extra_loop:8', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=25, if_header=31, if_then_body=1, if_else_body=22, loop_header=10, loop_body=5

-- SOURCE CONTROL OUTLINE --
loop L246
    loop L248
        if L252
                if L258
                  if L293
              if L305
            if L309
            if L315
            if L322
              if L343
                    if L355
                  if L364
              if L372
                  if L374
    if L393
    if L401
    if L413
        loop L415
            if L417
                if L419
                    if L421
                    if L430
                    if L432
                if L447
          loop L466
              if L468
                  if L473
                    if L475
                    if L477

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    loop
                    if
                    if
                    if
                    if
                    loop
                    loop
                    if
                    loop
                    if
                    if
                    if
                    if
                    if
                    loop
                    if
                    loop
                    loop
                    if
                    if
                    if
                    if
                    if
                    if
                    loop
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_header
BB_3:sequential
BB_4:loop_header
BB_5:sequential
BB_6:loop_body
BB_7:sequential
BB_8:if_header
BB_9:if_header
BB_10:if_header
BB_11:if_header
BB_12:if_header
BB_13:if_header
BB_14:if_header
BB_15:if_header
BB_16:sequential
BB_17:if_else_body
BB_18:sequential
BB_19:if_header
BB_20:if_else_body
BB_21:if_header
BB_22:if_else_body
BB_23:if_header
BB_24:if_else_body
BB_25:if_header
BB_26:if_else_body
BB_27:if_else_body
BB_28:if_header
BB_29:if_header
BB_30:if_header
BB_31:if_else_body
BB_32:if_else_body
BB_33:if_header
BB_34:if_else_body
BB_35:sequential
BB_36:sequential
BB_37:if_header
BB_38:if_header
BB_39:if_else_body
BB_40:sequential
BB_41:sequential
BB_42:loop_header
BB_43:loop_body
BB_44:if_header
BB_45:if_else_body
BB_46:if_header
BB_47:if_else_body
BB_48:sequential
BB_49:loop_header
BB_50:sequential
BB_51:loop_header
BB_52:if_header
BB_53:if_else_body
BB_54:if_header
BB_55:if_then_body
BB_56:if_header
BB_57:if_header
BB_58:if_else_body
BB_59:if_else_body
BB_60:sequential
BB_61:if_else_body
BB_62:sequential
BB_63:sequential
BB_64:sequential
BB_65:if_header
BB_66:if_else_body
BB_67:sequential
BB_68:if_header
BB_69:if_else_body
BB_70:if_else_body
BB_71:sequential
BB_72:loop_body
BB_73:loop_header
BB_74:sequential
BB_75:loop_header
BB_76:sequential
BB_77:sequential
BB_78:if_header
BB_79:if_header
BB_80:if_header
BB_81:if_header
BB_82:if_header
BB_83:if_else_body
BB_84:if_else_body
BB_85:sequential
BB_86:sequential
BB_87:sequential
BB_88:if_else_body
BB_89:sequential
BB_90:loop_body
BB_91:loop_header
BB_92:loop_body
BB_93:sequential

-- REAL SOURCE FUNCTION --
  216: {
  217:   /* Last character read from the input buffer.  */
  218:   unsigned char ch;
  219: 
  220:   /* Determines how many consecutive newlines there have been in the
  221:      input.  0 newlines makes NEWLINES -1, 1 newline makes NEWLINES 1,
  222:      etc.  Initially 0 to indicate that we are at the beginning of a
  223:      new line.  The "state" of the procedure is determined by
  224:      NEWLINES.  */
  225:   int newlines = newlines2;
  226: 
  227: #ifdef FIONREAD
  228:   /* If nonzero, use the FIONREAD ioctl, as an optimization.
  229:      (On Ultrix, it is not supported on NFS file systems.)  */
  230:   bool use_fionread = true;
  231: #endif
  232: 
  233:   /* The inbuf pointers are initialized so that BPIN > EOB, and thereby input
  234:      is read immediately.  */
  235: 
  236:   /* Pointer to the first non-valid byte in the input buffer, i.e., the
  237:      current end of the buffer.  */
  238:   char *eob = inbuf;
  239: 
  240:   /* Pointer to the next character in the input buffer.  */
  241:   char *bpin = eob + 1;
  242: 
  243:   /* Pointer to the position where the next character shall be written.  */
  244:   char *bpout = outbuf;
  245: 
  246:   while (true)
  247:     {
  248:       do
  249:         {
  250:           /* Write if there are at least OUTSIZE bytes in OUTBUF.  */
  251: 
  252:           if (outbuf + outsize <= bpout)
  253:             {
  254:               char *wp = outbuf;
  255:               idx_t remaining_bytes;
  256:               do
  257:                 {
  258:                   if (full_write (STDOUT_FILENO, wp, outsize) != outsize)
  259:                     write_error ();
  260:                   wp += outsize;
  261:                   remaining_bytes = bpout - wp;
  262:                 }
  263:               while (outsize <= remaining_bytes);
  264: 
  265:               /* Move the remaining bytes to the beginning of the
  266:                  buffer.  */
  267: 
  268:               memmove (outbuf, wp, remaining_bytes);
  269:               bpout = outbuf + remaining_bytes;
  270:             }
  271: 
  272:           /* Is INBUF empty?  */
  273: 
  274:           if (bpin > eob)
  275:             {
  276:               bool input_pending = false;
  277: #ifdef FIONREAD
  278:               int n_to_read = 0;
  279: 
  280:               /* Is there any input to read immediately?
  281:                  If not, we are about to wait,
  282:                  so write all buffered output before waiting.  */
  283: 
  284:               if (use_fionread
  285:                   && ioctl (input_desc, FIONREAD, &n_to_read) < 0)
  286:                 {
  287:                   /* Ultrix returns EOPNOTSUPP on NFS;
  288:                      HP-UX returns ENOTTY on pipes.
  289:                      SunOS returns EINVAL and
  290:                      More/BSD returns ENODEV on special files
  291:                      like /dev/null.
  292:                      Irix-5 returns ENOSYS on pipes.  */
  293:                   if (errno == EOPNOTSUPP || errno == ENOTTY
  294:                       || errno == EINVAL || errno == ENODEV
  295:                       || errno == ENOSYS)
  296:                     use_fionread = false;
  297:                   else
  298:                     {
  299:                       error (0, errno, _("cannot do ioctl on %s"),
  300:                              quoteaf (infile));
  301:                       newlines2 = newlines;
  302:                       return false;
  303:                     }
  304:                 }
  305:               if (n_to_read != 0)
  306:                 input_pending = true;
  307: #endif
  308: 
  309:               if (!input_pending)
  310:                 write_pending (outbuf, &bpout);
  311: 
  312:               /* Read more input into INBUF.  */
  313: 
  314:               size_t n_read = safe_read (input_desc, inbuf, insize);
  315:               if (n_read == SAFE_READ_ERROR)
  316:                 {
  317:                   error (0, errno, "%s", quotef (infile));
  318:                   write_pending (outbuf, &bpout);
  319:                   newlines2 = newlines;
  320:                   return false;
  321:                 }
  322:               if (n_read == 0)
  323:                 {
  324:                   write_pending (outbuf, &bpout);
  325:                   newlines2 = newlines;
  326:                   return true;
  327:                 }
  328: 
  329:               /* Update the pointers and insert a sentinel at the buffer
  330:                  end.  */
  331: 
  332:               bpin = inbuf;
  333:               eob = bpin + n_read;
  334:               *eob = '\n';
  335:             }
  336:           else
  337:             {
  338:               /* It was a real (not a sentinel) newline.  */
  339: 
  340:               /* Was the last line empty?
  341:                  (i.e., have two or more consecutive newlines been read?)  */
  342: 
  343:               if (++newlines > 0)
  344:                 {
  345:                   if (newlines >= 2)
  346:                     {
  347:                       /* Limit this to 2 here.  Otherwise, with lots of
  348:                          consecutive newlines, the counter could wrap
  349:                          around at INT_MAX.  */
  350:                       newlines = 2;
  351: 
  352:                       /* Are multiple adjacent empty lines to be substituted
  353:                          by single ditto (-s), and this was the second empty
  354:                          line?  */
  355:                       if (squeeze_blank)
  356:                         {
  357:                           ch = *bpin++;
  358:                           continue;
  359:                         }
  360:                     }
  361: 
  362:                   /* Are line numbers to be written at empty lines (-n)?  */
  363: 
  364:                   if (number && !number_nonblank)
  365:                     {
  366:                       next_line_num ();
  367:                       bpout = stpcpy (bpout, line_num_print);
  368:                     }
  369:                 }
  370: 
  371:               /* Output a currency symbol if requested (-e).  */
  372:               if (show_ends)
  373:                 {
  374:                   if (pending_cr)
  375:                     {
  376:                       *bpout++ = '^';
  377:                       *bpout++ = 'M';
  378:                       pending_cr = false;
  379:                     }
  380:                   *bpout++ = '$';
  381:                 }
  382: 
  383:               /* Output the newline.  */
  384: 
  385:               *bpout++ = '\n';
  386:             }
  387:           ch = *bpin++;
  388:         }
  389:       while (ch == '\n');
  390: 
  391:       /* Here CH cannot contain a newline character.  */
  392: 
  393:       if (pending_cr)
  394:         {
  395:           *bpout++ = '\r';
  396:           pending_cr = false;
  397:         }
  398: 
  399:       /* Are we at the beginning of a line, and line numbers are requested?  */
  400: 
  401:       if (newlines >= 0 && number)
  402:         {
  403:           next_line_num ();
  404:           bpout = stpcpy (bpout, line_num_print);
  405:         }
  406: 
  407:       /* The loops below continue until a newline character is found,
  408:          which means that the buffer is empty or that a proper newline
  409:          has been found.  */
  410: 
  411:       /* If quoting, i.e., at least one of -v, -e, or -t specified,
  412:          scan for chars that need conversion.  */
  413:       if (show_nonprinting)
  414:         {
  415:           while (true)
  416:             {
  417:               if (ch >= 32)
  418:                 {
  419:                   if (ch < 127)
  420:                     *bpout++ = ch;
  421:                   else if (ch == 127)
  422:                     {
  423:                       *bpout++ = '^';
  424:                       *bpout++ = '?';
  425:                     }
  426:                   else
  427:                     {
  428:                       *bpout++ = 'M';
  429:                       *bpout++ = '-';
  430:                       if (ch >= 128 + 32)
  431:                         {
  432:                           if (ch < 128 + 127)
  433:                             *bpout++ = ch - 128;
  434:                           else
  435:                             {
  436:                               *bpout++ = '^';
  437:                               *bpout++ = '?';
  438:                             }
  439:                         }
  440:                       else
  441:                         {
  442:                           *bpout++ = '^';
  443:                           *bpout++ = ch - 128 + 64;
  444:                         }
  445:                     }
  446:                 }
  447:               else if (ch == '\t' && !show_tabs)
  448:                 *bpout++ = '\t';
  449:               else if (ch == '\n')
  450:                 {
  451:                   newlines = -1;
  452:                   break;
  453:                 }
  454:               else
  455:                 {
  456:                   *bpout++ = '^';
  457:                   *bpout++ = ch + 64;
  458:                 }
  459: 
  460:               ch = *bpin++;
  461:             }
  462:         }
  463:       else
  464:         {
  465:           /* Not quoting, neither of -v, -e, or -t specified.  */
  466:           while (true)
  467:             {
  468:               if (ch == '\t' && show_tabs)
  469:                 {
  470:                   *bpout++ = '^';
  471:                   *bpout++ = ch + 64;
  472:                 }
  473:               else if (ch != '\n')
  474:                 {
  475:                   if (ch == '\r' && *bpin == '\n' && show_ends)
  476:                     {
  477:                       if (bpin == eob)
  478:                         pending_cr = true;
  479:                       else
  480:                         {
  481:                           *bpout++ = '^';
  482:                           *bpout++ = 'M';
  483:                         }
  484:                     }
  485:                   else
  486:                     *bpout++ = ch;
  487:                 }
  488:               else
  489:                 {
  490:                   newlines = -1;
  491:                   break;
  492:                 }
  493: 
  494:               ch = *bpin++;
  495:             }
  496:         }
  497:     }
  498: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_4 (loop_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (loop_body)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (if_else_body)
 if (<FILL_COND:slot_10>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_11 (if_header)
 if (<FILL_COND:slot_14>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_13 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_14 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_15 (if_header)
 <FILL_STMT:slot_18>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_19>;  // aligned to BB_18 (sequential)
 if (<FILL_COND:slot_20>) {  // aligned to BB_19 (if_header)
 if (<FILL_COND:slot_22>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_23>;  // aligned to BB_22 (sequential)
 if (<FILL_COND:slot_24>) {  // aligned to BB_23 (if_header)
 <FILL_STMT:slot_26>;  // aligned to BB_93 (sequential)
 <FILL_STMT:slot_28>;  // aligned to BB_26 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_27 (sequential)
 while (<FILL_COND:slot_30>) {  // aligned to BB_42 (loop_header)
 <FILL_STMT:slot_31>;  // aligned to BB_43 (loop_body)
 if (<FILL_COND:slot_32>) {  // aligned to BB_44 (if_header)
 if (<FILL_COND:slot_34>) {  // aligned to BB_46 (if_header)
 if (<FILL_COND:slot_35>) {  // aligned to BB_47 (if_header)
 if (<FILL_COND:slot_37>) {  // aligned to BB_49 (if_header)
 while (<FILL_COND:slot_38>) {  // aligned to BB_50 (loop_header)
 while (<FILL_COND:slot_39>) {  // aligned to BB_51 (loop_header)
 if (<FILL_COND:slot_40>) {  // aligned to BB_52 (if_header)
 while (<FILL_COND:slot_42>) {  // aligned to BB_64 (loop_header)
 <FILL_STMT:slot_43>;  // aligned to BB_72 (loop_body)
 if (<FILL_COND:slot_44>) {  // aligned to BB_54 (if_header)
 <FILL_STMT:slot_45>;  // aligned to BB_55 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_63 (sequential)
 if (<FILL_COND:slot_47>) {  // aligned to BB_56 (if_header)
 if (<FILL_COND:slot_48>) {  // aligned to BB_57 (if_header)
 <FILL_STMT:slot_50>;  // aligned to BB_60 (sequential)
 <FILL_STMT:slot_51>;  // aligned to BB_62 (sequential)
 if (<FILL_COND:slot_54>) {  // aligned to BB_65 (if_header)
 if (<FILL_COND:slot_55>) {  // aligned to BB_66 (if_header)
 <FILL_STMT:slot_56>;  // aligned to BB_67 (sequential)
 while (<FILL_COND:slot_57>) {  // aligned to BB_71 (loop_header)
 if (<FILL_COND:slot_58>) {  // aligned to BB_68 (if_header)
 <FILL_STMT:slot_60>;  // aligned to BB_73 (sequential)
 while (<FILL_COND:slot_62>) {  // aligned to BB_74 (loop_header)
 while (<FILL_COND:slot_63>) {  // aligned to BB_75 (loop_header)
 if (<FILL_COND:slot_64>) {  // aligned to BB_76 (if_header)
 if (<FILL_COND:slot_66>) {  // aligned to BB_78 (if_header)
 if (<FILL_COND:slot_67>) {  // aligned to BB_79 (if_header)
 if (<FILL_COND:slot_68>) {  // aligned to BB_80 (if_header)
 if (<FILL_COND:slot_69>) {  // aligned to BB_81 (if_header)
 if (<FILL_COND:slot_70>) {  // aligned to BB_82 (if_header)
 <FILL_STMT:slot_71>;  // aligned to BB_83 (sequential)
 <FILL_STMT:slot_72>;  // aligned to BB_85 (sequential)
 <FILL_STMT:slot_73>;  // aligned to BB_87 (sequential)
 while (<FILL_COND:slot_74>) {  // aligned to BB_89 (loop_header)
 <FILL_STMT:slot_75>;  // aligned to BB_84 (sequential)
 <FILL_STMT:slot_76>;  // aligned to BB_86 (sequential)
 <FILL_STMT:slot_77>;  // aligned to BB_91 (sequential)
 if (<FILL_COND:slot_78>) {  // aligned to BB_28 (if_header)
 if (<FILL_COND:slot_79>) {  // aligned to BB_29 (if_header)
 if (<FILL_COND:slot_80>) {  // aligned to BB_30 (if_header)
 <FILL_STMT:slot_81>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_82>;  // aligned to BB_32 (sequential)
 if (<FILL_COND:slot_83>) {  // aligned to BB_33 (if_header)
 if (<FILL_COND:slot_84>) {  // aligned to BB_34 (if_header)
 <FILL_STMT:slot_86>;  // aligned to BB_36 (sequential)
 if (<FILL_COND:slot_87>) {  // aligned to BB_37 (if_header)
 if (<FILL_COND:slot_88>) {  // aligned to BB_38 (if_header)
 <FILL_STMT:slot_90>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_91>;  // aligned to BB_41 (sequential)
 } else {
 <FILL_STMT:slot_89>;  // aligned to BB_39 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_85>;  // aligned to BB_35 (if_else_body)
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_92>;  // aligned to BB_88 (if_else_body)
 <FILL_STMT:slot_93>;  // aligned to BB_17 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_65>;  // aligned to BB_77 (if_else_body)
 }
 <FILL_STMT:slot_94>;  // aligned to BB_90 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_59>;  // aligned to BB_69 (if_else_body)
 <FILL_STMT:slot_61>;  // aligned to BB_70 (if_else_body)
 }
 <FILL_STMT:slot_95>;  // aligned to BB_92 (loop_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_49>;  // aligned to BB_58 (if_else_body)
 <FILL_STMT:slot_52>;  // aligned to BB_59 (if_else_body)
 <FILL_STMT:slot_53>;  // aligned to BB_61 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_41>;  // aligned to BB_53 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_36>;  // aligned to BB_48 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_33>;  // aligned to BB_45 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_25>;  // aligned to BB_24 (if_else_body)
 <FILL_STMT:slot_27>;  // aligned to BB_25 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_21>;  // aligned to BB_20 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 30  input_line = 57  func = write_pending  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 190-198
bb = 5  src_rows = 7  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/2  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.500
bb_cover = 5/5  reasons = ['same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L192
    if L194

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:sequential
BB_4:sequential

-- REAL SOURCE FUNCTION --
  190: {
  191:   idx_t n_write = *bpout - outbuf;
  192:   if (0 < n_write)
  193:     {
  194:       if (full_write (STDOUT_FILENO, outbuf, n_write) != n_write)
  195:         write_error ();
  196:       *bpout = outbuf;
  197:     }
  198: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 31  input_line = 58  func = next_line_num  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 134-150
bb = 10  src_rows = 12  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 3/3  gaps(if,loop,switch)=(-1,1,0)  sim(type,depth)=0.667/0.000
bb_cover = 10/10  reasons = ['missing_if:1', 'extra_loop:1']
10class_pattern_label_counts = sequential=3, if_header=2, if_else_body=3, loop_header=1, loop_body=1

-- SOURCE CONTROL OUTLINE --
    if L138
if L144
if L148

-- AB SKELETON CONTROL OUTLINE --
loop
  if
    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:sequential
BB_3:loop_body
BB_4:if_header
BB_5:if_else_body
BB_6:if_else_body
BB_7:if_header
BB_8:if_else_body
BB_9:sequential

-- REAL SOURCE FUNCTION --
  134: {
  135:   char *endp = line_num_end;
  136:   do
  137:     {
  138:       if ((*endp)++ < '9')
  139:         return;
  140:       *endp-- = '0';
  141:     }
  142:   while (endp >= line_num_start);
  143: 
  144:   if (line_num_start > line_buf)
  145:     *--line_num_start = '1';
  146:   else
  147:     *line_buf = '>';
  148:   if (line_num_start < line_num_print)
  149:     line_num_print--;
  150: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_then_body)
 <FILL_STMT:slot_5>;  // aligned to BB_9 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (loop_body)
 if (<FILL_COND:slot_7>) {  // aligned to BB_4 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (sequential)
 } else {
 <FILL_STMT:slot_11>;  // aligned to BB_6 (if_else_body)
 }
 }
 }
}

========================================================================================================================
example = 32  input_line = 59  func = usage  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 86-128
bb = 5  src_rows = 14  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 5/5  reasons = []
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L87

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_else_body
BB_4:sequential

-- REAL SOURCE FUNCTION --
   86: {
   87:   if (status != EXIT_SUCCESS)
   88:     emit_try_help ();
   89:   else
   90:     {
   91:       printf (_("\
   92: Usage: %s [OPTION]... [FILE]...\n\
   93: "),
   94:               program_name);
   95:       fputs (_("\
   96: Concatenate FILE(s) to standard output.\n\
   97: "), stdout);
   98: 
   99:       emit_stdin_note ();
  100: 
  101:       fputs (_("\
  102: \n\
  103:   -A, --show-all           equivalent to -vET\n\
  104:   -b, --number-nonblank    number nonempty output lines, overrides -n\n\
  105:   -e                       equivalent to -vE\n\
  106:   -E, --show-ends          display $ at end of each line\n\
  107:   -n, --number             number all output lines\n\
  108:   -s, --squeeze-blank      suppress repeated empty output lines\n\
  109: "), stdout);
  110:       fputs (_("\
  111:   -t                       equivalent to -vT\n\
  112:   -T, --show-tabs          display TAB characters as ^I\n\
  113:   -u                       (ignored)\n\
  114:   -v, --show-nonprinting   use ^ and M- notation, except for LFD and TAB\n\
  115: "), stdout);
  116:       fputs (HELP_OPTION_DESCRIPTION, stdout);
  117:       fputs (VERSION_OPTION_DESCRIPTION, stdout);
  118:       printf (_("\
  119: \n\
  120: Examples:\n\
  121:   %s f - g  Output f's contents, then standard input, then g's contents.\n\
  122:   %s        Copy standard input to standard output.\n\
  123: "),
  124:               program_name, program_name);
  125:       emit_ancillary_info (PROGRAM_NAME);
  126:     }
  127:   exit (status);
  128: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 }
}

========================================================================================================================
example = 33  input_line = 60  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cat.c
source_bounds = 538-803
bb = 99  src_rows = 137  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 48/17  gaps(if,loop,switch)=(13,7,11)  sim(type,depth)=0.354/0.042
bb_cover = 99/99  reasons = ['extra_if:13', 'extra_loop:7', 'extra_switch:11', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=40, if_header=24, if_then_body=1, if_else_body=13, loop_header=6, loop_body=2, switch_header=12, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L582
    switch L585
if L643
if L656
    if L671
    if L675
        if L679
          if L685
    if L693
    if L709
    if L725
        if L730
          if L769
      if L785
if L793
    if L795
if L799

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      switch
        switch
          switch
            switch
              switch
                switch
                  switch
                    switch
                    switch
                    switch
                    switch
                    loop
                    if
                    loop
                    if
                    if
                    if
                    loop
                    loop
                    loop
                    if
                    if
                    if
                    if
                    if
                    loop
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:switch_header
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:switch_header
BB_13:sequential
BB_14:switch_header
BB_15:sequential
BB_16:switch_header
BB_17:sequential
BB_18:switch_header
BB_19:sequential
BB_20:switch_header
BB_21:sequential
BB_22:switch_header
BB_23:sequential
BB_24:switch_header
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:sequential
BB_29:sequential
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:loop_header
BB_39:loop_body
BB_40:if_header
BB_41:if_else_body
BB_42:if_header
BB_43:if_header
BB_44:if_else_body
BB_45:sequential
BB_46:sequential
BB_47:loop_header
BB_48:sequential
BB_49:if_header
BB_50:if_header
BB_51:if_else_body
BB_52:sequential
BB_53:if_header
BB_54:if_else_body
BB_55:if_else_body
BB_56:if_header
BB_57:if_else_body
BB_58:if_header
BB_59:if_header
BB_60:if_header
BB_61:if_header
BB_62:if_else_body
BB_63:if_header
BB_64:if_header
BB_65:if_header
BB_66:if_header
BB_67:if_header
BB_68:if_else_body
BB_69:sequential
BB_70:sequential
BB_71:sequential
BB_72:if_header
BB_73:if_then_body
BB_74:if_else_body
BB_75:sequential
BB_76:sequential
BB_77:sequential
BB_78:sequential
BB_79:if_header
BB_80:if_header
BB_81:if_header
BB_82:if_else_body
BB_83:sequential
BB_84:sequential
BB_85:sequential
BB_86:if_header
BB_87:if_else_body
BB_88:loop_header
BB_89:loop_header
BB_90:loop_body
BB_91:if_header
BB_92:if_header
BB_93:if_else_body
BB_94:sequential
BB_95:if_header
BB_96:if_else_body
BB_97:sequential
BB_98:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  538: {
  539:   /* Nonzero if we have ever read standard input.  */
  540:   bool have_read_stdin = false;
  541: 
  542:   struct stat stat_buf;
  543: 
  544:   /* Variables that are set according to the specified options.  */
  545:   bool number = false;
  546:   bool number_nonblank = false;
  547:   bool squeeze_blank = false;
  548:   bool show_ends = false;
  549:   bool show_nonprinting = false;
  550:   bool show_tabs = false;
  551:   int file_open_mode = O_RDONLY;
  552: 
  553:   static struct option const long_options[] =
  554:   {
  555:     {"number-nonblank", no_argument, nullptr, 'b'},
  556:     {"number", no_argument, nullptr, 'n'},
  557:     {"squeeze-blank", no_argument, nullptr, 's'},
  558:     {"show-nonprinting", no_argument, nullptr, 'v'},
  559:     {"show-ends", no_argument, nullptr, 'E'},
  560:     {"show-tabs", no_argument, nullptr, 'T'},
  561:     {"show-all", no_argument, nullptr, 'A'},
  562:     {GETOPT_HELP_OPTION_DECL},
  563:     {GETOPT_VERSION_OPTION_DECL},
  564:     {nullptr, 0, nullptr, 0}
  565:   };
  566: 
  567:   initialize_main (&argc, &argv);
  568:   set_program_name (argv[0]);
  569:   setlocale (LC_ALL, "");
  570:   bindtextdomain (PACKAGE, LOCALEDIR);
  571:   textdomain (PACKAGE);
  572: 
  573:   /* Arrange to close stdout if we exit via the
  574:      case_GETOPT_HELP_CHAR or case_GETOPT_VERSION_CHAR code.
  575:      Normally STDOUT_FILENO is used rather than stdout, so
  576:      close_stdout does nothing.  */
  577:   atexit (close_stdout);
  578: 
  579:   /* Parse command line options.  */
  580: 
  581:   int c;
  582:   while ((c = getopt_long (argc, argv, "benstuvAET", long_options, nullptr))
  583:          != -1)
  584:     {
  585:       switch (c)
  586:         {
  587:         case 'b':
  588:           number = true;
  589:           number_nonblank = true;
  590:           break;
  591: 
  592:         case 'e':
  593:           show_ends = true;
  594:           show_nonprinting = true;
  595:           break;
  596: 
  597:         case 'n':
  598:           number = true;
  599:           break;
  600: 
  601:         case 's':
  602:           squeeze_blank = true;
  603:           break;
  604: 
  605:         case 't':
  606:           show_tabs = true;
  607:           show_nonprinting = true;
  608:           break;
  609: 
  610:         case 'u':
  611:           /* We provide the -u feature unconditionally.  */
  612:           break;
  613: 
  614:         case 'v':
  615:           show_nonprinting = true;
  616:           break;
  617: 
  618:         case 'A':
  619:           show_nonprinting = true;
  620:           show_ends = true;
  621:           show_tabs = true;
  622:           break;
  623: 
  624:         case 'E':
  625:           show_ends = true;
  626:           break;
  627: 
  628:         case 'T':
  629:           show_tabs = true;
  630:           break;
  631: 
  632:         case_GETOPT_HELP_CHAR;
  633: 
  634:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  635: 
  636:         default:
  637:           usage (EXIT_FAILURE);
  638:         }
  639:     }
  640: 
  641:   /* Get device, i-node number, and optimal blocksize of output.  */
  642: 
  643:   if (fstat (STDOUT_FILENO, &stat_buf) < 0)
  644:     error (EXIT_FAILURE, errno, _("standard output"));
  645: 
  646:   /* Optimal size of i/o operations of output.  */
  647:   idx_t outsize = io_blksize (stat_buf);
  648: 
  649:   /* Device and I-node number of the output.  */
  650:   dev_t out_dev = stat_buf.st_dev;
  651:   ino_t out_ino = stat_buf.st_ino;
  652: 
  653:   /* True if the output is a regular file.  */
  654:   bool out_isreg = S_ISREG (stat_buf.st_mode) != 0;
  655: 
  656:   if (! (number || show_ends || squeeze_blank))
  657:     {
  658:       file_open_mode |= O_BINARY;
  659:       xset_binary_mode (STDOUT_FILENO, O_BINARY);
  660:     }
  661: 
  662:   /* Main loop.  */
  663: 
  664:   infile = "-";
  665:   int argind = optind;
  666:   bool ok = true;
  667:   idx_t page_size = getpagesize ();
  668: 
  669:   do
  670:     {
  671:       if (argind < argc)
  672:         infile = argv[argind];
  673: 
  674:       bool reading_stdin = STREQ (infile, "-");
  675:       if (reading_stdin)
  676:         {
  677:           have_read_stdin = true;
  678:           input_desc = STDIN_FILENO;
  679:           if (file_open_mode & O_BINARY)
  680:             xset_binary_mode (STDIN_FILENO, O_BINARY);
  681:         }
  682:       else
  683:         {
  684:           input_desc = open (infile, file_open_mode);
  685:           if (input_desc < 0)
  686:             {
  687:               error (0, errno, "%s", quotef (infile));
  688:               ok = false;
  689:               continue;
  690:             }
  691:         }
  692: 
  693:       if (fstat (input_desc, &stat_buf) < 0)
  694:         {
  695:           error (0, errno, "%s", quotef (infile));
  696:           ok = false;
  697:           goto contin;
  698:         }
  699: 
  700:       /* Optimal size of i/o operations of input.  */
  701:       idx_t insize = io_blksize (stat_buf);
  702: 
  703:       fdadvise (input_desc, 0, 0, FADVISE_SEQUENTIAL);
  704: 
  705:       /* Don't copy a nonempty regular file to itself, as that would
  706:          merely exhaust the output device.  It's better to catch this
  707:          error earlier rather than later.  */
  708: 
  709:       if (out_isreg
  710:           && stat_buf.st_dev == out_dev && stat_buf.st_ino == out_ino
  711:           && lseek (input_desc, 0, SEEK_CUR) < stat_buf.st_size)
  712:         {
  713:           error (0, 0, _("%s: input file is output file"), quotef (infile));
  714:           ok = false;
  715:           goto contin;
  716:         }
  717: 
  718:       /* Pointer to the input buffer.  */
  719:       char *inbuf;
  720: 
  721:       /* Select which version of 'cat' to use.  If any format-oriented
  722:          options were given use 'cat'; if not, use 'copy_cat' if it
  723:          works, 'simple_cat' otherwise.  */
  724: 
  725:       if (! (number || show_ends || show_nonprinting
  726:              || show_tabs || squeeze_blank))
  727:         {
  728:           int copy_cat_status =
  729:             out_isreg && S_ISREG (stat_buf.st_mode) ? copy_cat () : 0;
  730:           if (copy_cat_status != 0)
  731:             {
  732:               inbuf = nullptr;
  733:               ok &= 0 < copy_cat_status;
  734:             }
  735:           else
  736:             {
  737:               insize = MAX (insize, outsize);
  738:               inbuf = xalignalloc (page_size, insize);
  739:               ok &= simple_cat (inbuf, insize);
  740:             }
  741:         }
  742:       else
  743:         {
  744:           /* Allocate, with an extra byte for a newline sentinel.  */
  745:           inbuf = xalignalloc (page_size, insize + 1);
  746: 
  747:           /* Why are
  748:              (OUTSIZE - 1 + INSIZE * 4 + LINE_COUNTER_BUF_LEN)
  749:              bytes allocated for the output buffer?
  750: 
  751:              A test whether output needs to be written is done when the input
  752:              buffer empties or when a newline appears in the input.  After
  753:              output is written, at most (OUTSIZE - 1) bytes will remain in the
  754:              buffer.  Now INSIZE bytes of input is read.  Each input character
  755:              may grow by a factor of 4 (by the prepending of M-^).  If all
  756:              characters do, and no newlines appear in this block of input, we
  757:              will have at most (OUTSIZE - 1 + INSIZE * 4) bytes in the buffer.
  758:              If the last character in the preceding block of input was a
  759:              newline, a line number may be written (according to the given
  760:              options) as the first thing in the output buffer. (Done after the
  761:              new input is read, but before processing of the input begins.)
  762:              A line number requires seldom more than LINE_COUNTER_BUF_LEN
  763:              positions.
  764: 
  765:              Align the output buffer to a page size boundary, for efficiency
  766:              on some paging implementations.  */
  767: 
  768:           idx_t bufsize;
  769:           if (ckd_mul (&bufsize, insize, 4)
  770:               || ckd_add (&bufsize, bufsize, outsize)
  771:               || ckd_add (&bufsize, bufsize, LINE_COUNTER_BUF_LEN - 1))
  772:             xalloc_die ();
  773:           char *outbuf = xalignalloc (page_size, bufsize);
  774: 
  775:           ok &= cat (inbuf, insize, outbuf, outsize, show_nonprinting,
  776:                      show_tabs, number, number_nonblank, show_ends,
  777:                      squeeze_blank);
  778: 
  779:           alignfree (outbuf);
  780:         }
  781: 
  782:       alignfree (inbuf);
  783: 
  784:     contin:
  785:       if (!reading_stdin && close (input_desc) < 0)
  786:         {
  787:           error (0, errno, "%s", quotef (infile));
  788:           ok = false;
  789:         }
  790:     }
  791:   while (++argind < argc);
  792: 
  793:   if (pending_cr)
  794:     {
  795:       if (full_write (STDOUT_FILENO, "\r", 1) != 1)
  796:         write_error ();
  797:     }
  798: 
  799:   if (have_read_stdin && close (STDIN_FILENO) < 0)
  800:     error (EXIT_FAILURE, errno, _("closing standard input"));
  801: 
  802:   return ok ? EXIT_SUCCESS : EXIT_FAILURE;
  803: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 switch (<FILL_COND:slot_8>) {  // aligned to BB_6 (switch_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 switch (<FILL_COND:slot_10>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_12>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 switch (<FILL_COND:slot_14>) {  // aligned to BB_12 (switch_header)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 switch (<FILL_COND:slot_16>) {  // aligned to BB_14 (switch_header)
 <FILL_STMT:slot_17>;  // aligned to BB_15 (sequential)
 switch (<FILL_COND:slot_18>) {  // aligned to BB_16 (switch_header)
 <FILL_STMT:slot_19>;  // aligned to BB_17 (sequential)
 switch (<FILL_COND:slot_20>) {  // aligned to BB_18 (switch_header)
 <FILL_STMT:slot_21>;  // aligned to BB_19 (sequential)
 switch (<FILL_COND:slot_22>) {  // aligned to BB_20 (switch_header)
 <FILL_STMT:slot_23>;  // aligned to BB_21 (sequential)
 switch (<FILL_COND:slot_24>) {  // aligned to BB_22 (switch_header)
 <FILL_STMT:slot_25>;  // aligned to BB_23 (sequential)
 switch (<FILL_COND:slot_26>) {  // aligned to BB_24 (switch_header)
 <FILL_STMT:slot_27>;  // aligned to BB_25 (sequential)
 while (<FILL_COND:slot_28>) {  // aligned to BB_38 (loop_header)
 <FILL_STMT:slot_29>;  // aligned to BB_39 (loop_body)
 <FILL_STMT:slot_30>;  // aligned to BB_32 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_32>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_33>;  // aligned to BB_29 (sequential)
 <FILL_STMT:slot_34>;  // aligned to BB_28 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_27 (sequential)
 <FILL_STMT:slot_36>;  // aligned to BB_26 (sequential)
 <FILL_STMT:slot_37>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_38>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_39>;  // aligned to BB_33 (sequential)
 <FILL_STMT:slot_40>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_41>;  // aligned to BB_37 (sequential)
 if (<FILL_COND:slot_42>) {  // aligned to BB_40 (if_header)
 while (<FILL_COND:slot_43>) {  // aligned to BB_41 (loop_header)
 if (<FILL_COND:slot_44>) {  // aligned to BB_42 (if_header)
 if (<FILL_COND:slot_45>) {  // aligned to BB_43 (if_header)
 if (<FILL_COND:slot_46>) {  // aligned to BB_44 (if_header)
 while (<FILL_COND:slot_47>) {  // aligned to BB_45 (loop_header)
 while (<FILL_COND:slot_48>) {  // aligned to BB_46 (loop_header)
 while (<FILL_COND:slot_49>) {  // aligned to BB_47 (loop_header)
 <FILL_STMT:slot_50>;  // aligned to BB_48 (sequential)
 if (<FILL_COND:slot_51>) {  // aligned to BB_49 (if_header)
 if (<FILL_COND:slot_52>) {  // aligned to BB_50 (if_header)
 <FILL_STMT:slot_54>;  // aligned to BB_52 (sequential)
 if (<FILL_COND:slot_55>) {  // aligned to BB_56 (if_header)
 if (<FILL_COND:slot_57>) {  // aligned to BB_86 (if_header)
 if (<FILL_COND:slot_58>) {  // aligned to BB_87 (if_header)
 while (<FILL_COND:slot_60>) {  // aligned to BB_89 (loop_header)
 <FILL_STMT:slot_61>;  // aligned to BB_90 (loop_body)
 if (<FILL_COND:slot_62>) {  // aligned to BB_91 (if_header)
 if (<FILL_COND:slot_63>) {  // aligned to BB_92 (if_header)
 <FILL_STMT:slot_65>;  // aligned to BB_94 (sequential)
 if (<FILL_COND:slot_66>) {  // aligned to BB_95 (if_header)
 <FILL_STMT:slot_68>;  // aligned to BB_97 (sequential)
 <FILL_STMT:slot_69>;  // aligned to BB_98 (sequential)
 if (<FILL_COND:slot_70>) {  // aligned to BB_58 (if_header)
 if (<FILL_COND:slot_71>) {  // aligned to BB_59 (if_header)
 if (<FILL_COND:slot_72>) {  // aligned to BB_60 (if_header)
 if (<FILL_COND:slot_73>) {  // aligned to BB_61 (if_header)
 if (<FILL_COND:slot_75>) {  // aligned to BB_63 (if_header)
 if (<FILL_COND:slot_76>) {  // aligned to BB_64 (if_header)
 if (<FILL_COND:slot_77>) {  // aligned to BB_65 (if_header)
 if (<FILL_COND:slot_78>) {  // aligned to BB_66 (if_header)
 if (<FILL_COND:slot_79>) {  // aligned to BB_67 (if_header)
 if (<FILL_COND:slot_80>) {  // aligned to BB_68 (if_header)
 if (<FILL_COND:slot_81>) {  // aligned to BB_69 (if_header)
 <FILL_STMT:slot_82>;  // aligned to BB_70 (sequential)
 if (<FILL_COND:slot_83>) {  // aligned to BB_72 (if_header)
 <FILL_STMT:slot_85>;  // aligned to BB_78 (sequential)
 <FILL_STMT:slot_86>;  // aligned to BB_85 (sequential)
 <FILL_STMT:slot_88>;  // aligned to BB_75 (sequential)
 <FILL_STMT:slot_89>;  // aligned to BB_77 (sequential)
 <FILL_STMT:slot_90>;  // aligned to BB_76 (sequential)
 <FILL_STMT:slot_91>;  // aligned to BB_71 (sequential)
 if (<FILL_COND:slot_92>) {  // aligned to BB_79 (if_header)
 if (<FILL_COND:slot_93>) {  // aligned to BB_80 (if_header)
 if (<FILL_COND:slot_94>) {  // aligned to BB_81 (if_header)
 <FILL_STMT:slot_96>;  // aligned to BB_84 (sequential)
 <FILL_STMT:slot_97>;  // aligned to BB_83 (sequential)
 if (<FILL_COND:slot_98>) {  // aligned to BB_53 (if_header)

 } else {
 <FILL_STMT:slot_99>;  // aligned to BB_54 (if_else_body)
 <FILL_STMT:slot_100>;  // aligned to BB_55 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_95>;  // aligned to BB_82 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_84>;  // aligned to BB_73 (if_else_body)
 <FILL_STMT:slot_87>;  // aligned to BB_74 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_74>;  // aligned to BB_62 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_67>;  // aligned to BB_96 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_64>;  // aligned to BB_93 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_59>;  // aligned to BB_88 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_56>;  // aligned to BB_57 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_53>;  // aligned to BB_51 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 34  input_line = 63  func = openat_proc_name  verdict = NEEDS_PATH_AUDIT
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/openat-proc.c
source_bounds = 48-154
bb = 15  src_rows = 52  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 6/5  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.833/0.500
bb_cover = 15/15  reasons = ['extra_if:1', 'filled_terminal_count_mismatch', 'small_if_count_gap']
10class_pattern_label_counts = sequential=3, if_header=6, if_else_body=6

-- SOURCE CONTROL OUTLINE --
if L70
    if L83
if L99
      if L104
          if L107

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_header
BB_4:if_else_body
BB_5:if_else_body
BB_6:sequential
BB_7:if_header
BB_8:if_else_body
BB_9:if_header
BB_10:if_header
BB_11:if_else_body
BB_12:if_else_body
BB_13:sequential
BB_14:sequential

-- REAL SOURCE FUNCTION --
   48: {
   49:   char *result = buf;
   50:   int dirlen;
   51: 
   52:   /* Make sure the caller gets ENOENT when appropriate.  */
   53:   if (!*file)
   54:     {
   55:       buf[0] = '\0';
   56:       return buf;
   57:     }
   58: 
   59: #if !(defined __KLIBC__ || defined __MVS__)
   60:   /* Generic code for Linux, Solaris, and similar platforms.  */
   61: # define PROC_SELF_FD_FORMAT "/proc/self/fd/%d/"
   62:   {
   63:     enum {
   64:       PROC_SELF_FD_DIR_SIZE_BOUND
   65:         = (sizeof PROC_SELF_FD_FORMAT - (sizeof "%d" - 1)
   66:            + INT_STRLEN_BOUND (int))
   67:     };
   68: 
   69:     static int proc_status = 0;
   70:     if (! proc_status)
   71:       {
   72:         /* Set PROC_STATUS to a positive value if /proc/self/fd is
   73:            reliable, and a negative value otherwise.  Solaris 10
   74:            /proc/self/fd mishandles "..", and any file name might expand
   75:            to ".." after symbolic link expansion, so avoid /proc/self/fd
   76:            if it mishandles "..".  Solaris 10 has openat, but this
   77:            problem is exhibited on code that built on Solaris 8 and
   78:            running on Solaris 10.  */
   79: 
   80:         int proc_self_fd =
   81:           open ("/proc/self/fd",
   82:                 O_SEARCH | O_DIRECTORY | O_NOCTTY | O_NONBLOCK | O_CLOEXEC);
   83:         if (proc_self_fd < 0)
   84:           proc_status = -1;
   85:         else
   86:           {
   87:             /* Detect whether /proc/self/fd/%i/../fd exists, where %i is the
   88:                number of a file descriptor open on /proc/self/fd.  On Linux,
   89:                that name resolves to /proc/self/fd, which was opened above.
   90:                However, on Solaris, it may resolve to /proc/self/fd/fd, which
   91:                cannot exist, since all names in /proc/self/fd are numeric.  */
   92:             char dotdot_buf[PROC_SELF_FD_DIR_SIZE_BOUND + sizeof "../fd" - 1];
   93:             sprintf (dotdot_buf, PROC_SELF_FD_FORMAT "../fd", proc_self_fd);
   94:             proc_status = access (dotdot_buf, F_OK) ? -1 : 1;
   95:             close (proc_self_fd);
   96:           }
   97:       }
   98: 
   99:     if (proc_status < 0)
  100:       return NULL;
  101:     else
  102:       {
  103:         size_t bufsize = PROC_SELF_FD_DIR_SIZE_BOUND + strlen (file);
  104:         if (OPENAT_BUFFER_SIZE < bufsize)
  105:           {
  106:             result = malloc (bufsize);
  107:             if (! result)
  108:               return NULL;
  109:           }
  110: 
  111:         dirlen = sprintf (result, PROC_SELF_FD_FORMAT, fd);
  112:       }
  113:   }
  114: #else /* (defined __KLIBC__ || defined __MVS__), i.e. OS/2 or z/OS */
  115:   /* OS/2 kLIBC provides a function to retrieve a path from a fd.  */
  116:   {
  117:     size_t bufsize;
  118: 
  119: # ifdef __KLIBC__
  120:     char dir[_MAX_PATH];
  121:     if (__libc_Back_ioFHToPath (fd, dir, sizeof dir))
  122:       return NULL;
  123: # endif
  124: # ifdef __MVS__
  125:     char dir[_XOPEN_PATH_MAX];
  126:     /* Documentation:
  127:        https://www.ibm.com/docs/en/zos/2.2.0?topic=functions-w-ioctl-w-pioctl-control-devices */
  128:     if (w_ioctl (fd, _IOCC_GPN, sizeof dir, dir) < 0)
  129:       return NULL;
  130:     /* Documentation:
  131:        https://www.ibm.com/docs/en/zos/2.2.0?topic=functions-e2a-l-convert-characters-from-ebcdic-ascii */
  132:     dirlen = __e2a_l (dir, strlen (dir));
  133:     if (dirlen < 0 || dirlen >= sizeof dir)
  134:       return NULL;
  135:     dir[dirlen] = '\0';
  136: # endif
  137: 
  138:     dirlen = strlen (dir);
  139:     bufsize = dirlen + 1 + strlen (file) + 1; /* 1 for '/', 1 for null */
  140:     if (OPENAT_BUFFER_SIZE < bufsize)
  141:       {
  142:         result = malloc (bufsize);
  143:         if (! result)
  144:           return NULL;
  145:       }
  146: 
  147:     strcpy (result, dir);
  148:     result[dirlen++] = '/';
  149:   }
  150: #endif
  151: 
  152:   strcpy (result + dirlen, file);
  153:   return result;
  154: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_14 (sequential)
 if (<FILL_COND:slot_5>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_10 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (if_then_body)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_12 (if_else_body)
 <FILL_STMT:slot_16>;  // aligned to BB_5 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_8 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 35  input_line = 64  func = process_files  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chcon.c
source_bounds = 311-342
bb = 9  src_rows = 20  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/4  gaps(if,loop,switch)=(-1,1,0)  sim(type,depth)=0.750/0.250
bb_cover = 9/9  reasons = ['missing_if:1', 'extra_loop:1']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=2, loop_header=1, loop_body=1, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L316
    if L321
        if L323
if L335

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:if_header
BB_3:if_else_body
BB_4:sequential
BB_5:loop_body
BB_6:if_header
BB_7:if_else_body
BB_8:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  311: {
  312:   bool ok = true;
  313: 
  314:   FTS *fts = xfts_open (files, bit_flags, nullptr);
  315: 
  316:   while (true)
  317:     {
  318:       FTSENT *ent;
  319: 
  320:       ent = fts_read (fts);
  321:       if (ent == nullptr)
  322:         {
  323:           if (errno != 0)
  324:             {
  325:               /* FIXME: try to give a better message  */
  326:               error (0, errno, _("fts_read failed"));
  327:               ok = false;
  328:             }
  329:           break;
  330:         }
  331: 
  332:       ok &= process_file (fts, ent);
  333:     }
  334: 
  335:   if (fts_close (fts) != 0)
  336:     {
  337:       error (0, errno, _("fts_close failed"));
  338:       ok = false;
  339:     }
  340: 
  341:   return ok;
  342: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 if (<FILL_COND:slot_7>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_9>;  // aligned to BB_8 (sequential)
 } else {
 <FILL_STMT:slot_8>;  // aligned to BB_7 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 }
 <FILL_STMT:slot_10>;  // aligned to BB_5 (loop_body)
 }
 }
}

========================================================================================================================
example = 36  input_line = 66  func = change_file_context  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chcon.c
source_bounds = 141-588
bb = 25  src_rows = 248  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 11/7  gaps(if,loop,switch)=(4,0,0)  sim(type,depth)=0.636/0.182
bb_cover = 25/25  reasons = ['extra_if:4', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=9, if_header=7, if_then_body=1, if_else_body=8

-- SOURCE CONTROL OUTLINE --
if L147
    if L153
    if L163
    if L170
if L180
    if L186
if L194

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if
                  if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:sequential
BB_4:if_header
BB_5:if_else_body
BB_6:sequential
BB_7:if_header
BB_8:if_else_body
BB_9:if_header
BB_10:if_then_body
BB_11:if_else_body
BB_12:if_else_body
BB_13:if_header
BB_14:if_else_body
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:if_header
BB_19:if_else_body
BB_20:sequential
BB_21:if_header
BB_22:if_else_body
BB_23:sequential
BB_24:sequential

-- REAL SOURCE FUNCTION --
  141: {
  142:   char *file_context = nullptr;
  143:   context_t context IF_LINT (= 0);
  144:   char const * context_string;
  145:   int errors = 0;
  146: 
  147:   if (specified_context == nullptr)
  148:     {
  149:       int status = (affect_symlink_referent
  150:                     ? getfileconat (fd, file, &file_context)
  151:                     : lgetfileconat (fd, file, &file_context));
  152: 
  153:       if (status < 0 && errno != ENODATA)
  154:         {
  155:           error (0, errno, _("failed to get security context of %s"),
  156:                  quoteaf (file));
  157:           return 1;
  158:         }
  159: 
  160:       /* If the file doesn't have a context, and we're not setting all of
  161:          the context components, there isn't really an obvious default.
  162:          Thus, we just give up. */
  163:       if (file_context == nullptr)
  164:         {
  165:           error (0, 0, _("can't apply partial context to unlabeled file %s"),
  166:                  quoteaf (file));
  167:           return 1;
  168:         }
  169: 
  170:       if (compute_context_from_mask (file_context, &context))
  171:         return 1;
  172: 
  173:       context_string = context_str (context);
  174:     }
  175:   else
  176:     {
  177:       context_string = specified_context;
  178:     }
  179: 
  180:   if (file_context == nullptr || ! STREQ (context_string, file_context))
  181:     {
  182:       int fail = (affect_symlink_referent
  183:                   ?  setfileconat (fd, file, context_string)
  184:                   : lsetfileconat (fd, file, context_string));
  185: 
  186:       if (fail)
  187:         {
  188:           errors = 1;
  189:           error (0, errno, _("failed to change context of %s to %s"),
  190:                  quoteaf_n (0, file), quote_n (1, context_string));
  191:         }
  192:     }
  193: 
  194:   if (specified_context == nullptr)
  195:     {
  196:       context_free (context);
  197:       freecon (file_context);
  198:     }
  199: 
  200:   return errors;
  201: }
  202: 
  203: /* Change the context of FILE.
  204:    Return true if successful.  This function is called
  205:    once for every file system object that fts encounters.  */
  206: 
  207: static bool
  208: process_file (FTS *fts, FTSENT *ent)
  209: {
  210:   char const *file_full_name = ent->fts_path;
  211:   char const *file = ent->fts_accpath;
  212:   const struct stat *file_stats = ent->fts_statp;
  213:   bool ok = true;
  214: 
  215:   switch (ent->fts_info)
  216:     {
  217:     case FTS_D:
  218:       if (recurse)
  219:         {
  220:           if (ROOT_DEV_INO_CHECK (root_dev_ino, ent->fts_statp))
  221:             {
  222:               /* This happens e.g., with "chcon -R --preserve-root ... /"
  223:                  and with "chcon -RH --preserve-root ... symlink-to-root".  */
  224:               ROOT_DEV_INO_WARN (file_full_name);
  225:               /* Tell fts not to traverse into this hierarchy.  */
  226:               fts_set (fts, ent, FTS_SKIP);
  227:               /* Ensure that we do not process "/" on the second visit.  */
  228:               ignore_value (fts_read (fts));
  229:               return false;
  230:             }
  231:           return true;
  232:         }
  233:       break;
  234: 
  235:     case FTS_DP:
  236:       if (! recurse)
  237:         return true;
  238:       break;
  239: 
  240:     case FTS_NS:
  241:       /* For a top-level file or directory, this FTS_NS (stat failed)
  242:          indicator is determined at the time of the initial fts_open call.
  243:          With programs like chmod, chown, and chgrp, that modify
  244:          permissions, it is possible that the file in question is
  245:          accessible when control reaches this point.  So, if this is
  246:          the first time we've seen the FTS_NS for this file, tell
  247:          fts_read to stat it "again".  */
  248:       if (ent->fts_level == 0 && ent->fts_number == 0)
  249:         {
  250:           ent->fts_number = 1;
  251:           fts_set (fts, ent, FTS_AGAIN);
  252:           return true;
  253:         }
  254:       error (0, ent->fts_errno, _("cannot access %s"),
  255:              quoteaf (file_full_name));
  256:       ok = false;
  257:       break;
  258: 
  259:     case FTS_ERR:
  260:       error (0, ent->fts_errno, "%s", quotef (file_full_name));
  261:       ok = false;
  262:       break;
  263: 
  264:     case FTS_DNR:
  265:       error (0, ent->fts_errno, _("cannot read directory %s"),
  266:              quoteaf (file_full_name));
  267:       ok = false;
  268:       break;
  269: 
  270:     case FTS_DC:		/* directory that causes cycles */
  271:       if (cycle_warning_required (fts, ent))
  272:         {
  273:           emit_cycle_warning (file_full_name);
  274:           return false;
  275:         }
  276:       break;
  277: 
  278:     default:
  279:       break;
  280:     }
  281: 
  282:   if (ent->fts_info == FTS_DP
  283:       && ok && ROOT_DEV_INO_CHECK (root_dev_ino, file_stats))
  284:     {
  285:       ROOT_DEV_INO_WARN (file_full_name);
  286:       ok = false;
  287:     }
  288: 
  289:   if (ok)
  290:     {
  291:       if (verbose)
  292:         printf (_("changing security context of %s\n"),
  293:                 quoteaf (file_full_name));
  294: 
  295:       if (change_file_context (fts->fts_cwd_fd, file) != 0)
  296:         ok = false;
  297:     }
  298: 
  299:   if ( ! recurse)
  300:     fts_set (fts, ent, FTS_SKIP);
  301: 
  302:   return ok;
  303: }
  304: 
  305: /* Recursively operate on the specified FILES (the last entry
  306:    of which is null).  BIT_FLAGS controls how fts works.
  307:    Return true if successful.  */
  308: 
  309: static bool
  310: process_files (char **files, int bit_flags)
  311: {
  312:   bool ok = true;
  313: 
  314:   FTS *fts = xfts_open (files, bit_flags, nullptr);
  315: 
  316:   while (true)
  317:     {
  318:       FTSENT *ent;
  319: 
  320:       ent = fts_read (fts);
  321:       if (ent == nullptr)
  322:         {
  323:           if (errno != 0)
  324:             {
  325:               /* FIXME: try to give a better message  */
  326:               error (0, errno, _("fts_read failed"));
  327:               ok = false;
  328:             }
  329:           break;
  330:         }
  331: 
  332:       ok &= process_file (fts, ent);
  333:     }
  334: 
  335:   if (fts_close (fts) != 0)
  336:     {
  337:       error (0, errno, _("fts_close failed"));
  338:       ok = false;
  339:     }
  340: 
  341:   return ok;
  342: }
  343: 
  344: void
  345: usage (int status)
  346: {
  347:   if (status != EXIT_SUCCESS)
  348:     emit_try_help ();
  349:   else
  350:     {
  351:       printf (_("\
  352: Usage: %s [OPTION]... CONTEXT FILE...\n\
  353:   or:  %s [OPTION]... [-u USER] [-r ROLE] [-l RANGE] [-t TYPE] FILE...\n\
  354:   or:  %s [OPTION]... --reference=RFILE FILE...\n\
  355: "),
  356:         program_name, program_name, program_name);
  357:       fputs (_("\
  358: Change the SELinux security context of each FILE to CONTEXT.\n\
  359: With --reference, change the security context of each FILE to that of RFILE.\n\
  360: "), stdout);
  361: 
  362:       emit_mandatory_arg_note ();
  363: 
  364:       fputs (_("\
  365:       --dereference      affect the referent of each symbolic link (this is\n\
  366:                          the default), rather than the symbolic link itself\n\
  367:   -h, --no-dereference   affect symbolic links instead of any referenced file\n\
  368: "), stdout);
  369:       fputs (_("\
  370:   -u, --user=USER        set user USER in the target security context\n\
  371:   -r, --role=ROLE        set role ROLE in the target security context\n\
  372:   -t, --type=TYPE        set type TYPE in the target security context\n\
  373:   -l, --range=RANGE      set range RANGE in the target security context\n\
  374: "), stdout);
  375:       fputs (_("\
  376:       --no-preserve-root  do not treat '/' specially (the default)\n\
  377:       --preserve-root    fail to operate recursively on '/'\n\
  378: "), stdout);
  379:       fputs (_("\
  380:       --reference=RFILE  use RFILE's security context rather than specifying\n\
  381:                          a CONTEXT value\n\
  382: "), stdout);
  383:       fputs (_("\
  384:   -R, --recursive        operate on files and directories recursively\n\
  385: "), stdout);
  386:       fputs (_("\
  387:   -v, --verbose          output a diagnostic for every file processed\n\
  388: "), stdout);
  389:       fputs (_("\
  390: \n\
  391: The following options modify how a hierarchy is traversed when the -R\n\
  392: option is also specified.  If more than one is specified, only the final\n\
  393: one takes effect.\n\
  394: \n\
  395:   -H                     if a command line argument is a symbolic link\n\
  396:                          to a directory, traverse it\n\
  397:   -L                     traverse every symbolic link to a directory\n\
  398:                          encountered\n\
  399:   -P                     do not traverse any symbolic links (default)\n\
  400: \n\
  401: "), stdout);
  402:       fputs (HELP_OPTION_DESCRIPTION, stdout);
  403:       fputs (VERSION_OPTION_DESCRIPTION, stdout);
  404:       emit_ancillary_info (PROGRAM_NAME);
  405:     }
  406:   exit (status);
  407: }
  408: 
  409: int
  410: main (int argc, char **argv)
  411: {
  412:   /* Bit flags that control how fts works.  */
  413:   int bit_flags = FTS_PHYSICAL;
  414: 
  415:   /* 1 if --dereference, 0 if --no-dereference, -1 if neither has been
  416:      specified.  */
  417:   int dereference = -1;
  418: 
  419:   bool ok;
  420:   bool preserve_root = false;
  421:   bool component_specified = false;
  422:   char *reference_file = nullptr;
  423:   int optc;
  424: 
  425:   initialize_main (&argc, &argv);
  426:   set_program_name (argv[0]);
  427:   setlocale (LC_ALL, "");
  428:   bindtextdomain (PACKAGE, LOCALEDIR);
  429:   textdomain (PACKAGE);
  430: 
  431:   atexit (close_stdout);
  432: 
  433:   while ((optc = getopt_long (argc, argv, "HLPRhvu:r:t:l:",
  434:                               long_options, nullptr))
  435:          != -1)
  436:     {
  437:       switch (optc)
  438:         {
  439:         case 'H': /* Traverse command-line symlinks-to-directories.  */
  440:           bit_flags = FTS_COMFOLLOW | FTS_PHYSICAL;
  441:           break;
  442: 
  443:         case 'L': /* Traverse all symlinks-to-directories.  */
  444:           bit_flags = FTS_LOGICAL;
  445:           break;
  446: 
  447:         case 'P': /* Traverse no symlinks-to-directories.  */
  448:           bit_flags = FTS_PHYSICAL;
  449:           break;
  450: 
  451:         case 'h': /* --no-dereference: affect symlinks */
  452:           dereference = 0;
  453:           break;
  454: 
  455:         case DEREFERENCE_OPTION: /* --dereference: affect the referent
  456:                                     of each symlink */
  457:           dereference = 1;
  458:           break;
  459: 
  460:         case NO_PRESERVE_ROOT:
  461:           preserve_root = false;
  462:           break;
  463: 
  464:         case PRESERVE_ROOT:
  465:           preserve_root = true;
  466:           break;
  467: 
  468:         case REFERENCE_FILE_OPTION:
  469:           reference_file = optarg;
  470:           break;
  471: 
  472:         case 'R':
  473:           recurse = true;
  474:           break;
  475: 
  476:         case 'f':
  477:           /* ignore */
  478:           break;
  479: 
  480:         case 'v':
  481:           verbose = true;
  482:           break;
  483: 
  484:         case 'u':
  485:           specified_user = optarg;
  486:           component_specified = true;
  487:           break;
  488: 
  489:         case 'r':
  490:           specified_role = optarg;
  491:           component_specified = true;
  492:           break;
  493: 
  494:         case 't':
  495:           specified_type = optarg;
  496:           component_specified = true;
  497:           break;
  498: 
  499:         case 'l':
  500:           specified_range = optarg;
  501:           component_specified = true;
  502:           break;
  503: 
  504:         case_GETOPT_HELP_CHAR;
  505:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  506:         default:
  507:           usage (EXIT_FAILURE);
  508:         }
  509:     }
  510: 
  511:   if (recurse)
  512:     {
  513:       if (bit_flags == FTS_PHYSICAL)
  514:         {
  515:           if (dereference == 1)
  516:             error (EXIT_FAILURE, 0,
  517:                    _("-R --dereference requires either -H or -L"));
  518:           affect_symlink_referent = false;
  519:         }
  520:       else
  521:         {
  522:           if (dereference == 0)
  523:             error (EXIT_FAILURE, 0, _("-R -h requires -P"));
  524:           affect_symlink_referent = true;
  525:         }
  526:     }
  527:   else
  528:     {
  529:       bit_flags = FTS_PHYSICAL;
  530:       affect_symlink_referent = (dereference != 0);
  531:     }
  532: 
  533:   if (argc - optind < (reference_file || component_specified ? 1 : 2))
  534:     {
  535:       if (argc <= optind)
  536:         error (0, 0, _("missing operand"));
  537:       else
  538:         error (0, 0, _("missing operand after %s"), quote (argv[argc - 1]));
  539:       usage (EXIT_FAILURE);
  540:     }
  541: 
  542:   if (reference_file)
  543:     {
  544:       char *ref_context = nullptr;
  545: 
  546:       if (getfilecon (reference_file, &ref_context) < 0)
  547:         error (EXIT_FAILURE, errno, _("failed to get security context of %s"),
  548:                quoteaf (reference_file));
  549: 
  550:       specified_context = ref_context;
  551:     }
  552:   else if (component_specified)
  553:     {
  554:       /* FIXME: it's already null, so this is a no-op. */
  555:       specified_context = nullptr;
  556:     }
  557:   else
  558:     {
  559:       specified_context = argv[optind++];
  560:       if (0 < is_selinux_enabled ()
  561:           && security_check_context (specified_context) < 0)
  562:         error (EXIT_FAILURE, errno, _("invalid context: %s"),
  563:                quote (specified_context));
  564:     }
  565: 
  566:   if (reference_file && component_specified)
  567:     {
  568:       error (0, 0, _("conflicting security context specifiers given"));
  569:       usage (EXIT_FAILURE);
  570:     }
  571: 
  572:   if (recurse && preserve_root)
  573:     {
  574:       static struct dev_ino dev_ino_buf;
  575:       root_dev_ino = get_root_dev_ino (&dev_ino_buf);
  576:       if (root_dev_ino == nullptr)
  577:         error (EXIT_FAILURE, errno, _("failed to get attributes of %s"),
  578:                quoteaf ("/"));
  579:     }
  580:   else
  581:     {
  582:       root_dev_ino = nullptr;
  583:     }
  584: 
  585:   ok = process_files (argv + optind, bit_flags | FTS_NOSTAT);
  586: 
  587:   return ok ? EXIT_SUCCESS : EXIT_FAILURE;
  588: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 if (<FILL_COND:slot_5>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_24 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_12>;  // aligned to BB_10 (if_then_body)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 if (<FILL_COND:slot_14>) {  // aligned to BB_13 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_14 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_15 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_16 (sequential)
 if (<FILL_COND:slot_18>) {  // aligned to BB_18 (if_header)
 <FILL_STMT:slot_20>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_21>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_23>;  // aligned to BB_23 (sequential)
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_22 (if_else_body)
 <FILL_STMT:slot_24>;  // aligned to BB_17 (if_else_body)
 <FILL_STMT:slot_25>;  // aligned to BB_3 (if_else_body)
 <FILL_STMT:slot_26>;  // aligned to BB_12 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_19 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_8 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_6 (if_else_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 37  input_line = 67  func = compute_context_from_mask  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chcon.c
source_bounds = 94-133
bb = 26  src_rows = 19  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 9/2  gaps(if,loop,switch)=(7,0,0)  sim(type,depth)=0.222/0.111
bb_cover = 26/26  reasons = ['extra_if:7']
10class_pattern_label_counts = sequential=20, if_header=2, if_then_body=1, if_else_body=3

-- SOURCE CONTROL OUTLINE --
if L97
if L123

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:sequential
BB_10:sequential
BB_11:sequential
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:sequential
BB_22:if_header
BB_23:if_else_body
BB_24:if_else_body
BB_25:sequential

-- REAL SOURCE FUNCTION --
   94: {
   95:   bool ok = true;
   96:   context_t new_context = context_new (context);
   97:   if (!new_context)
   98:     {
   99:       error (0, errno, _("failed to create security context: %s"),
  100:              quote (context));
  101:       return 1;
  102:     }
  103: 
  104: #define SET_COMPONENT(C, comp)						\
  105:    do									\
  106:      {									\
  107:        if (specified_ ## comp						\
  108:            && context_ ## comp ## _set ((C), specified_ ## comp))	\
  109:          {								\
  110:             error (0, errno,						\
  111:                    _("failed to set %s security context component to %s"), \
  112:                    #comp, quote (specified_ ## comp));			\
  113:            ok = false;							\
  114:          }								\
  115:      }									\
  116:    while (0)
  117: 
  118:   SET_COMPONENT (new_context, user);
  119:   SET_COMPONENT (new_context, range);
  120:   SET_COMPONENT (new_context, role);
  121:   SET_COMPONENT (new_context, type);
  122: 
  123:   if (!ok)
  124:     {
  125:       int saved_errno = errno;
  126:       context_free (new_context);
  127:       errno = saved_errno;
  128:       return 1;
  129:     }
  130: 
  131:   *ret = new_context;
  132:   return 0;
  133: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_25 (sequential)
 if (<FILL_COND:slot_6>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_6 (sequential)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (sequential)
 if (<FILL_COND:slot_11>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_11 (sequential)
 <FILL_STMT:slot_15>;  // aligned to BB_12 (sequential)
 if (<FILL_COND:slot_16>) {  // aligned to BB_13 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_14 (if_header)
 <FILL_STMT:slot_19>;  // aligned to BB_16 (sequential)
 if (<FILL_COND:slot_21>) {  // aligned to BB_18 (if_header)
 if (<FILL_COND:slot_22>) {  // aligned to BB_19 (if_header)
 <FILL_STMT:slot_24>;  // aligned to BB_21 (sequential)
 if (<FILL_COND:slot_25>) {  // aligned to BB_22 (if_header)
 <FILL_STMT:slot_27>;  // aligned to BB_24 (sequential)
 } else {
 <FILL_STMT:slot_26>;  // aligned to BB_23 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_20 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_15 (if_else_body)
 <FILL_STMT:slot_20>;  // aligned to BB_17 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_13>;  // aligned to BB_10 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_5>;  // aligned to BB_2 (if_else_body)
 }
}

========================================================================================================================
example = 38  input_line = 68  func = usage  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chcon.c
source_bounds = 346-407
bb = 5  src_rows = 18  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 5/5  reasons = []
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L347

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_else_body
BB_4:sequential

-- REAL SOURCE FUNCTION --
  346: {
  347:   if (status != EXIT_SUCCESS)
  348:     emit_try_help ();
  349:   else
  350:     {
  351:       printf (_("\
  352: Usage: %s [OPTION]... CONTEXT FILE...\n\
  353:   or:  %s [OPTION]... [-u USER] [-r ROLE] [-l RANGE] [-t TYPE] FILE...\n\
  354:   or:  %s [OPTION]... --reference=RFILE FILE...\n\
  355: "),
  356:         program_name, program_name, program_name);
  357:       fputs (_("\
  358: Change the SELinux security context of each FILE to CONTEXT.\n\
  359: With --reference, change the security context of each FILE to that of RFILE.\n\
  360: "), stdout);
  361: 
  362:       emit_mandatory_arg_note ();
  363: 
  364:       fputs (_("\
  365:       --dereference      affect the referent of each symbolic link (this is\n\
  366:                          the default), rather than the symbolic link itself\n\
  367:   -h, --no-dereference   affect symbolic links instead of any referenced file\n\
  368: "), stdout);
  369:       fputs (_("\
  370:   -u, --user=USER        set user USER in the target security context\n\
  371:   -r, --role=ROLE        set role ROLE in the target security context\n\
  372:   -t, --type=TYPE        set type TYPE in the target security context\n\
  373:   -l, --range=RANGE      set range RANGE in the target security context\n\
  374: "), stdout);
  375:       fputs (_("\
  376:       --no-preserve-root  do not treat '/' specially (the default)\n\
  377:       --preserve-root    fail to operate recursively on '/'\n\
  378: "), stdout);
  379:       fputs (_("\
  380:       --reference=RFILE  use RFILE's security context rather than specifying\n\
  381:                          a CONTEXT value\n\
  382: "), stdout);
  383:       fputs (_("\
  384:   -R, --recursive        operate on files and directories recursively\n\
  385: "), stdout);
  386:       fputs (_("\
  387:   -v, --verbose          output a diagnostic for every file processed\n\
  388: "), stdout);
  389:       fputs (_("\
  390: \n\
  391: The following options modify how a hierarchy is traversed when the -R\n\
  392: option is also specified.  If more than one is specified, only the final\n\
  393: one takes effect.\n\
  394: \n\
  395:   -H                     if a command line argument is a symbolic link\n\
  396:                          to a directory, traverse it\n\
  397:   -L                     traverse every symbolic link to a directory\n\
  398:                          encountered\n\
  399:   -P                     do not traverse any symbolic links (default)\n\
  400: \n\
  401: "), stdout);
  402:       fputs (HELP_OPTION_DESCRIPTION, stdout);
  403:       fputs (VERSION_OPTION_DESCRIPTION, stdout);
  404:       emit_ancillary_info (PROGRAM_NAME);
  405:     }
  406:   exit (status);
  407: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 }
}

========================================================================================================================
example = 39  input_line = 70  func = parse_group  verdict = NEEDS_PATH_AUDIT
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chgrp.c
source_bounds = 78-99
bb = 9  src_rows = 14  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.750/0.500
bb_cover = 9/9  reasons = ['extra_if:1', 'filled_terminal_count_mismatch', 'small_if_count_gap']
10class_pattern_label_counts = sequential=3, if_header=4, if_else_body=1, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L81
    if L84
          if L89

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:if_header
BB_4:if_header
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
   78: {
   79:   gid_t gid = -1;
   80: 
   81:   if (*name)
   82:     {
   83:       struct group *grp = getgrnam (name);
   84:       if (grp)
   85:         gid = grp->gr_gid;
   86:       else
   87:         {
   88:           uintmax_t tmp;
   89:           if (! (xstrtoumax (name, nullptr, 10, &tmp, "") == LONGINT_OK
   90:                  && tmp <= GID_T_MAX))
   91:             error (EXIT_FAILURE, 0, _("invalid group: %s"),
   92:                    quote (name));
   93:           gid = tmp;
   94:         }
   95:       endgrent ();		/* Save a file descriptor. */
   96:     }
   97: 
   98:   return gid;
   99: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_8 (sequential)
 if (<FILL_COND:slot_7>) {  // aligned to BB_3 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_4 (if_header)
 <FILL_STMT:slot_9>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_10>;  // aligned to BB_6 (sequential)
 }
 }
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 40  input_line = 71  func = usage  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chgrp.c
source_bounds = 103-168
bb = 5  src_rows = 18  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 5/5  reasons = []
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L104

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_else_body
BB_4:sequential

-- REAL SOURCE FUNCTION --
  103: {
  104:   if (status != EXIT_SUCCESS)
  105:     emit_try_help ();
  106:   else
  107:     {
  108:       printf (_("\
  109: Usage: %s [OPTION]... GROUP FILE...\n\
  110:   or:  %s [OPTION]... --reference=RFILE FILE...\n\
  111: "),
  112:               program_name, program_name);
  113:       fputs (_("\
  114: Change the group of each FILE to GROUP.\n\
  115: With --reference, change the group of each FILE to that of RFILE.\n\
  116: \n\
  117: "), stdout);
  118:       fputs (_("\
  119:   -c, --changes          like verbose but report only when a change is made\n\
  120:   -f, --silent, --quiet  suppress most error messages\n\
  121:   -v, --verbose          output a diagnostic for every file processed\n\
  122: "), stdout);
  123:       fputs (_("\
  124:       --dereference      affect the referent of each symbolic link (this is\n\
  125:                          the default), rather than the symbolic link itself\n\
  126:   -h, --no-dereference   affect symbolic links instead of any referenced file\n\
  127: "), stdout);
  128:       fputs (_("\
  129:                          (useful only on systems that can change the\n\
  130:                          ownership of a symlink)\n\
  131: "), stdout);
  132:       fputs (_("\
  133:       --no-preserve-root  do not treat '/' specially (the default)\n\
  134:       --preserve-root    fail to operate recursively on '/'\n\
  135: "), stdout);
  136:       fputs (_("\
  137:       --reference=RFILE  use RFILE's group rather than specifying a GROUP.\n\
  138:                          RFILE is always dereferenced if a symbolic link.\n\
  139: "), stdout);
  140:       fputs (_("\
  141:   -R, --recursive        operate on files and directories recursively\n\
  142: "), stdout);
  143:       fputs (_("\
  144: \n\
  145: The following options modify how a hierarchy is traversed when the -R\n\
  146: option is also specified.  If more than one is specified, only the final\n\
  147: one takes effect.\n\
  148: \n\
  149:   -H                     if a command line argument is a symbolic link\n\
  150:                          to a directory, traverse it\n\
  151:   -L                     traverse every symbolic link to a directory\n\
  152:                          encountered\n\
  153:   -P                     do not traverse any symbolic links (default)\n\
  154: \n\
  155: "), stdout);
  156:       fputs (HELP_OPTION_DESCRIPTION, stdout);
  157:       fputs (VERSION_OPTION_DESCRIPTION, stdout);
  158:       printf (_("\
  159: \n\
  160: Examples:\n\
  161:   %s staff /u      Change the group of /u to \"staff\".\n\
  162:   %s -hR staff /u  Change the group of /u and subfiles to \"staff\".\n\
  163: "),
  164:               program_name, program_name);
  165:       emit_ancillary_info (PROGRAM_NAME);
  166:     }
  167:   exit (status);
  168: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 }
}

========================================================================================================================
example = 41  input_line = 72  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chgrp.c
source_bounds = 172-315
bb = 72  src_rows = 95  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 26/11  gaps(if,loop,switch)=(0,2,13)  sim(type,depth)=0.423/0.077
bb_cover = 72/72  reasons = ['extra_loop:2', 'extra_switch:13', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=37, if_header=9, if_else_body=8, loop_header=3, loop_body=1, switch_header=14

-- SOURCE CONTROL OUTLINE --
loop L197
    switch L200
if L258
    if L260
        if L262
if L274
    if L276
if L283
    if L286
if L300
    if L304

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      switch
        switch
          switch
            switch
              switch
                switch
                  switch
                    switch
                    switch
                    switch
                    switch
                    switch
                    switch
                    loop
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:switch_header
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:switch_header
BB_13:sequential
BB_14:switch_header
BB_15:sequential
BB_16:switch_header
BB_17:sequential
BB_18:switch_header
BB_19:sequential
BB_20:switch_header
BB_21:sequential
BB_22:switch_header
BB_23:sequential
BB_24:switch_header
BB_25:sequential
BB_26:switch_header
BB_27:sequential
BB_28:switch_header
BB_29:sequential
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:sequential
BB_39:sequential
BB_40:sequential
BB_41:sequential
BB_42:sequential
BB_43:sequential
BB_44:loop_header
BB_45:loop_body
BB_46:if_header
BB_47:if_header
BB_48:if_header
BB_49:if_else_body
BB_50:sequential
BB_51:sequential
BB_52:if_else_body
BB_53:if_header
BB_54:if_header
BB_55:if_else_body
BB_56:if_else_body
BB_57:sequential
BB_58:if_header
BB_59:if_header
BB_60:if_else_body
BB_61:sequential
BB_62:if_else_body
BB_63:sequential
BB_64:sequential
BB_65:sequential
BB_66:if_header
BB_67:if_else_body
BB_68:if_header
BB_69:if_else_body
BB_70:sequential
BB_71:sequential

-- REAL SOURCE FUNCTION --
  172: {
  173:   bool preserve_root = false;
  174:   gid_t gid;
  175: 
  176:   /* Bit flags that control how fts works.  */
  177:   int bit_flags = FTS_PHYSICAL;
  178: 
  179:   /* 1 if --dereference, 0 if --no-dereference, -1 if neither has been
  180:      specified.  */
  181:   int dereference = -1;
  182: 
  183:   struct Chown_option chopt;
  184:   bool ok;
  185:   int optc;
  186: 
  187:   initialize_main (&argc, &argv);
  188:   set_program_name (argv[0]);
  189:   setlocale (LC_ALL, "");
  190:   bindtextdomain (PACKAGE, LOCALEDIR);
  191:   textdomain (PACKAGE);
  192: 
  193:   atexit (close_stdout);
  194: 
  195:   chopt_init (&chopt);
  196: 
  197:   while ((optc = getopt_long (argc, argv, "HLPRcfhv", long_options, nullptr))
  198:          != -1)
  199:     {
  200:       switch (optc)
  201:         {
  202:         case 'H': /* Traverse command-line symlinks-to-directories.  */
  203:           bit_flags = FTS_COMFOLLOW | FTS_PHYSICAL;
  204:           break;
  205: 
  206:         case 'L': /* Traverse all symlinks-to-directories.  */
  207:           bit_flags = FTS_LOGICAL;
  208:           break;
  209: 
  210:         case 'P': /* Traverse no symlinks-to-directories.  */
  211:           bit_flags = FTS_PHYSICAL;
  212:           break;
  213: 
  214:         case 'h': /* --no-dereference: affect symlinks */
  215:           dereference = 0;
  216:           break;
  217: 
  218:         case DEREFERENCE_OPTION: /* --dereference: affect the referent
  219:                                     of each symlink */
  220:           dereference = 1;
  221:           break;
  222: 
  223:         case NO_PRESERVE_ROOT:
  224:           preserve_root = false;
  225:           break;
  226: 
  227:         case PRESERVE_ROOT:
  228:           preserve_root = true;
  229:           break;
  230: 
  231:         case REFERENCE_FILE_OPTION:
  232:           reference_file = optarg;
  233:           break;
  234: 
  235:         case 'R':
  236:           chopt.recurse = true;
  237:           break;
  238: 
  239:         case 'c':
  240:           chopt.verbosity = V_changes_only;
  241:           break;
  242: 
  243:         case 'f':
  244:           chopt.force_silent = true;
  245:           break;
  246: 
  247:         case 'v':
  248:           chopt.verbosity = V_high;
  249:           break;
  250: 
  251:         case_GETOPT_HELP_CHAR;
  252:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  253:         default:
  254:           usage (EXIT_FAILURE);
  255:         }
  256:     }
  257: 
  258:   if (chopt.recurse)
  259:     {
  260:       if (bit_flags == FTS_PHYSICAL)
  261:         {
  262:           if (dereference == 1)
  263:             error (EXIT_FAILURE, 0,
  264:                    _("-R --dereference requires either -H or -L"));
  265:           dereference = 0;
  266:         }
  267:     }
  268:   else
  269:     {
  270:       bit_flags = FTS_PHYSICAL;
  271:     }
  272:   chopt.affect_symlink_referent = (dereference != 0);
  273: 
  274:   if (argc - optind < (reference_file ? 1 : 2))
  275:     {
  276:       if (argc <= optind)
  277:         error (0, 0, _("missing operand"));
  278:       else
  279:         error (0, 0, _("missing operand after %s"), quote (argv[argc - 1]));
  280:       usage (EXIT_FAILURE);
  281:     }
  282: 
  283:   if (reference_file)
  284:     {
  285:       struct stat ref_stats;
  286:       if (stat (reference_file, &ref_stats))
  287:         error (EXIT_FAILURE, errno, _("failed to get attributes of %s"),
  288:                quoteaf (reference_file));
  289: 
  290:       gid = ref_stats.st_gid;
  291:       chopt.group_name = gid_to_name (ref_stats.st_gid);
  292:     }
  293:   else
  294:     {
  295:       char *group_name = argv[optind++];
  296:       chopt.group_name = (*group_name ? xstrdup (group_name) : nullptr);
  297:       gid = parse_group (group_name);
  298:     }
  299: 
  300:   if (chopt.recurse && preserve_root)
  301:     {
  302:       static struct dev_ino dev_ino_buf;
  303:       chopt.root_dev_ino = get_root_dev_ino (&dev_ino_buf);
  304:       if (chopt.root_dev_ino == nullptr)
  305:         error (EXIT_FAILURE, errno, _("failed to get attributes of %s"),
  306:                quoteaf ("/"));
  307:     }
  308: 
  309:   bit_flags |= FTS_DEFER_STAT;
  310:   ok = chown_files (argv + optind, bit_flags,
  311:                     (uid_t) -1, gid,
  312:                     (uid_t) -1, (gid_t) -1, &chopt);
  313: 
  314:   main_exit (ok ? EXIT_SUCCESS : EXIT_FAILURE);
  315: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 switch (<FILL_COND:slot_8>) {  // aligned to BB_6 (switch_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 switch (<FILL_COND:slot_10>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_12>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 switch (<FILL_COND:slot_14>) {  // aligned to BB_12 (switch_header)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 switch (<FILL_COND:slot_16>) {  // aligned to BB_14 (switch_header)
 <FILL_STMT:slot_17>;  // aligned to BB_15 (sequential)
 switch (<FILL_COND:slot_18>) {  // aligned to BB_16 (switch_header)
 <FILL_STMT:slot_19>;  // aligned to BB_17 (sequential)
 switch (<FILL_COND:slot_20>) {  // aligned to BB_18 (switch_header)
 <FILL_STMT:slot_21>;  // aligned to BB_19 (sequential)
 switch (<FILL_COND:slot_22>) {  // aligned to BB_20 (switch_header)
 <FILL_STMT:slot_23>;  // aligned to BB_21 (sequential)
 switch (<FILL_COND:slot_24>) {  // aligned to BB_22 (switch_header)
 <FILL_STMT:slot_25>;  // aligned to BB_23 (sequential)
 switch (<FILL_COND:slot_26>) {  // aligned to BB_24 (switch_header)
 <FILL_STMT:slot_27>;  // aligned to BB_25 (sequential)
 switch (<FILL_COND:slot_28>) {  // aligned to BB_26 (switch_header)
 <FILL_STMT:slot_29>;  // aligned to BB_27 (sequential)
 switch (<FILL_COND:slot_30>) {  // aligned to BB_28 (switch_header)
 <FILL_STMT:slot_31>;  // aligned to BB_29 (sequential)
 while (<FILL_COND:slot_32>) {  // aligned to BB_44 (loop_header)
 <FILL_STMT:slot_33>;  // aligned to BB_45 (loop_body)
 <FILL_STMT:slot_34>;  // aligned to BB_37 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_36>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_37>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_38>;  // aligned to BB_41 (sequential)
 <FILL_STMT:slot_39>;  // aligned to BB_33 (sequential)
 <FILL_STMT:slot_40>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_41>;  // aligned to BB_39 (sequential)
 <FILL_STMT:slot_42>;  // aligned to BB_38 (sequential)
 <FILL_STMT:slot_43>;  // aligned to BB_32 (sequential)
 <FILL_STMT:slot_44>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_45>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_42 (sequential)
 <FILL_STMT:slot_47>;  // aligned to BB_43 (sequential)
 if (<FILL_COND:slot_48>) {  // aligned to BB_46 (if_header)
 if (<FILL_COND:slot_49>) {  // aligned to BB_47 (if_header)
 if (<FILL_COND:slot_50>) {  // aligned to BB_48 (if_header)
 <FILL_STMT:slot_52>;  // aligned to BB_50 (sequential)
 <FILL_STMT:slot_53>;  // aligned to BB_51 (sequential)
 if (<FILL_COND:slot_54>) {  // aligned to BB_53 (if_header)
 if (<FILL_COND:slot_55>) {  // aligned to BB_54 (if_header)
 <FILL_STMT:slot_57>;  // aligned to BB_57 (sequential)
 if (<FILL_COND:slot_58>) {  // aligned to BB_58 (if_header)
 <FILL_STMT:slot_61>;  // aligned to BB_61 (sequential)
 if (<FILL_COND:slot_62>) {  // aligned to BB_66 (if_header)
 if (<FILL_COND:slot_63>) {  // aligned to BB_67 (if_header)
 if (<FILL_COND:slot_64>) {  // aligned to BB_68 (if_header)
 <FILL_STMT:slot_66>;  // aligned to BB_70 (sequential)
 <FILL_STMT:slot_67>;  // aligned to BB_71 (sequential)
 <FILL_STMT:slot_69>;  // aligned to BB_63 (sequential)
 <FILL_STMT:slot_70>;  // aligned to BB_65 (sequential)
 <FILL_STMT:slot_71>;  // aligned to BB_64 (sequential)
 } else {
 <FILL_STMT:slot_65>;  // aligned to BB_69 (if_else_body)
 <FILL_STMT:slot_68>;  // aligned to BB_62 (if_else_body)
 <FILL_STMT:slot_72>;  // aligned to BB_56 (if_else_body)
 <FILL_STMT:slot_73>;  // aligned to BB_52 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_59>;  // aligned to BB_59 (if_else_body)
 <FILL_STMT:slot_60>;  // aligned to BB_60 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_56>;  // aligned to BB_55 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_51>;  // aligned to BB_49 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 42  input_line = 73  func = process_files  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chmod.c
source_bounds = 334-366
bb = 11  src_rows = 21  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 5/5  gaps(if,loop,switch)=(-1,1,0)  sim(type,depth)=0.800/0.200
bb_cover = 11/11  reasons = ['missing_if:1', 'extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=3, if_header=3, if_else_body=2, loop_header=1, loop_body=1, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L339
    if L344
        if L346
            if L349
if L359

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if
        if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:if_header
BB_3:if_header
BB_4:if_else_body
BB_5:sequential
BB_6:sequential
BB_7:loop_body
BB_8:if_header
BB_9:if_else_body
BB_10:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  334: {
  335:   bool ok = true;
  336: 
  337:   FTS *fts = xfts_open (files, bit_flags, nullptr);
  338: 
  339:   while (true)
  340:     {
  341:       FTSENT *ent;
  342: 
  343:       ent = fts_read (fts);
  344:       if (ent == nullptr)
  345:         {
  346:           if (errno != 0)
  347:             {
  348:               /* FIXME: try to give a better message  */
  349:               if (! force_silent)
  350:                 error (0, errno, _("fts_read failed"));
  351:               ok = false;
  352:             }
  353:           break;
  354:         }
  355: 
  356:       ok &= process_file (fts, ent);
  357:     }
  358: 
  359:   if (fts_close (fts) != 0)
  360:     {
  361:       error (0, errno, _("fts_close failed"));
  362:       ok = false;
  363:     }
  364: 
  365:   return ok;
  366: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_11>;  // aligned to BB_10 (sequential)
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_9 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 }
 }
 <FILL_STMT:slot_12>;  // aligned to BB_7 (loop_body)
 }
 }
}

========================================================================================================================
example = 43  input_line = 75  func = mode_changed  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chmod.c
source_bounds = 122-142
bb = 8  src_rows = 11  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/3  gaps(if,loop,switch)=(-1,0,0)  sim(type,depth)=0.667/0.333
bb_cover = 8/8  reasons = ['missing_if:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=3, if_header=3, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L123
    if L130
        if L132

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_header
BB_3:if_else_body
BB_4:sequential
BB_5:if_else_body
BB_6:sequential
BB_7:sequential

-- REAL SOURCE FUNCTION --
  122: {
  123:   if (new_mode & (S_ISUID | S_ISGID | S_ISVTX))
  124:     {
  125:       /* The new mode contains unusual bits that the call to chmod may
  126:          have silently cleared.  Check whether they actually changed.  */
  127: 
  128:       struct stat new_stats;
  129: 
  130:       if (fstatat (dir_fd, file, &new_stats, 0) != 0)
  131:         {
  132:           if (! force_silent)
  133:             error (0, errno, _("getting new attributes of %s"),
  134:                    quoteaf (file_full_name));
  135:           return false;
  136:         }
  137: 
  138:       new_mode = new_stats.st_mode;
  139:     }
  140: 
  141:   return ((old_mode ^ new_mode) & CHMOD_MODE_BITS) != 0;
  142: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_6 (sequential)
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 44  input_line = 76  func = describe_change  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chmod.c
source_bounds = 149-196
bb = 19  src_rows = 35  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 5/2  gaps(if,loop,switch)=(0,0,3)  sim(type,depth)=0.400/0.200
bb_cover = 19/19  reasons = ['extra_switch:3']
10class_pattern_label_counts = sequential=14, switch_header=5

-- SOURCE CONTROL OUTLINE --
switch L155
switch L180

-- AB SKELETON CONTROL OUTLINE --
switch
  switch
    switch
      switch
        switch

-- 10CLASS BB LABEL SEQUENCE --
BB_0:switch_header
BB_1:sequential
BB_2:switch_header
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:switch_header
BB_8:sequential
BB_9:switch_header
BB_10:sequential
BB_11:switch_header
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential

-- REAL SOURCE FUNCTION --
  149: {
  150:   char perms[12];		/* "-rwxrwxrwx" ls-style modes. */
  151:   char old_perms[12];
  152:   char const *fmt;
  153:   char const *quoted_file = quoteaf (file);
  154: 
  155:   switch (ch->status)
  156:     {
  157:     case CH_NOT_APPLIED:
  158:       printf (_("neither symbolic link %s nor referent has been changed\n"),
  159:               quoted_file);
  160:       return;
  161: 
  162:     case CH_NO_STAT:
  163:       printf (_("%s could not be accessed\n"), quoted_file);
  164:       return;
  165: 
  166:     default:
  167:       break;
  168:   }
  169: 
  170:   unsigned long int
  171:     old_m = ch->old_mode & CHMOD_MODE_BITS,
  172:     m = ch->new_mode & CHMOD_MODE_BITS;
  173: 
  174:   strmode (ch->new_mode, perms);
  175:   perms[10] = '\0';		/* Remove trailing space.  */
  176: 
  177:   strmode (ch->old_mode, old_perms);
  178:   old_perms[10] = '\0';		/* Remove trailing space.  */
  179: 
  180:   switch (ch->status)
  181:     {
  182:     case CH_SUCCEEDED:
  183:       fmt = _("mode of %s changed from %04lo (%s) to %04lo (%s)\n");
  184:       break;
  185:     case CH_FAILED:
  186:       fmt = _("failed to change mode of %s from %04lo (%s) to %04lo (%s)\n");
  187:       break;
  188:     case CH_NO_CHANGE_REQUESTED:
  189:       fmt = _("mode of %s retained as %04lo (%s)\n");
  190:       printf (fmt, quoted_file, m, &perms[1]);
  191:       return;
  192:     default:
  193:       affirm (false);
  194:     }
  195:   printf (fmt, quoted_file, old_m, &old_perms[1], m, &perms[1]);
  196: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 switch (<FILL_COND:slot_2>) {  // aligned to BB_0 (switch_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (sequential)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 <FILL_STMT:slot_7>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (sequential)
 switch (<FILL_COND:slot_9>) {  // aligned to BB_7 (switch_header)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (sequential)
 switch (<FILL_COND:slot_11>) {  // aligned to BB_9 (switch_header)
 <FILL_STMT:slot_12>;  // aligned to BB_10 (sequential)
 switch (<FILL_COND:slot_13>) {  // aligned to BB_11 (switch_header)
 <FILL_STMT:slot_14>;  // aligned to BB_12 (sequential)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_16>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_17>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_15 (if_else_body)
 <FILL_STMT:slot_19>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_5 (if_else_body)
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 45  input_line = 77  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chmod.c
source_bounds = 418-571
bb = 46  src_rows = 115  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 16/13  gaps(if,loop,switch)=(1,2,0)  sim(type,depth)=0.812/0.125
bb_cover = 46/46  reasons = ['extra_if:1', 'extra_loop:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=22, if_header=11, if_else_body=8, loop_header=3, loop_body=1, switch_header=1

-- SOURCE CONTROL OUTLINE --
loop L437
    switch L443
            if L475
if L513
    if L515
      if L523
if L527
    if L529
if L536
    if L539
      if L546
if L554
    if L558

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      loop
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:switch_header
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:if_header
BB_7:if_else_body
BB_8:sequential
BB_9:sequential
BB_10:sequential
BB_11:sequential
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:loop_header
BB_18:loop_body
BB_19:if_header
BB_20:if_header
BB_21:if_else_body
BB_22:sequential
BB_23:if_header
BB_24:if_else_body
BB_25:sequential
BB_26:if_header
BB_27:if_header
BB_28:if_else_body
BB_29:sequential
BB_30:sequential
BB_31:sequential
BB_32:if_header
BB_33:if_header
BB_34:if_else_body
BB_35:sequential
BB_36:if_header
BB_37:if_else_body
BB_38:sequential
BB_39:if_header
BB_40:if_else_body
BB_41:if_header
BB_42:if_else_body
BB_43:sequential
BB_44:sequential
BB_45:sequential

-- REAL SOURCE FUNCTION --
  418: {
  419:   char *mode = nullptr;
  420:   idx_t mode_len = 0;
  421:   idx_t mode_alloc = 0;
  422:   bool ok;
  423:   bool preserve_root = false;
  424:   char const *reference_file = nullptr;
  425:   int c;
  426: 
  427:   initialize_main (&argc, &argv);
  428:   set_program_name (argv[0]);
  429:   setlocale (LC_ALL, "");
  430:   bindtextdomain (PACKAGE, LOCALEDIR);
  431:   textdomain (PACKAGE);
  432: 
  433:   atexit (close_stdout);
  434: 
  435:   recurse = force_silent = diagnose_surprises = false;
  436: 
  437:   while ((c = getopt_long (argc, argv,
  438:                            ("Rcfvr::w::x::X::s::t::u::g::o::a::,::+::=::"
  439:                             "0::1::2::3::4::5::6::7::"),
  440:                            long_options, nullptr))
  441:          != -1)
  442:     {
  443:       switch (c)
  444:         {
  445:         case 'r':
  446:         case 'w':
  447:         case 'x':
  448:         case 'X':
  449:         case 's':
  450:         case 't':
  451:         case 'u':
  452:         case 'g':
  453:         case 'o':
  454:         case 'a':
  455:         case ',':
  456:         case '+':
  457:         case '=':
  458:         case '0': case '1': case '2': case '3':
  459:         case '4': case '5': case '6': case '7':
  460:           /* Support non-portable uses like "chmod -w", but diagnose
  461:              surprises due to umask confusion.  Even though "--", "--r",
  462:              etc., are valid modes, there is no "case '-'" here since
  463:              getopt_long reserves leading "--" for long options.  */
  464:           {
  465:             /* Allocate a mode string (e.g., "-rwx") by concatenating
  466:                the argument containing this option.  If a previous mode
  467:                string was given, concatenate the previous string, a
  468:                comma, and the new string (e.g., "-s,-rwx").  */
  469: 
  470:             char const *arg = argv[optind - 1];
  471:             idx_t arg_len = strlen (arg);
  472:             idx_t mode_comma_len = mode_len + !!mode_len;
  473:             idx_t new_mode_len = mode_comma_len + arg_len;
  474:             assume (0 <= new_mode_len);  /* Pacify GCC bug #109613.  */
  475:             if (mode_alloc <= new_mode_len)
  476:               mode = xpalloc (mode, &mode_alloc,
  477:                               new_mode_len + 1 - mode_alloc, -1, 1);
  478:             mode[mode_len] = ',';
  479:             memcpy (mode + mode_comma_len, arg, arg_len + 1);
  480:             mode_len = new_mode_len;
  481: 
  482:             diagnose_surprises = true;
  483:           }
  484:           break;
  485:         case NO_PRESERVE_ROOT:
  486:           preserve_root = false;
  487:           break;
  488:         case PRESERVE_ROOT:
  489:           preserve_root = true;
  490:           break;
  491:         case REFERENCE_FILE_OPTION:
  492:           reference_file = optarg;
  493:           break;
  494:         case 'R':
  495:           recurse = true;
  496:           break;
  497:         case 'c':
  498:           verbosity = V_changes_only;
  499:           break;
  500:         case 'f':
  501:           force_silent = true;
  502:           break;
  503:         case 'v':
  504:           verbosity = V_high;
  505:           break;
  506:         case_GETOPT_HELP_CHAR;
  507:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  508:         default:
  509:           usage (EXIT_FAILURE);
  510:         }
  511:     }
  512: 
  513:   if (reference_file)
  514:     {
  515:       if (mode)
  516:         {
  517:           error (0, 0, _("cannot combine mode and --reference options"));
  518:           usage (EXIT_FAILURE);
  519:         }
  520:     }
  521:   else
  522:     {
  523:       if (!mode)
  524:         mode = argv[optind++];
  525:     }
  526: 
  527:   if (optind >= argc)
  528:     {
  529:       if (!mode || mode != argv[optind - 1])
  530:         error (0, 0, _("missing operand"));
  531:       else
  532:         error (0, 0, _("missing operand after %s"), quote (argv[argc - 1]));
  533:       usage (EXIT_FAILURE);
  534:     }
  535: 
  536:   if (reference_file)
  537:     {
  538:       change = mode_create_from_ref (reference_file);
  539:       if (!change)
  540:         error (EXIT_FAILURE, errno, _("failed to get attributes of %s"),
  541:                quoteaf (reference_file));
  542:     }
  543:   else
  544:     {
  545:       change = mode_compile (mode);
  546:       if (!change)
  547:         {
  548:           error (0, 0, _("invalid mode: %s"), quote (mode));
  549:           usage (EXIT_FAILURE);
  550:         }
  551:       umask_value = umask (0);
  552:     }
  553: 
  554:   if (recurse && preserve_root)
  555:     {
  556:       static struct dev_ino dev_ino_buf;
  557:       root_dev_ino = get_root_dev_ino (&dev_ino_buf);
  558:       if (root_dev_ino == nullptr)
  559:         error (EXIT_FAILURE, errno, _("failed to get attributes of %s"),
  560:                quoteaf ("/"));
  561:     }
  562:   else
  563:     {
  564:       root_dev_ino = nullptr;
  565:     }
  566: 
  567:   ok = process_files (argv + optind,
  568:                       FTS_COMFOLLOW | FTS_PHYSICAL | FTS_DEFER_STAT);
  569: 
  570:   main_exit (ok ? EXIT_SUCCESS : EXIT_FAILURE);
  571: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 while (<FILL_COND:slot_6>) {  // aligned to BB_17 (loop_header)
 <FILL_STMT:slot_7>;  // aligned to BB_18 (loop_body)
 if (<FILL_COND:slot_8>) {  // aligned to BB_19 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_20 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_21 (sequential)
 <FILL_STMT:slot_11>;  // aligned to BB_22 (sequential)
 if (<FILL_COND:slot_12>) {  // aligned to BB_26 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_27 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_29 (sequential)
 <FILL_STMT:slot_16>;  // aligned to BB_31 (sequential)
 if (<FILL_COND:slot_17>) {  // aligned to BB_32 (if_header)
 if (<FILL_COND:slot_18>) {  // aligned to BB_33 (if_header)
 <FILL_STMT:slot_20>;  // aligned to BB_35 (sequential)
 if (<FILL_COND:slot_21>) {  // aligned to BB_39 (if_header)
 if (<FILL_COND:slot_23>) {  // aligned to BB_41 (if_header)
 <FILL_STMT:slot_25>;  // aligned to BB_43 (sequential)
 <FILL_STMT:slot_26>;  // aligned to BB_45 (sequential)
 <FILL_STMT:slot_27>;  // aligned to BB_44 (sequential)
 if (<FILL_COND:slot_28>) {  // aligned to BB_36 (if_header)
 <FILL_STMT:slot_30>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_32>) {  // aligned to BB_23 (if_header)
 <FILL_STMT:slot_34>;  // aligned to BB_25 (sequential)
 if (<FILL_COND:slot_35>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_37>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_39>;  // aligned to BB_8 (sequential)
 <FILL_STMT:slot_40>;  // aligned to BB_9 (sequential)
 <FILL_STMT:slot_41>;  // aligned to BB_10 (sequential)
 <FILL_STMT:slot_42>;  // aligned to BB_11 (sequential)
 <FILL_STMT:slot_43>;  // aligned to BB_12 (sequential)
 <FILL_STMT:slot_44>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_45>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_47>;  // aligned to BB_16 (sequential)
 } else {
 <FILL_STMT:slot_38>;  // aligned to BB_7 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_36>;  // aligned to BB_5 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_33>;  // aligned to BB_24 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_29>;  // aligned to BB_37 (if_else_body)
 <FILL_STMT:slot_31>;  // aligned to BB_30 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_24>;  // aligned to BB_42 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_40 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_34 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_28 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 46  input_line = 80  func = restricted_chown  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chown-core.c
source_bounds = 233-279
bb = 29  src_rows = 31  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 15/8  gaps(if,loop,switch)=(7,0,0)  sim(type,depth)=0.533/0.133
bb_cover = 29/29  reasons = ['extra_if:7']
10class_pattern_label_counts = sequential=8, if_header=14, if_else_body=7

-- SOURCE CONTROL OUTLINE --
if L239
if L242
    if L244
if L251
if L256
    if L258
        if L260
            if L263

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_header
BB_4:if_header
BB_5:if_else_body
BB_6:if_else_body
BB_7:sequential
BB_8:if_header
BB_9:if_header
BB_10:if_header
BB_11:if_header
BB_12:sequential
BB_13:if_header
BB_14:if_else_body
BB_15:if_header
BB_16:if_else_body
BB_17:sequential
BB_18:if_header
BB_19:if_header
BB_20:if_header
BB_21:if_header
BB_22:if_header
BB_23:if_else_body
BB_24:if_else_body
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:sequential

-- REAL SOURCE FUNCTION --
  233: {
  234:   enum RCH_status status = RC_ok;
  235:   struct stat st;
  236:   int open_flags = O_NONBLOCK | O_NOCTTY;
  237:   int fd;
  238: 
  239:   if (required_uid == (uid_t) -1 && required_gid == (gid_t) -1)
  240:     return RC_do_ordinary_chown;
  241: 
  242:   if (! S_ISREG (orig_st->st_mode))
  243:     {
  244:       if (S_ISDIR (orig_st->st_mode))
  245:         open_flags |= O_DIRECTORY;
  246:       else
  247:         return RC_do_ordinary_chown;
  248:     }
  249: 
  250:   fd = openat (cwd_fd, file, O_RDONLY | open_flags);
  251:   if (! (0 <= fd
  252:          || (errno == EACCES && S_ISREG (orig_st->st_mode)
  253:              && 0 <= (fd = openat (cwd_fd, file, O_WRONLY | open_flags)))))
  254:     return (errno == EACCES ? RC_do_ordinary_chown : RC_error);
  255: 
  256:   if (fstat (fd, &st) != 0)
  257:     status = RC_error;
  258:   else if (! SAME_INODE (*orig_st, st))
  259:     status = RC_inode_changed;
  260:   else if ((required_uid == (uid_t) -1 || required_uid == st.st_uid)
  261:            && (required_gid == (gid_t) -1 || required_gid == st.st_gid))
  262:     {
  263:       if (fchown (fd, uid, gid) == 0)
  264:         {
  265:           status = (close (fd) == 0
  266:                     ? RC_ok : RC_error);
  267:           return status;
  268:         }
  269:       else
  270:         {
  271:           status = RC_error;
  272:         }
  273:     }
  274: 
  275:   int saved_errno = errno;
  276:   close (fd);
  277:   errno = saved_errno;
  278:   return status;
  279: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_then_body)
 <FILL_STMT:slot_5>;  // aligned to BB_28 (sequential)
 if (<FILL_COND:slot_6>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 if (<FILL_COND:slot_10>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_12 (sequential)
 if (<FILL_COND:slot_15>) {  // aligned to BB_13 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_27 (sequential)
 if (<FILL_COND:slot_18>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_19>) {  // aligned to BB_16 (if_header)
 <FILL_STMT:slot_20>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_21>;  // aligned to BB_26 (sequential)
 if (<FILL_COND:slot_22>) {  // aligned to BB_18 (if_header)
 if (<FILL_COND:slot_23>) {  // aligned to BB_19 (if_header)
 if (<FILL_COND:slot_24>) {  // aligned to BB_20 (if_header)
 if (<FILL_COND:slot_25>) {  // aligned to BB_21 (if_header)
 if (<FILL_COND:slot_26>) {  // aligned to BB_22 (if_header)
 <FILL_STMT:slot_28>;  // aligned to BB_24 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_25 (sequential)
 } else {
 <FILL_STMT:slot_27>;  // aligned to BB_23 (if_else_body)
 <FILL_STMT:slot_30>;  // aligned to BB_6 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_14 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_else_body)
 }
 }
 }
}

========================================================================================================================
example = 47  input_line = 81  func = describe_change  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chown-core.c
source_bounds = 155-208
bb = 47  src_rows = 33  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 7/3  gaps(if,loop,switch)=(2,0,2)  sim(type,depth)=0.429/0.143
bb_cover = 47/47  reasons = ['extra_if:2', 'extra_switch:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=38, if_header=2, if_then_body=1, if_else_body=3, switch_header=3

-- SOURCE CONTROL OUTLINE --
if L160
switch L171
      if L179

-- AB SKELETON CONTROL OUTLINE --
if
  switch
    switch
      switch
        if
          if
            if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:switch_header
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:if_header
BB_22:if_else_body
BB_23:sequential
BB_24:sequential
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:sequential
BB_29:if_else_body
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:sequential
BB_39:sequential
BB_40:sequential
BB_41:sequential
BB_42:sequential
BB_43:sequential
BB_44:sequential
BB_45:sequential
BB_46:sequential

-- REAL SOURCE FUNCTION --
  155: {
  156:   char const *fmt;
  157:   char *old_spec;
  158:   char *spec;
  159: 
  160:   if (changed == CH_NOT_APPLIED)
  161:     {
  162:       printf (_("neither symbolic link %s nor referent has been changed\n"),
  163:               quoteaf (file));
  164:       return;
  165:     }
  166: 
  167:   spec = user_group_str (user, group);
  168:   old_spec = user_group_str (user ? old_user : nullptr,
  169:                              group ? old_group : nullptr);
  170: 
  171:   switch (changed)
  172:     {
  173:     case CH_SUCCEEDED:
  174:       fmt = (user ? _("changed ownership of %s from %s to %s\n")
  175:              : group ? _("changed group of %s from %s to %s\n")
  176:              : _("no change to ownership of %s\n"));
  177:       break;
  178:     case CH_FAILED:
  179:       if (old_spec)
  180:         {
  181:           fmt = (user ? _("failed to change ownership of %s from %s to %s\n")
  182:                  : group ? _("failed to change group of %s from %s to %s\n")
  183:                  : _("failed to change ownership of %s\n"));
  184:         }
  185:       else
  186:         {
  187:           fmt = (user ? _("failed to change ownership of %s to %s\n")
  188:                  : group ? _("failed to change group of %s to %s\n")
  189:                  : _("failed to change ownership of %s\n"));
  190:           free (old_spec);
  191:           old_spec = spec;
  192:           spec = nullptr;
  193:         }
  194:       break;
  195:     case CH_NO_CHANGE_REQUESTED:
  196:       fmt = (user ? _("ownership of %s retained as %s\n")
  197:              : group ? _("group of %s retained as %s\n")
  198:              : _("ownership of %s retained\n"));
  199:       break;
  200:     default:
  201:       affirm (false);
  202:     }
  203: 
  204:   printf (fmt, quoteaf (file), old_spec, spec);
  205: 
  206:   free (old_spec);
  207:   free (spec);
  208: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_46 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (sequential)
 switch (<FILL_COND:slot_9>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_10>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_11>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_12>;  // aligned to BB_11 (sequential)
 switch (<FILL_COND:slot_13>) {  // aligned to BB_12 (switch_header)
 <FILL_STMT:slot_14>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_15>;  // aligned to BB_44 (sequential)
 <FILL_STMT:slot_16>;  // aligned to BB_45 (sequential)
 <FILL_STMT:slot_17>;  // aligned to BB_38 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_43 (sequential)
 <FILL_STMT:slot_19>;  // aligned to BB_39 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_21>;  // aligned to BB_42 (sequential)
 <FILL_STMT:slot_22>;  // aligned to BB_41 (sequential)
 if (<FILL_COND:slot_23>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_24>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_25>;  // aligned to BB_23 (sequential)
 <FILL_STMT:slot_26>;  // aligned to BB_28 (sequential)
 <FILL_STMT:slot_27>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_28>;  // aligned to BB_24 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_25 (sequential)
 <FILL_STMT:slot_30>;  // aligned to BB_27 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_26 (sequential)
 if (<FILL_COND:slot_32>) {  // aligned to BB_29 (if_header)
 <FILL_STMT:slot_33>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_34>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_36>;  // aligned to BB_32 (sequential)
 <FILL_STMT:slot_37>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_38>;  // aligned to BB_33 (sequential)
 if (<FILL_COND:slot_39>) {  // aligned to BB_14 (if_header)
 <FILL_STMT:slot_40>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_41>;  // aligned to BB_20 (sequential)
 <FILL_STMT:slot_43>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_44>;  // aligned to BB_19 (sequential)
 <FILL_STMT:slot_45>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_47>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_42>;  // aligned to BB_16 (if_else_body)
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_5>;  // aligned to BB_2 (if_else_body)
 <FILL_STMT:slot_48>;  // aligned to BB_37 (if_else_body)
 }
}

========================================================================================================================
example = 48  input_line = 82  func = user_group_str  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chown-core.c
source_bounds = 124-145
bb = 9  src_rows = 14  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 3/3  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.667
bb_cover = 9/9  reasons = ['filled_terminal_count_mismatch', 'same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=2, if_header=3, if_then_body=1, if_else_body=2, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L127
    if L129
    if L139

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_then_body
BB_3:if_else_body
BB_4:sequential
BB_5:if_header
BB_6:if_else_body
BB_7:sequential
BB_8:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  124: {
  125:   char *spec = nullptr;
  126: 
  127:   if (user)
  128:     {
  129:       if (group)
  130:         {
  131:           spec = xmalloc (strlen (user) + 1 + strlen (group) + 1);
  132:           stpcpy (stpcpy (stpcpy (spec, user), ":"), group);
  133:         }
  134:       else
  135:         {
  136:           spec = xstrdup (user);
  137:         }
  138:     }
  139:   else if (group)
  140:     {
  141:       spec = xstrdup (group);
  142:     }
  143: 
  144:   return spec;
  145: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_8 (sequential)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (sequential)
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_6 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 <FILL_STMT:slot_7>;  // aligned to BB_3 (if_else_body)
 }
 }
}

========================================================================================================================
example = 49  input_line = 91  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/chroot.c
source_bounds = 219-431
bb = 113  src_rows = 127  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 52/30  gaps(if,loop,switch)=(16,2,4)  sim(type,depth)=0.577/0.038
bb_cover = 113/113  reasons = ['extra_if:16', 'extra_loop:2', 'extra_switch:4', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=39, if_header=36, if_then_body=2, if_else_body=27, loop_header=3, loop_body=1, switch_header=5

-- SOURCE CONTROL OUTLINE --
loop L243
    switch L245
            if L254
if L276
if L285
if L292
    if L300
    if L305
        if L308
            if L310
    if L316
        if L323
if L329
if L333
if L336
    if L340
if L354
    if L359
if L365
    if L368
        if L370
        if L374
if L383
    if L385
        if L387
    if L398
        if L400
if L413
if L419
if L422

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      switch
        switch
          switch
            switch
              loop
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:switch_header
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:if_header
BB_13:if_else_body
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:loop_header
BB_21:loop_body
BB_22:if_header
BB_23:if_else_body
BB_24:if_header
BB_25:if_else_body
BB_26:sequential
BB_27:if_header
BB_28:if_header
BB_29:if_else_body
BB_30:if_header
BB_31:if_header
BB_32:if_header
BB_33:if_else_body
BB_34:if_header
BB_35:if_header
BB_36:if_else_body
BB_37:if_else_body
BB_38:sequential
BB_39:sequential
BB_40:if_header
BB_41:if_else_body
BB_42:sequential
BB_43:if_header
BB_44:if_header
BB_45:if_else_body
BB_46:if_header
BB_47:if_else_body
BB_48:sequential
BB_49:if_then_body
BB_50:sequential
BB_51:if_header
BB_52:if_else_body
BB_53:if_header
BB_54:if_else_body
BB_55:sequential
BB_56:if_header
BB_57:if_header
BB_58:if_else_body
BB_59:sequential
BB_60:if_else_body
BB_61:if_header
BB_62:if_header
BB_63:if_else_body
BB_64:sequential
BB_65:sequential
BB_66:sequential
BB_67:sequential
BB_68:if_header
BB_69:if_header
BB_70:if_header
BB_71:if_else_body
BB_72:if_header
BB_73:if_header
BB_74:if_else_body
BB_75:if_else_body
BB_76:sequential
BB_77:if_header
BB_78:if_else_body
BB_79:if_else_body
BB_80:sequential
BB_81:sequential
BB_82:if_header
BB_83:if_header
BB_84:if_header
BB_85:if_header
BB_86:if_else_body
BB_87:if_else_body
BB_88:if_else_body
BB_89:sequential
BB_90:sequential
BB_91:sequential
BB_92:sequential
BB_93:if_header
BB_94:if_header
BB_95:if_else_body
BB_96:sequential
BB_97:if_else_body
BB_98:sequential
BB_99:sequential
BB_100:if_header
BB_101:if_then_body
BB_102:if_header
BB_103:sequential
BB_104:sequential
BB_105:if_header
BB_106:if_else_body
BB_107:sequential
BB_108:if_header
BB_109:if_else_body
BB_110:sequential
BB_111:sequential
BB_112:sequential

-- REAL SOURCE FUNCTION --
  219: {
  220:   int c;
  221: 
  222:   /* Input user and groups spec.  */
  223:   char *userspec = nullptr;
  224:   char const *username = nullptr;
  225:   char const *groups = nullptr;
  226:   bool skip_chdir = false;
  227: 
  228:   /* Parsed user and group IDs.  */
  229:   uid_t uid = -1;
  230:   gid_t gid = -1;
  231:   GETGROUPS_T *out_gids = nullptr;
  232:   size_t n_gids = 0;
  233: 
  234:   initialize_main (&argc, &argv);
  235:   set_program_name (argv[0]);
  236:   setlocale (LC_ALL, "");
  237:   bindtextdomain (PACKAGE, LOCALEDIR);
  238:   textdomain (PACKAGE);
  239: 
  240:   initialize_exit_failure (EXIT_CANCELED);
  241:   atexit (close_stdout);
  242: 
  243:   while ((c = getopt_long (argc, argv, "+", long_opts, nullptr)) != -1)
  244:     {
  245:       switch (c)
  246:         {
  247:         case USERSPEC:
  248:           {
  249:             userspec = optarg;
  250:             /* Treat 'user:' just like 'user'
  251:                as we lookup the primary group by default
  252:                (and support doing so for UIDs as well as names.  */
  253:             size_t userlen = strlen (userspec);
  254:             if (userlen && userspec[userlen - 1] == ':')
  255:               userspec[userlen - 1] = '\0';
  256:             break;
  257:           }
  258: 
  259:         case GROUPS:
  260:           groups = optarg;
  261:           break;
  262: 
  263:         case SKIP_CHDIR:
  264:           skip_chdir = true;
  265:           break;
  266: 
  267:         case_GETOPT_HELP_CHAR;
  268: 
  269:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  270: 
  271:         default:
  272:           usage (EXIT_CANCELED);
  273:         }
  274:     }
  275: 
  276:   if (argc <= optind)
  277:     {
  278:       error (0, 0, _("missing operand"));
  279:       usage (EXIT_CANCELED);
  280:     }
  281: 
  282:   char const *newroot = argv[optind];
  283:   bool is_oldroot = is_root (newroot);
  284: 
  285:   if (! is_oldroot && skip_chdir)
  286:     {
  287:       error (0, 0, _("option --skip-chdir only permitted if NEWROOT is old %s"),
  288:              quoteaf ("/"));
  289:       usage (EXIT_CANCELED);
  290:     }
  291: 
  292:   if (! is_oldroot)
  293:     {
  294:       /* We have to look up users and groups twice.
  295:         - First, outside the chroot to load potentially necessary passwd/group
  296:           parsing plugins (e.g. NSS);
  297:         - Second, inside chroot to redo parsing in case IDs are different.
  298:           Within chroot lookup is the main justification for having
  299:           the --user option supported by the chroot command itself.  */
  300:       if (userspec)
  301:         ignore_value (parse_user_spec (userspec, &uid, &gid, nullptr, nullptr));
  302: 
  303:       /* If no gid is supplied or looked up, do so now.
  304:         Also lookup the username for use with getgroups.  */
  305:       if (uid_set (uid) && (! groups || gid_unset (gid)))
  306:         {
  307:           const struct passwd *pwd;
  308:           if ((pwd = getpwuid (uid)))
  309:             {
  310:               if (gid_unset (gid))
  311:                 gid = pwd->pw_gid;
  312:               username = pwd->pw_name;
  313:             }
  314:         }
  315: 
  316:       if (groups && *groups)
  317:         ignore_value (parse_additional_groups (groups, &out_gids, &n_gids,
  318:                                                false));
  319: #if HAVE_SETGROUPS
  320:       else if (! groups && gid_set (gid) && username)
  321:         {
  322:           int ngroups = xgetgroups (username, gid, &out_gids);
  323:           if (0 < ngroups)
  324:             n_gids = ngroups;
  325:         }
  326: #endif
  327:     }
  328: 
  329:   if (chroot (newroot) != 0)
  330:     error (EXIT_CANCELED, errno, _("cannot change root directory to %s"),
  331:            quoteaf (newroot));
  332: 
  333:   if (! skip_chdir && chdir ("/"))
  334:     error (EXIT_CANCELED, errno, _("cannot chdir to root directory"));
  335: 
  336:   if (argc == optind + 1)
  337:     {
  338:       /* No command.  Run an interactive shell.  */
  339:       char *shell = getenv ("SHELL");
  340:       if (shell == nullptr)
  341:         shell = bad_cast ("/bin/sh");
  342:       argv[0] = shell;
  343:       argv[1] = bad_cast ("-i");
  344:       argv[2] = nullptr;
  345:     }
  346:   else
  347:     {
  348:       /* The following arguments give the command.  */
  349:       argv += optind + 1;
  350:     }
  351: 
  352:   /* Attempt to set all three: supplementary groups, group ID, user ID.
  353:      Diagnose any failures.  If any have failed, exit before execvp.  */
  354:   if (userspec)
  355:     {
  356:       bool warn;
  357:       char const *err = parse_user_spec_warn (userspec, &uid, &gid,
  358:                                               nullptr, nullptr, &warn);
  359:       if (err)
  360:         error (warn ? 0 : EXIT_CANCELED, 0, "%s", (err));
  361:     }
  362: 
  363:   /* If no gid is supplied or looked up, do so now.
  364:      Also lookup the username for use with getgroups.  */
  365:   if (uid_set (uid) && (! groups || gid_unset (gid)))
  366:     {
  367:       const struct passwd *pwd;
  368:       if ((pwd = getpwuid (uid)))
  369:         {
  370:           if (gid_unset (gid))
  371:             gid = pwd->pw_gid;
  372:           username = pwd->pw_name;
  373:         }
  374:       else if (gid_unset (gid))
  375:         {
  376:           error (EXIT_CANCELED, errno,
  377:                  _("no group specified for unknown uid: %d"), (int) uid);
  378:         }
  379:     }
  380: 
  381:   GETGROUPS_T *gids = out_gids;
  382:   GETGROUPS_T *in_gids = nullptr;
  383:   if (groups && *groups)
  384:     {
  385:       if (parse_additional_groups (groups, &in_gids, &n_gids, !n_gids) != 0)
  386:         {
  387:           if (! n_gids)
  388:             return EXIT_CANCELED;
  389:           /* else look-up outside the chroot worked, then go with those.  */
  390:         }
  391:       else
  392:         gids = in_gids;
  393:     }
  394: #if HAVE_SETGROUPS
  395:   else if (! groups && gid_set (gid) && username)
  396:     {
  397:       int ngroups = xgetgroups (username, gid, &in_gids);
  398:       if (ngroups <= 0)
  399:         {
  400:           if (! n_gids)
  401:             error (EXIT_CANCELED, errno,
  402:                    _("failed to get supplemental groups"));
  403:           /* else look-up outside the chroot worked, then go with those.  */
  404:         }
  405:       else
  406:         {
  407:           n_gids = ngroups;
  408:           gids = in_gids;
  409:         }
  410:     }
  411: #endif
  412: 
  413:   if ((uid_set (uid) || groups) && setgroups (n_gids, gids) != 0)
  414:     error (EXIT_CANCELED, errno, _("failed to set supplemental groups"));
  415: 
  416:   free (in_gids);
  417:   free (out_gids);
  418: 
  419:   if (gid_set (gid) && setgid (gid))
  420:     error (EXIT_CANCELED, errno, _("failed to set group-ID"));
  421: 
  422:   if (uid_set (uid) && setuid (uid))
  423:     error (EXIT_CANCELED, errno, _("failed to set user-ID"));
  424: 
  425:   /* Execute the given command.  */
  426:   execvp (argv[0], argv);
  427: 
  428:   int exit_status = errno == ENOENT ? EXIT_ENOENT : EXIT_CANNOT_INVOKE;
  429:   error (0, errno, _("failed to run command %s"), quote (argv[0]));
  430:   return exit_status;
  431: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 switch (<FILL_COND:slot_8>) {  // aligned to BB_6 (switch_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 switch (<FILL_COND:slot_10>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_12>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 while (<FILL_COND:slot_14>) {  // aligned to BB_20 (loop_header)
 <FILL_STMT:slot_15>;  // aligned to BB_21 (loop_body)
 <FILL_STMT:slot_16>;  // aligned to BB_17 (sequential)
 if (<FILL_COND:slot_17>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_18>) {  // aligned to BB_13 (if_header)
 <FILL_STMT:slot_19>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_21>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_22>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_23>;  // aligned to BB_19 (sequential)
 if (<FILL_COND:slot_24>) {  // aligned to BB_22 (if_header)
 if (<FILL_COND:slot_26>) {  // aligned to BB_24 (if_header)
 if (<FILL_COND:slot_27>) {  // aligned to BB_25 (if_header)
 if (<FILL_COND:slot_29>) {  // aligned to BB_27 (if_header)
 if (<FILL_COND:slot_30>) {  // aligned to BB_28 (if_header)
 if (<FILL_COND:slot_32>) {  // aligned to BB_30 (if_header)
 if (<FILL_COND:slot_33>) {  // aligned to BB_31 (if_header)
 if (<FILL_COND:slot_34>) {  // aligned to BB_32 (if_header)
 <FILL_STMT:slot_35>;  // aligned to BB_33 (sequential)
 if (<FILL_COND:slot_36>) {  // aligned to BB_40 (if_header)
 if (<FILL_COND:slot_37>) {  // aligned to BB_41 (if_header)
 <FILL_STMT:slot_39>;  // aligned to BB_50 (sequential)
 if (<FILL_COND:slot_40>) {  // aligned to BB_51 (if_header)
 if (<FILL_COND:slot_42>) {  // aligned to BB_53 (if_header)
 if (<FILL_COND:slot_43>) {  // aligned to BB_54 (if_header)
 if (<FILL_COND:slot_45>) {  // aligned to BB_56 (if_header)
 if (<FILL_COND:slot_46>) {  // aligned to BB_57 (if_header)
 <FILL_STMT:slot_47>;  // aligned to BB_58 (sequential)
 <FILL_STMT:slot_48>;  // aligned to BB_59 (sequential)
 if (<FILL_COND:slot_49>) {  // aligned to BB_61 (if_header)
 if (<FILL_COND:slot_50>) {  // aligned to BB_62 (if_header)
 <FILL_STMT:slot_52>;  // aligned to BB_64 (sequential)
 <FILL_STMT:slot_54>;  // aligned to BB_66 (sequential)
 <FILL_STMT:slot_55>;  // aligned to BB_67 (sequential)
 if (<FILL_COND:slot_56>) {  // aligned to BB_68 (if_header)
 if (<FILL_COND:slot_57>) {  // aligned to BB_69 (if_header)
 if (<FILL_COND:slot_58>) {  // aligned to BB_70 (if_header)
 if (<FILL_COND:slot_60>) {  // aligned to BB_82 (if_header)
 if (<FILL_COND:slot_61>) {  // aligned to BB_83 (if_header)
 if (<FILL_COND:slot_62>) {  // aligned to BB_84 (if_header)
 if (<FILL_COND:slot_63>) {  // aligned to BB_85 (if_header)
 <FILL_STMT:slot_64>;  // aligned to BB_86 (if_then_body)
 <FILL_EXPR:slot_65>;  // aligned to BB_112 (func_prologue_epilogue)
 <FILL_STMT:slot_66>;  // aligned to BB_87 (sequential)
 <FILL_STMT:slot_67>;  // aligned to BB_89 (sequential)
 if (<FILL_COND:slot_68>) {  // aligned to BB_100 (if_header)
 if (<FILL_COND:slot_70>) {  // aligned to BB_103 (if_header)
 if (<FILL_COND:slot_72>) {  // aligned to BB_105 (if_header)
 if (<FILL_COND:slot_73>) {  // aligned to BB_106 (if_header)
 if (<FILL_COND:slot_75>) {  // aligned to BB_108 (if_header)
 <FILL_STMT:slot_77>;  // aligned to BB_110 (sequential)
 <FILL_STMT:slot_78>;  // aligned to BB_111 (sequential)
 if (<FILL_COND:slot_79>) {  // aligned to BB_102 (if_header)
 if (<FILL_COND:slot_81>) {  // aligned to BB_90 (if_header)
 if (<FILL_COND:slot_82>) {  // aligned to BB_91 (if_header)
 if (<FILL_COND:slot_83>) {  // aligned to BB_92 (if_header)
 if (<FILL_COND:slot_84>) {  // aligned to BB_93 (if_header)
 <FILL_STMT:slot_87>;  // aligned to BB_96 (sequential)
 <FILL_STMT:slot_88>;  // aligned to BB_98 (sequential)
 <FILL_STMT:slot_89>;  // aligned to BB_99 (sequential)
 <FILL_STMT:slot_90>;  // aligned to BB_97 (sequential)
 if (<FILL_COND:slot_91>) {  // aligned to BB_72 (if_header)
 <FILL_STMT:slot_93>;  // aligned to BB_74 (sequential)
 <FILL_STMT:slot_94>;  // aligned to BB_76 (sequential)
 <FILL_STMT:slot_95>;  // aligned to BB_81 (sequential)
 <FILL_STMT:slot_96>;  // aligned to BB_75 (sequential)
 if (<FILL_COND:slot_97>) {  // aligned to BB_77 (if_header)
 <FILL_STMT:slot_98>;  // aligned to BB_78 (sequential)
 <FILL_STMT:slot_99>;  // aligned to BB_80 (sequential)
 if (<FILL_COND:slot_102>) {  // aligned to BB_43 (if_header)
 if (<FILL_COND:slot_103>) {  // aligned to BB_44 (if_header)
 if (<FILL_COND:slot_104>) {  // aligned to BB_45 (if_header)
 if (<FILL_COND:slot_105>) {  // aligned to BB_46 (if_header)
 <FILL_STMT:slot_106>;  // aligned to BB_47 (sequential)
 <FILL_STMT:slot_107>;  // aligned to BB_48 (sequential)
 <FILL_STMT:slot_108>;  // aligned to BB_49 (sequential)
 if (<FILL_COND:slot_109>) {  // aligned to BB_34 (if_header)
 if (<FILL_COND:slot_110>) {  // aligned to BB_35 (if_header)
 <FILL_STMT:slot_111>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_112>;  // aligned to BB_38 (sequential)
 <FILL_STMT:slot_113>;  // aligned to BB_39 (sequential)
 <FILL_STMT:slot_114>;  // aligned to BB_37 (sequential)
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_100>;  // aligned to BB_79 (if_else_body)
 <FILL_STMT:slot_101>;  // aligned to BB_60 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_92>;  // aligned to BB_73 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_85>;  // aligned to BB_94 (if_else_body)
 <FILL_STMT:slot_86>;  // aligned to BB_95 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_80>;  // aligned to BB_88 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_76>;  // aligned to BB_109 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_74>;  // aligned to BB_107 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_71>;  // aligned to BB_104 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_69>;  // aligned to BB_101 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_59>;  // aligned to BB_71 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_51>;  // aligned to BB_63 (if_else_body)
 <FILL_STMT:slot_53>;  // aligned to BB_65 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_44>;  // aligned to BB_55 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_41>;  // aligned to BB_52 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_38>;  // aligned to BB_42 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_31>;  // aligned to BB_29 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_28>;  // aligned to BB_26 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_25>;  // aligned to BB_23 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 50  input_line = 93  func = algorithm_from_tag  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/digest.c
source_bounds = 725-755
bb = 22  src_rows = 19  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 13/4  gaps(if,loop,switch)=(0,9,0)  sim(type,depth)=0.308/0.154
bb_cover = 22/22  reasons = ['extra_loop:9']
10class_pattern_label_counts = sequential=6, if_header=2, if_then_body=1, if_else_body=1, loop_header=10, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L728
    loop L731
loop L741
if L745

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        loop
          loop
            loop
              loop
                loop
                  loop
                    loop
                    loop
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:loop_header
BB_2:loop_header
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:loop_body
BB_7:sequential
BB_8:loop_header
BB_9:loop_header
BB_10:loop_header
BB_11:loop_header
BB_12:loop_header
BB_13:loop_header
BB_14:loop_header
BB_15:loop_header
BB_16:sequential
BB_17:loop_body
BB_18:if_header
BB_19:if_then_body
BB_20:if_else_body
BB_21:sequential

-- REAL SOURCE FUNCTION --
  725: {
  726:   /* Limit check size to this length for perf reasons.  */
  727:   static size_t max_tag_len;
  728:   if (! max_tag_len)
  729:     {
  730:       char const * const * tag = algorithm_tags;
  731:       while (*tag)
  732:         {
  733:           size_t tag_len = strlen (*tag++);
  734:           max_tag_len = MAX (tag_len, max_tag_len);
  735:         }
  736:     }
  737: 
  738:   size_t i = 0;
  739: 
  740:   /* Find end of tag */
  741:   while (i <= max_tag_len && s[i] && ! ISWHITE (s[i])
  742:          && s[i] != '-' && s[i] != '(')
  743:     ++i;
  744: 
  745:   if (i > max_tag_len)
  746:     return -1;
  747: 
  748:   /* Terminate tag, and lookup.  */
  749:   char sep = s[i];
  750:   s[i] = '\0';
  751:   ptrdiff_t algo = argmatch_exact (s, algorithm_tags);
  752:   s[i] = sep;
  753: 
  754:   return algo;
  755: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_6 (loop_body)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 while (<FILL_COND:slot_9>) {  // aligned to BB_7 (loop_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_8 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_9 (loop_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_10 (loop_header)
 while (<FILL_COND:slot_13>) {  // aligned to BB_11 (loop_header)
 while (<FILL_COND:slot_14>) {  // aligned to BB_12 (loop_header)
 while (<FILL_COND:slot_15>) {  // aligned to BB_13 (loop_header)
 while (<FILL_COND:slot_16>) {  // aligned to BB_14 (loop_header)
 while (<FILL_COND:slot_17>) {  // aligned to BB_15 (loop_header)
 <FILL_STMT:slot_18>;  // aligned to BB_16 (sequential)
 if (<FILL_COND:slot_19>) {  // aligned to BB_18 (if_header)
 <FILL_STMT:slot_20>;  // aligned to BB_19 (if_then_body)
 <FILL_EXPR:slot_21>;  // aligned to BB_21 (func_prologue_epilogue)
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_20 (if_else_body)
 }
 <FILL_STMT:slot_23>;  // aligned to BB_17 (loop_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 51  input_line = 102  func = blake2b_init0  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 77-82
bb = 4  src_rows = 5  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=2, loop_header=1, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L81

-- AB SKELETON CONTROL OUTLINE --
loop
  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:loop_body
BB_3:sequential

-- REAL SOURCE FUNCTION --
   77: {
   78:   size_t i;
   79:   memset( S, 0, sizeof( blake2b_state ) );
   80: 
   81:   for( i = 0; i < 8; ++i ) S->h[i] = blake2b_IV[i];
   82: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (loop_body)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 }
 }
}

========================================================================================================================
example = 52  input_line = 105  func = blake2b_set_lastblock  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 64-68
bb = 3  src_rows = 4  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 3/3  reasons = []
10class_pattern_label_counts = sequential=3

-- SOURCE CONTROL OUTLINE --
if L65

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:sequential
BB_2:sequential

-- REAL SOURCE FUNCTION --
   64: {
   65:   if( S->last_node ) blake2b_set_lastnode( S );
   66: 
   67:   S->f[0] = (uint64_t)-1;
   68: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 53  input_line = 107  func = pclmul_supported  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cksum.c
source_bounds = 148-159
bb = 8  src_rows = 5  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 8/8  reasons = ['filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=5, if_header=1, if_else_body=1, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L152

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:sequential
BB_2:if_header
BB_3:if_else_body
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  148: {
  149:   bool pclmul_enabled = (0 < __builtin_cpu_supports ("pclmul")
  150:                          && 0 < __builtin_cpu_supports ("avx"));
  151: 
  152:   if (cksum_debug)
  153:     error (0, 0, "%s",
  154:            (pclmul_enabled
  155:             ? _("using pclmul hardware support")
  156:             : _("pclmul support not detected")));
  157: 
  158:   return pclmul_enabled;
  159: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (sequential)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_6 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_5 (sequential)
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 }
}

========================================================================================================================
example = 54  input_line = 108  func = cksum_slice8  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cksum.c
source_bounds = 164-214
bb = 19  src_rows = 31  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 11/6  gaps(if,loop,switch)=(2,3,0)  sim(type,depth)=0.545/0.182
bb_cover = 19/19  reasons = ['extra_if:2', 'extra_loop:3']
10class_pattern_label_counts = sequential=2, if_header=4, if_else_body=2, loop_header=8, loop_body=3

-- SOURCE CONTROL OUTLINE --
if L170
loop L173
    if L177
    loop L186
    loop L204
    if L206

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      loop
        loop
          if
            loop
              loop
                loop
                  loop
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:loop_header
BB_4:loop_header
BB_5:loop_header
BB_6:if_header
BB_7:if_else_body
BB_8:loop_header
BB_9:loop_header
BB_10:loop_body
BB_11:loop_header
BB_12:loop_header
BB_13:loop_body
BB_14:if_header
BB_15:loop_header
BB_16:loop_body
BB_17:sequential
BB_18:sequential

-- REAL SOURCE FUNCTION --
  164: {
  165:   uint32_t buf[BUFLEN / sizeof (uint32_t)];
  166:   uint_fast32_t crc = 0;
  167:   uintmax_t length = 0;
  168:   size_t bytes_read;
  169: 
  170:   if (!fp || !crc_out || !length_out)
  171:     return false;
  172: 
  173:   while ((bytes_read = fread (buf, 1, BUFLEN, fp)) > 0)
  174:     {
  175:       uint32_t *datap;
  176: 
  177:       if (length + bytes_read < length)
  178:         {
  179:           errno = EOVERFLOW;
  180:           return false;
  181:         }
  182:       length += bytes_read;
  183: 
  184:       /* Process multiples of 8 bytes */
  185:       datap = (uint32_t *)buf;
  186:       while (bytes_read >= 8)
  187:         {
  188:           uint32_t first = *datap++, second = *datap++;
  189:           crc ^= SWAP (first);
  190:           second = SWAP (second);
  191:           crc = (crctab[7][(crc >> 24) & 0xFF]
  192:                  ^ crctab[6][(crc >> 16) & 0xFF]
  193:                  ^ crctab[5][(crc >> 8) & 0xFF]
  194:                  ^ crctab[4][(crc) & 0xFF]
  195:                  ^ crctab[3][(second >> 24) & 0xFF]
  196:                  ^ crctab[2][(second >> 16) & 0xFF]
  197:                  ^ crctab[1][(second >> 8) & 0xFF]
  198:                  ^ crctab[0][(second) & 0xFF]);
  199:           bytes_read -= 8;
  200:         }
  201: 
  202:       /* And finish up last 0-7 bytes in a byte by byte fashion */
  203:       unsigned char *cp = (unsigned char *)datap;
  204:       while (bytes_read--)
  205:         crc = (crc << 8) ^ crctab[0][((crc >> 24) ^ *cp++) & 0xFF];
  206:       if (feof (fp))
  207:         break;
  208:     }
  209: 
  210:   *crc_out = crc;
  211:   *length_out = length;
  212: 
  213:   return !ferror (fp);
  214: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_18 (sequential)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_8 (loop_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_9 (loop_header)
 <FILL_STMT:slot_13>;  // aligned to BB_10 (loop_body)
 while (<FILL_COND:slot_14>) {  // aligned to BB_11 (loop_header)
 while (<FILL_COND:slot_15>) {  // aligned to BB_12 (loop_header)
 <FILL_STMT:slot_16>;  // aligned to BB_13 (loop_body)
 if (<FILL_COND:slot_17>) {  // aligned to BB_14 (if_header)
 <FILL_STMT:slot_19>;  // aligned to BB_17 (sequential)
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_15 (if_else_body)
 }
 <FILL_STMT:slot_20>;  // aligned to BB_16 (loop_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_7 (if_else_body)
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 55  input_line = 109  func = base64_encode_fast  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base64.c
source_bounds = 72-83
bb = 4  src_rows = 9  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L73

-- AB SKELETON CONTROL OUTLINE --
loop
  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_body
BB_3:sequential

-- REAL SOURCE FUNCTION --
   72: {
   73:   while (inlen)
   74:     {
   75:       *out++ = b64c[(to_uchar (in[0]) >> 2) & 0x3f];
   76:       *out++ = b64c[((to_uchar (in[0]) << 4) + (to_uchar (in[1]) >> 4)) & 0x3f];
   77:       *out++ = b64c[((to_uchar (in[1]) << 2) + (to_uchar (in[2]) >> 6)) & 0x3f];
   78:       *out++ = b64c[to_uchar (in[2]) & 0x3f];
   79: 
   80:       inlen -= 3;
   81:       in += 3;
   82:     }
   83: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (loop_body)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 }
 }
}

========================================================================================================================
example = 56  input_line = 111  func = decode_4  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base64.c
source_bounds = 398-460
bb = 41  src_rows = 40  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 14/14  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.071
bb_cover = 41/41  reasons = ['filled_terminal_count_mismatch', 'same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=12, if_header=14, if_then_body=1, if_else_body=14

-- SOURCE CONTROL OUTLINE --
if L400
if L403
if L406
if L413
if L416
    if L418
    if L421
      if L426
      if L429
      if L436
      if L439
          if L441
            if L446
            if L449

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_then_body
BB_4:if_else_body
BB_5:sequential
BB_6:if_header
BB_7:if_else_body
BB_8:if_header
BB_9:if_else_body
BB_10:sequential
BB_11:if_header
BB_12:if_header
BB_13:if_else_body
BB_14:sequential
BB_15:if_header
BB_16:if_else_body
BB_17:sequential
BB_18:if_else_body
BB_19:if_header
BB_20:if_else_body
BB_21:sequential
BB_22:if_header
BB_23:if_else_body
BB_24:if_header
BB_25:if_else_body
BB_26:sequential
BB_27:if_header
BB_28:if_header
BB_29:if_else_body
BB_30:sequential
BB_31:if_else_body
BB_32:if_header
BB_33:if_else_body
BB_34:sequential
BB_35:if_header
BB_36:if_else_body
BB_37:sequential
BB_38:sequential
BB_39:sequential
BB_40:sequential

-- REAL SOURCE FUNCTION --
  398: {
  399:   char *out = *outp;
  400:   if (inlen < 2)
  401:     return false;
  402: 
  403:   if (!isbase64 (in[0]) || !isbase64 (in[1]))
  404:     return false;
  405: 
  406:   if (*outleft)
  407:     {
  408:       *out++ = ((b64[to_uchar (in[0])] << 2)
  409:                 | (b64[to_uchar (in[1])] >> 4));
  410:       --*outleft;
  411:     }
  412: 
  413:   if (inlen == 2)
  414:     return_false;
  415: 
  416:   if (in[2] == '=')
  417:     {
  418:       if (inlen != 4)
  419:         return_false;
  420: 
  421:       if (in[3] != '=')
  422:         return_false;
  423:     }
  424:   else
  425:     {
  426:       if (!isbase64 (in[2]))
  427:         return_false;
  428: 
  429:       if (*outleft)
  430:         {
  431:           *out++ = (((b64[to_uchar (in[1])] << 4) & 0xf0)
  432:                     | (b64[to_uchar (in[2])] >> 2));
  433:           --*outleft;
  434:         }
  435: 
  436:       if (inlen == 3)
  437:         return_false;
  438: 
  439:       if (in[3] == '=')
  440:         {
  441:           if (inlen != 4)
  442:             return_false;
  443:         }
  444:       else
  445:         {
  446:           if (!isbase64 (in[3]))
  447:             return_false;
  448: 
  449:           if (*outleft)
  450:             {
  451:               *out++ = (((b64[to_uchar (in[2])] << 6) & 0xc0)
  452:                         | b64[to_uchar (in[3])]);
  453:               --*outleft;
  454:             }
  455:         }
  456:     }
  457: 
  458:   *outp = out;
  459:   return true;
  460: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_40 (sequential)
 if (<FILL_COND:slot_5>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_14>) {  // aligned to BB_11 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_18>) {  // aligned to BB_15 (if_header)
 <FILL_STMT:slot_22>;  // aligned to BB_39 (sequential)
 if (<FILL_COND:slot_23>) {  // aligned to BB_19 (if_header)
 <FILL_STMT:slot_25>;  // aligned to BB_21 (sequential)
 if (<FILL_COND:slot_26>) {  // aligned to BB_22 (if_header)
 if (<FILL_COND:slot_28>) {  // aligned to BB_24 (if_header)
 if (<FILL_COND:slot_31>) {  // aligned to BB_27 (if_header)
 if (<FILL_COND:slot_32>) {  // aligned to BB_28 (if_header)
 <FILL_STMT:slot_34>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_36>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_37>) {  // aligned to BB_32 (if_header)
 <FILL_STMT:slot_39>;  // aligned to BB_34 (sequential)
 if (<FILL_COND:slot_40>) {  // aligned to BB_35 (if_header)
 <FILL_STMT:slot_42>;  // aligned to BB_37 (sequential)
 } else {
 <FILL_STMT:slot_41>;  // aligned to BB_36 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_38>;  // aligned to BB_33 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_33>;  // aligned to BB_29 (if_else_body)
 <FILL_STMT:slot_35>;  // aligned to BB_31 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_29>;  // aligned to BB_25 (if_else_body)
 <FILL_STMT:slot_30>;  // aligned to BB_26 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_27>;  // aligned to BB_23 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_24>;  // aligned to BB_20 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_16 (if_else_body)
 <FILL_STMT:slot_20>;  // aligned to BB_17 (if_else_body)
 <FILL_STMT:slot_21>;  // aligned to BB_18 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_13 (if_else_body)
 <FILL_STMT:slot_17>;  // aligned to BB_14 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_9 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_7 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 <FILL_STMT:slot_8>;  // aligned to BB_4 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 57  input_line = 112  func = get_4  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base64.c
source_bounds = 345-379
bb = 16  src_rows = 24  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 9/5  gaps(if,loop,switch)=(2,2,0)  sim(type,depth)=0.556/0.222
bb_cover = 16/16  reasons = ['extra_if:2', 'extra_loop:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=4, if_header=5, if_else_body=4, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L349
    if L352
  loop L364
      if L367
          if L370

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        loop
          loop
            loop
              if
                if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_header
BB_4:if_else_body
BB_5:sequential
BB_6:sequential
BB_7:loop_header
BB_8:loop_header
BB_9:if_header
BB_10:if_header
BB_11:if_else_body
BB_12:if_else_body
BB_13:loop_body
BB_14:sequential
BB_15:sequential

-- REAL SOURCE FUNCTION --
  345: {
  346:   if (ctx->i == 4)
  347:     ctx->i = 0;
  348: 
  349:   if (ctx->i == 0)
  350:     {
  351:       char const *t = *in;
  352:       if (4 <= in_end - *in && memchr (t, '\n', 4) == NULL)
  353:         {
  354:           /* This is the common case: no newline.  */
  355:           *in += 4;
  356:           *n_non_newline = 4;
  357:           return (char *) t;
  358:         }
  359:     }
  360: 
  361:   {
  362:     /* Copy non-newline bytes into BUF.  */
  363:     char const *p = *in;
  364:     while (p < in_end)
  365:       {
  366:         char c = *p++;
  367:         if (c != '\n')
  368:           {
  369:             ctx->buf[ctx->i++] = c;
  370:             if (ctx->i == 4)
  371:               break;
  372:           }
  373:       }
  374: 
  375:     *in = p;
  376:     *n_non_newline = ctx->i;
  377:     return ctx->buf;
  378:   }
  379: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_4 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_15 (sequential)
 while (<FILL_COND:slot_9>) {  // aligned to BB_6 (loop_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_7 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_8 (loop_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_10 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_14 (sequential)
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_11 (if_else_body)
 <FILL_STMT:slot_16>;  // aligned to BB_12 (if_else_body)
 }
 }
 <FILL_STMT:slot_17>;  // aligned to BB_13 (loop_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 58  input_line = 114  func = sm3_conclude_ctx  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/sm3.c
source_bounds = 109-131
bb = 3  src_rows = 10  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 3/3  reasons = []
10class_pattern_label_counts = sequential=1, if_header=1, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L116

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential

-- REAL SOURCE FUNCTION --
  109: {
  110:   /* Take yet unprocessed bytes into account.  */
  111:   size_t bytes = ctx->buflen;
  112:   size_t size = (bytes < 56) ? 64 / 4 : 64 * 2 / 4;
  113: 
  114:   /* Now count remaining bytes.  */
  115:   ctx->total[0] += bytes;
  116:   if (ctx->total[0] < bytes)
  117:     ++ctx->total[1];
  118: 
  119:   /* Put the 64-bit file length in *bits* at the end of the buffer.
  120:      Use set_uint32 rather than a simple assignment, to avoid risk of
  121:      unaligned access.  */
  122:   set_uint32 ((char *) &ctx->buffer[size - 2],
  123:               SWAP ((ctx->total[1] << 3) | (ctx->total[0] >> 29)));
  124:   set_uint32 ((char *) &ctx->buffer[size - 1],
  125:               SWAP (ctx->total[0] << 3));
  126: 
  127:   memcpy (&((char *) ctx->buffer)[bytes], fillbuf, (size - 2) * 4 - bytes);
  128: 
  129:   /* Process last bytes.  */
  130:   sm3_process_block (ctx->buffer, size * 4, ctx);
  131: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 59  input_line = 116  func = output_crc  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cksum.c
source_bounds = 254-268
bb = 6  src_rows = 11  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/2  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.500
bb_cover = 6/6  reasons = ['same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=2, if_header=2, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L255
if L265

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_else_body
BB_4:sequential
BB_5:sequential

-- REAL SOURCE FUNCTION --
  254: {
  255:   if (raw)
  256:     {
  257:       /* Output in network byte order (big endian).  */
  258:       uint32_t out_int = SWAP (*(uint32_t *)digest);
  259:       fwrite (&out_int, 1, 32/8, stdout);
  260:       return;
  261:     }
  262: 
  263:   char length_buf[INT_BUFSIZE_BOUND (uintmax_t)];
  264:   printf ("%u %s", *(unsigned int *)digest, umaxtostr (length, length_buf));
  265:   if (args)
  266:     printf (" %s", file);
  267:   putchar (delim);
  268: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_5 (sequential)
 if (<FILL_COND:slot_5>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 60  input_line = 118  func = blake2b_update  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 226-250
bb = 8  src_rows = 23  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.750/0.250
bb_cover = 8/8  reasons = ['extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=2, if_header=2, loop_header=2, loop_body=1, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L228
    if L232
        loop L239

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:loop_header
BB_3:loop_header
BB_4:loop_body
BB_5:sequential
BB_6:sequential
BB_7:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  226: {
  227:   const unsigned char * in = (const unsigned char *)pin;
  228:   if( inlen > 0 )
  229:   {
  230:     size_t left = S->buflen;
  231:     size_t fill = BLAKE2B_BLOCKBYTES - left;
  232:     if( inlen > fill )
  233:     {
  234:       S->buflen = 0;
  235:       memcpy( S->buf + left, in, fill ); /* Fill buffer */
  236:       blake2b_increment_counter( S, BLAKE2B_BLOCKBYTES );
  237:       blake2b_compress( S, S->buf ); /* Compress */
  238:       in += fill; inlen -= fill;
  239:       while(inlen > BLAKE2B_BLOCKBYTES) {
  240:         blake2b_increment_counter(S, BLAKE2B_BLOCKBYTES);
  241:         blake2b_compress( S, in );
  242:         in += BLAKE2B_BLOCKBYTES;
  243:         inlen -= BLAKE2B_BLOCKBYTES;
  244:       }
  245:     }
  246:     memcpy( S->buf + S->buflen, in, inlen );
  247:     S->buflen += inlen;
  248:   }
  249:   return 0;
  250: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (loop_body)
 <FILL_STMT:slot_7>;  // aligned to BB_6 (sequential)
 return <FILL_EXPR:slot_8>;  // aligned to BB_7 (func_prologue_epilogue)
 }
 }
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_5 (if_else_body)
 }
 }
}

========================================================================================================================
example = 61  input_line = 119  func = blake2b_final  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 253-274
bb = 10  src_rows = 16  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.750/0.250
bb_cover = 10/10  reasons = ['extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=3, if_header=2, if_then_body=1, if_else_body=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L257
if L260
loop L268

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_header
BB_4:if_then_body
BB_5:loop_header
BB_6:loop_header
BB_7:loop_body
BB_8:sequential
BB_9:sequential

-- REAL SOURCE FUNCTION --
  253: {
  254:   uint8_t buffer[BLAKE2B_OUTBYTES] = {0};
  255:   size_t i;
  256: 
  257:   if( out == NULL || outlen < S->outlen )
  258:     return -1;
  259: 
  260:   if( blake2b_is_lastblock( S ) )
  261:     return -1;
  262: 
  263:   blake2b_increment_counter( S, S->buflen );
  264:   blake2b_set_lastblock( S );
  265:   memset( S->buf + S->buflen, 0, BLAKE2B_BLOCKBYTES - S->buflen ); /* Padding */
  266:   blake2b_compress( S, S->buf );
  267: 
  268:   for( i = 0; i < 8; ++i ) /* Output full hash to temp buffer */
  269:     store64( buffer + sizeof( S->h[i] ) * i, S->h[i] );
  270: 
  271:   memcpy( out, buffer, S->outlen );
  272:   secure_zero_memory(buffer, sizeof(buffer));
  273:   return 0;
  274: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_9 (sequential)
 if (<FILL_COND:slot_6>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_then_body)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_6 (loop_header)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_11>;  // aligned to BB_8 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 62  input_line = 120  func = blake2b  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 278-304
bb = 22  src_rows = 22  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 8/8  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.125
bb_cover = 22/22  reasons = ['filled_terminal_count_mismatch', 'same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=19, if_header=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L282
if L284
if L286
if L288
if L290
if L292
    if L294
      if L298

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:sequential
BB_2:sequential
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:sequential
BB_10:sequential
BB_11:sequential
BB_12:sequential
BB_13:if_header
BB_14:if_else_body
BB_15:sequential
BB_16:sequential
BB_17:if_else_body
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:sequential

-- REAL SOURCE FUNCTION --
  278: {
  279:   blake2b_state S[1];
  280: 
  281:   /* Verify parameters */
  282:   if ( NULL == in && inlen > 0 ) return -1;
  283: 
  284:   if ( NULL == out ) return -1;
  285: 
  286:   if( NULL == key && keylen > 0 ) return -1;
  287: 
  288:   if( !outlen || outlen > BLAKE2B_OUTBYTES ) return -1;
  289: 
  290:   if( keylen > BLAKE2B_KEYBYTES ) return -1;
  291: 
  292:   if( keylen > 0 )
  293:   {
  294:     if( blake2b_init_key( S, outlen, key, keylen ) < 0 ) return -1;
  295:   }
  296:   else
  297:   {
  298:     if( blake2b_init( S, outlen ) < 0 ) return -1;
  299:   }
  300: 
  301:   blake2b_update( S, ( const uint8_t * )in, inlen );
  302:   blake2b_final( S, out, outlen );
  303:   return 0;
  304: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_then_body)
 <FILL_STMT:slot_5>;  // aligned to BB_21 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_then_body)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (if_then_body)
 if (<FILL_COND:slot_11>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_14>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_12 (if_then_body)
 if (<FILL_COND:slot_16>) {  // aligned to BB_13 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_14 (if_header)
 <FILL_STMT:slot_18>;  // aligned to BB_15 (if_then_body)
 <FILL_STMT:slot_20>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_21>) {  // aligned to BB_17 (if_header)
 <FILL_STMT:slot_22>;  // aligned to BB_18 (if_then_body)
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_19 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_16 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_9 (if_else_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 63  input_line = 122  func = crc_sum_stream  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cksum.c
source_bounds = 221-246
bb = 9  src_rows = 16  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.750/0.250
bb_cover = 9/9  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=2, if_header=2, if_then_body=1, if_else_body=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
  if L227
if L233
loop L238

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_then_body
BB_4:loop_header
BB_5:loop_header
BB_6:loop_body
BB_7:sequential
BB_8:sequential

-- REAL SOURCE FUNCTION --
  221: {
  222:   uintmax_t total_bytes = 0;
  223:   uint_fast32_t crc = 0;
  224: 
  225: # if USE_PCLMUL_CRC32
  226:   static bool (*cksum_fp) (FILE *, uint_fast32_t *, uintmax_t *);
  227:   if (! cksum_fp)
  228:     cksum_fp = pclmul_supported () ? cksum_pclmul : cksum_slice8;
  229: # else
  230:   bool (*cksum_fp) (FILE *, uint_fast32_t *, uintmax_t *) = cksum_slice8;
  231: # endif
  232: 
  233:   if (! cksum_fp (stream, &crc, &total_bytes))
  234:     return -1;
  235: 
  236:   *length = total_bytes;
  237: 
  238:   for (; total_bytes; total_bytes >>= 8)
  239:     crc = (crc << 8) ^ crctab[0][((crc >> 24) ^ total_bytes) & 0xFF];
  240:   crc = ~crc & 0xFFFFFFFF;
  241: 
  242:   unsigned int crc_out = crc;
  243:   memcpy (resstream, &crc_out, sizeof crc_out);
  244: 
  245:   return 0;
  246: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_then_body)
 <FILL_STMT:slot_6>;  // aligned to BB_8 (sequential)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 <FILL_STMT:slot_9>;  // aligned to BB_6 (loop_body)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (sequential)
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 64  input_line = 124  func = base64_encode  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base64.c
source_bounds = 92-135
bb = 36  src_rows = 25  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 15/8  gaps(if,loop,switch)=(2,5,0)  sim(type,depth)=0.533/0.067
bb_cover = 36/36  reasons = ['extra_if:2', 'extra_loop:5']
10class_pattern_label_counts = sequential=12, if_header=8, if_else_body=10, loop_header=5, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L100
loop L106
    if L109
    if L114
    if L122
    if L127
    if L129
if L133

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop
        loop
          loop
            if
              if
                if
                  if
                    if
                    if
                    loop
                    if
                    loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:loop_header
BB_3:loop_header
BB_4:loop_header
BB_5:sequential
BB_6:loop_header
BB_7:sequential
BB_8:if_header
BB_9:if_else_body
BB_10:if_else_body
BB_11:sequential
BB_12:sequential
BB_13:if_header
BB_14:if_else_body
BB_15:if_else_body
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:if_header
BB_22:if_else_body
BB_23:if_else_body
BB_24:sequential
BB_25:sequential
BB_26:if_header
BB_27:if_else_body
BB_28:if_header
BB_29:if_else_body
BB_30:if_header
BB_31:loop_header
BB_32:loop_body
BB_33:if_header
BB_34:if_else_body
BB_35:sequential

-- REAL SOURCE FUNCTION --
   92: {
   93:   /* Note this outlen constraint can be enforced at compile time.
   94:      I.E. that the output buffer is exactly large enough to hold
   95:      the encoded inlen bytes.  The inlen constraints (of corresponding
   96:      to outlen, and being a multiple of 3) can change at runtime
   97:      at the end of input.  However the common case when reading
   98:      large inputs is to have both constraints satisfied, so we depend
   99:      on both in base_encode_fast().  */
  100:   if (outlen % 4 == 0 && inlen == (outlen >> 2) * 3)
  101:     {
  102:       base64_encode_fast (in, inlen, out);
  103:       return;
  104:     }
  105: 
  106:   while (inlen && outlen)
  107:     {
  108:       *out++ = b64c[(to_uchar (in[0]) >> 2) & 0x3f];
  109:       if (!--outlen)
  110:         break;
  111:       *out++ = b64c[((to_uchar (in[0]) << 4)
  112:                        + (--inlen ? to_uchar (in[1]) >> 4 : 0))
  113:                       & 0x3f];
  114:       if (!--outlen)
  115:         break;
  116:       *out++ =
  117:         (inlen
  118:          ? b64c[((to_uchar (in[1]) << 2)
  119:                    + (--inlen ? to_uchar (in[2]) >> 6 : 0))
  120:                   & 0x3f]
  121:          : '=');
  122:       if (!--outlen)
  123:         break;
  124:       *out++ = inlen ? b64c[to_uchar (in[2]) & 0x3f] : '=';
  125:       if (!--outlen)
  126:         break;
  127:       if (inlen)
  128:         inlen--;
  129:       if (inlen)
  130:         in += 3;
  131:     }
  132: 
  133:   if (outlen)
  134:     *out = '\0';
  135: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_35 (sequential)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_6 (loop_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_33 (if_header)
 if (<FILL_COND:slot_12>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_11 (sequential)
 if (<FILL_COND:slot_16>) {  // aligned to BB_13 (if_header)
 <FILL_STMT:slot_18>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_19>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_21>;  // aligned to BB_19 (sequential)
 if (<FILL_COND:slot_22>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_25>;  // aligned to BB_24 (sequential)
 if (<FILL_COND:slot_26>) {  // aligned to BB_26 (if_header)
 <FILL_STMT:slot_27>;  // aligned to BB_27 (sequential)
 if (<FILL_COND:slot_28>) {  // aligned to BB_28 (if_header)
 while (<FILL_COND:slot_29>) {  // aligned to BB_29 (loop_header)
 if (<FILL_COND:slot_30>) {  // aligned to BB_30 (if_header)
 while (<FILL_COND:slot_31>) {  // aligned to BB_31 (loop_header)
 <FILL_STMT:slot_32>;  // aligned to BB_32 (loop_body)
 <FILL_STMT:slot_33>;  // aligned to BB_25 (sequential)
 <FILL_STMT:slot_34>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_20 (sequential)
 <FILL_STMT:slot_36>;  // aligned to BB_12 (sequential)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_22 (if_else_body)
 <FILL_STMT:slot_24>;  // aligned to BB_23 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_17>;  // aligned to BB_14 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_13>;  // aligned to BB_9 (if_else_body)
 <FILL_STMT:slot_14>;  // aligned to BB_10 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_11>;  // aligned to BB_34 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_37>;  // aligned to BB_7 (if_else_body)
 }
 }
}

========================================================================================================================
example = 65  input_line = 126  func = base64_encode_alloc  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/base64.c
source_bounds = 148-167
bb = 58  src_rows = 12  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 20/2  gaps(if,loop,switch)=(18,0,0)  sim(type,depth)=0.100/0.050
bb_cover = 58/58  reasons = ['extra_if:18']
10class_pattern_label_counts = sequential=42, if_header=11, if_then_body=4, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L153
if L161

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_header
BB_3:if_then_body
BB_4:if_header
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:sequential
BB_10:sequential
BB_11:sequential
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:sequential
BB_20:sequential
BB_21:sequential
BB_22:sequential
BB_23:sequential
BB_24:sequential
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:if_header
BB_29:if_then_body
BB_30:if_header
BB_31:sequential
BB_32:if_header
BB_33:if_header
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:sequential
BB_39:sequential
BB_40:sequential
BB_41:sequential
BB_42:sequential
BB_43:sequential
BB_44:sequential
BB_45:sequential
BB_46:sequential
BB_47:sequential
BB_48:if_header
BB_49:if_header
BB_50:sequential
BB_51:if_header
BB_52:sequential
BB_53:sequential
BB_54:if_header
BB_55:if_then_body
BB_56:if_else_body
BB_57:sequential

-- REAL SOURCE FUNCTION --
  148: {
  149:   /* Check for overflow in outlen computation.
  150:      Treat negative INLEN as overflow, for better compatibility with
  151:      pre-2021-08-27 API, which used size_t.  */
  152:   idx_t in_over_3 = inlen / 3 + (inlen % 3 != 0), outlen;
  153:   if (! INT_MULTIPLY_OK (in_over_3, 4, &outlen) || inlen < 0)
  154:     {
  155:       *out = NULL;
  156:       return 0;
  157:     }
  158:   outlen++;
  159: 
  160:   *out = imalloc (outlen);
  161:   if (!*out)
  162:     return outlen;
  163: 
  164:   base64_encode (in, inlen, *out, outlen);
  165: 
  166:   return outlen - 1;
  167: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (sequential)
 if (<FILL_COND:slot_4>) {  // aligned to BB_51 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_52 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_53 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_57 (sequential)
 if (<FILL_COND:slot_8>) {  // aligned to BB_54 (if_header)
 <FILL_STMT:slot_9>;  // aligned to BB_55 (sequential)
 if (<FILL_COND:slot_11>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_12>;  // aligned to BB_3 (if_then_body)
 if (<FILL_COND:slot_13>) {  // aligned to BB_28 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_29 (if_then_body)
 if (<FILL_COND:slot_15>) {  // aligned to BB_32 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_33 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_37 (sequential)
 <FILL_STMT:slot_19>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_20>) {  // aligned to BB_46 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_47 (sequential)
 if (<FILL_COND:slot_22>) {  // aligned to BB_49 (if_header)
 if (<FILL_COND:slot_24>) {  // aligned to BB_39 (if_header)
 <FILL_STMT:slot_25>;  // aligned to BB_40 (sequential)
 <FILL_STMT:slot_26>;  // aligned to BB_41 (sequential)
 <FILL_STMT:slot_27>;  // aligned to BB_44 (sequential)
 <FILL_STMT:slot_28>;  // aligned to BB_45 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_42 (sequential)
 <FILL_STMT:slot_30>;  // aligned to BB_43 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_35 (sequential)
 <FILL_STMT:slot_32>;  // aligned to BB_36 (sequential)
 if (<FILL_COND:slot_33>) {  // aligned to BB_48 (if_header)
 <FILL_STMT:slot_34>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_31 (sequential)
 if (<FILL_COND:slot_36>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_37>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_38>;  // aligned to BB_6 (sequential)
 if (<FILL_COND:slot_39>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_40>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_41>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_42>;  // aligned to BB_8 (sequential)
 <FILL_STMT:slot_43>;  // aligned to BB_11 (sequential)
 <FILL_STMT:slot_44>;  // aligned to BB_12 (sequential)
 <FILL_STMT:slot_45>;  // aligned to BB_15 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_16 (sequential)
 if (<FILL_COND:slot_47>) {  // aligned to BB_26 (if_header)
 <FILL_STMT:slot_48>;  // aligned to BB_27 (sequential)
 <FILL_STMT:slot_49>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_50>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_51>;  // aligned to BB_19 (sequential)
 <FILL_STMT:slot_52>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_53>) {  // aligned to BB_23 (if_header)
 if (<FILL_COND:slot_54>) {  // aligned to BB_24 (if_header)
 <FILL_STMT:slot_55>;  // aligned to BB_25 (sequential)
 if (<FILL_COND:slot_56>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_57>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_58>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_59>;  // aligned to BB_14 (sequential)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_50 (if_else_body)
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_56 (if_else_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 66  input_line = 127  func = blake2b_init_key  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 125-155
bb = 11  src_rows = 27  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/3  gaps(if,loop,switch)=(-2,0,0)  sim(type,depth)=0.333/0.333
bb_cover = 11/11  reasons = ['missing_if:2', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=11

-- SOURCE CONTROL OUTLINE --
if L128
if L130
if L145

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:sequential
BB_2:sequential
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:sequential
BB_10:sequential

-- REAL SOURCE FUNCTION --
  125: {
  126:   blake2b_param P[1];
  127: 
  128:   if ( ( !outlen ) || ( outlen > BLAKE2B_OUTBYTES ) ) return -1;
  129: 
  130:   if ( !key || !keylen || keylen > BLAKE2B_KEYBYTES ) return -1;
  131: 
  132:   P->digest_length = (uint8_t)outlen;
  133:   P->key_length    = (uint8_t)keylen;
  134:   P->fanout        = 1;
  135:   P->depth         = 1;
  136:   store32( &P->leaf_length, 0 );
  137:   store32( &P->node_offset, 0 );
  138:   store32( &P->xof_length, 0 );
  139:   P->node_depth    = 0;
  140:   P->inner_length  = 0;
  141:   memset( P->reserved, 0, sizeof( P->reserved ) );
  142:   memset( P->salt,     0, sizeof( P->salt ) );
  143:   memset( P->personal, 0, sizeof( P->personal ) );
  144: 
  145:   if( blake2b_init_param( S, P ) < 0 ) return -1;
  146: 
  147:   {
  148:     uint8_t block[BLAKE2B_BLOCKBYTES];
  149:     memset( block, 0, BLAKE2B_BLOCKBYTES );
  150:     memcpy( block, key, keylen );
  151:     blake2b_update( S, block, BLAKE2B_BLOCKBYTES );
  152:     secure_zero_memory( block, BLAKE2B_BLOCKBYTES ); /* Burn the key from stack */
  153:   }
  154:   return 0;
  155: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_10 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_6 (sequential)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_11>;  // aligned to BB_8 (if_then_body)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_12>;  // aligned to BB_9 (if_else_body)
 }
}

========================================================================================================================
example = 67  input_line = 128  func = sm3_read_ctx  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/sm3.c
source_bounds = 95-103
bb = 4  src_rows = 6  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = loop_header=2, loop_body=1, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L99

-- AB SKELETON CONTROL OUTLINE --
loop
  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_body
BB_3:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
   95: {
   96:   int i;
   97:   char *r = resbuf;
   98: 
   99:   for (i = 0; i < 8; i++)
  100:     set_uint32 (r + i * sizeof ctx->state[0], SWAP (ctx->state[i]));
  101: 
  102:   return resbuf;
  103: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (loop_body)
 return <FILL_EXPR:slot_5>;  // aligned to BB_3 (func_prologue_epilogue)
 }
 }
}

========================================================================================================================
example = 68  input_line = 131  func = sm3_process_bytes  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/lib/sm3.c
source_bounds = 161-226
bb = 20  src_rows = 37  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 7/7  gaps(if,loop,switch)=(-1,1,0)  sim(type,depth)=0.857/0.286
bb_cover = 20/20  reasons = ['missing_if:1', 'extra_loop:1']
10class_pattern_label_counts = sequential=8, if_header=5, if_else_body=3, loop_header=3, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L164
    if L172
if L189
      if L193
        loop L194
if L210
    if L216

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      loop
        loop
          if
            if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:sequential
BB_4:if_header
BB_5:if_else_body
BB_6:sequential
BB_7:if_header
BB_8:loop_header
BB_9:loop_header
BB_10:loop_header
BB_11:loop_body
BB_12:sequential
BB_13:sequential
BB_14:sequential
BB_15:if_header
BB_16:if_header
BB_17:if_else_body
BB_18:sequential
BB_19:sequential

-- REAL SOURCE FUNCTION --
  161: {
  162:   /* When we already have some bits in our internal buffer concatenate
  163:      both inputs first.  */
  164:   if (ctx->buflen != 0)
  165:     {
  166:       size_t left_over = ctx->buflen;
  167:       size_t add = 128 - left_over > len ? len : 128 - left_over;
  168: 
  169:       memcpy (&((char *) ctx->buffer)[left_over], buffer, add);
  170:       ctx->buflen += add;
  171: 
  172:       if (ctx->buflen > 64)
  173:         {
  174:           sm3_process_block (ctx->buffer, ctx->buflen & ~63, ctx);
  175: 
  176:           ctx->buflen &= 63;
  177:           /* The regions in the following copy operation cannot overlap,
  178:              because ctx->buflen < 64 ≤ (left_over + add) & ~63.  */
  179:           memcpy (ctx->buffer,
  180:                   &((char *) ctx->buffer)[(left_over + add) & ~63],
  181:                   ctx->buflen);
  182:         }
  183: 
  184:       buffer = (const char *) buffer + add;
  185:       len -= add;
  186:     }
  187: 
  188:   /* Process available complete blocks.  */
  189:   if (len >= 64)
  190:     {
  191: #if !(_STRING_ARCH_unaligned || _STRING_INLINE_unaligned)
  192: # define UNALIGNED_P(p) ((uintptr_t) (p) % alignof (uint32_t) != 0)
  193:       if (UNALIGNED_P (buffer))
  194:         while (len > 64)
  195:           {
  196:             sm3_process_block (memcpy (ctx->buffer, buffer, 64), 64, ctx);
  197:             buffer = (const char *) buffer + 64;
  198:             len -= 64;
  199:           }
  200:       else
  201: #endif
  202:         {
  203:           sm3_process_block (buffer, len & ~63, ctx);
  204:           buffer = (const char *) buffer + (len & ~63);
  205:           len &= 63;
  206:         }
  207:     }
  208: 
  209:   /* Move remaining bytes in internal buffer.  */
  210:   if (len > 0)
  211:     {
  212:       size_t left_over = ctx->buflen;
  213: 
  214:       memcpy (&((char *) ctx->buffer)[left_over], buffer, len);
  215:       left_over += len;
  216:       if (left_over >= 64)
  217:         {
  218:           sm3_process_block (ctx->buffer, 64, ctx);
  219:           left_over -= 64;
  220:           /* The regions in the following copy operation cannot overlap,
  221:              because left_over ≤ 64.  */
  222:           memcpy (ctx->buffer, &ctx->buffer[16], left_over);
  223:         }
  224:       ctx->buflen = left_over;
  225:     }
  226: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_6 (sequential)
 if (<FILL_COND:slot_8>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_8 (if_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_9 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_10 (loop_header)
 <FILL_STMT:slot_12>;  // aligned to BB_11 (loop_body)
 <FILL_STMT:slot_13>;  // aligned to BB_14 (sequential)
 if (<FILL_COND:slot_14>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_16 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_18>;  // aligned to BB_19 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_3 (sequential)
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_17 (if_else_body)
 <FILL_STMT:slot_19>;  // aligned to BB_13 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_21>;  // aligned to BB_12 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_5 (if_else_body)
 }
}

========================================================================================================================
example = 69  input_line = 132  func = blake2b_stream  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/b2sum.c
source_bounds = 85-132
bb = 19  src_rows = 35  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 8/8  gaps(if,loop,switch)=(-1,1,0)  sim(type,depth)=0.875/0.375
bb_cover = 19/19  reasons = ['missing_if:1', 'extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=9, if_header=3, if_else_body=2, loop_header=3, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L92
loop L96
    loop L100
        if L105
        if L108
            if L110
        if L116
if L125

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        if
          if
            if
              if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:sequential
BB_2:sequential
BB_3:loop_header
BB_4:loop_header
BB_5:sequential
BB_6:if_header
BB_7:if_header
BB_8:if_else_body
BB_9:if_else_body
BB_10:if_header
BB_11:loop_header
BB_12:loop_body
BB_13:loop_body
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:sequential

-- REAL SOURCE FUNCTION --
   85: {
   86:   int ret = -1;
   87:   size_t sum, n;
   88:   blake2b_state S[1];
   89:   static const size_t buffer_length = 32768;
   90:   uint8_t *buffer = ( uint8_t * )malloc( buffer_length );
   91: 
   92:   if( !buffer ) return -1;
   93: 
   94:   blake2b_init( S, outbytes );
   95: 
   96:   while( 1 )
   97:   {
   98:     sum = 0;
   99: 
  100:     while( 1 )
  101:     {
  102:       n = fread( buffer + sum, 1, buffer_length - sum, stream );
  103:       sum += n;
  104: 
  105:       if( buffer_length == sum )
  106:         break;
  107: 
  108:       if( 0 == n )
  109:       {
  110:         if( ferror( stream ) )
  111:           goto cleanup_buffer;
  112: 
  113:         goto final_process;
  114:       }
  115: 
  116:       if( feof( stream ) )
  117:         goto final_process;
  118:     }
  119: 
  120:     blake2b_update( S, buffer, buffer_length );
  121:   }
  122: 
  123: final_process:;
  124: 
  125:   if( sum > 0 ) blake2b_update( S, buffer, sum );
  126: 
  127:   blake2b_final( S, resstream, outbytes );
  128:   ret = 0;
  129: cleanup_buffer:
  130:   free( buffer );
  131:   return ret;
  132: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_then_body)
 <FILL_STMT:slot_4>;  // aligned to BB_18 (sequential)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 <FILL_STMT:slot_9>;  // aligned to BB_13 (loop_body)
 if (<FILL_COND:slot_10>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_13>;  // aligned to BB_17 (sequential)
 if (<FILL_COND:slot_15>) {  // aligned to BB_14 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_16 (sequential)
 if (<FILL_COND:slot_18>) {  // aligned to BB_10 (if_header)

 } else {
 <FILL_STMT:slot_19>;  // aligned to BB_11 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_15 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_8 (if_else_body)
 <FILL_STMT:slot_14>;  // aligned to BB_9 (if_else_body)
 }
 }
 <FILL_STMT:slot_20>;  // aligned to BB_12 (loop_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_2 (if_else_body)
 }
}

========================================================================================================================
example = 70  input_line = 133  func = blake2b_init  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 103-121
bb = 5  src_rows = 17  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 0/1  gaps(if,loop,switch)=(-1,0,0)  sim(type,depth)=0.000/0.000
bb_cover = 5/5  reasons = ['missing_if:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=5

-- SOURCE CONTROL OUTLINE --
if L106

-- AB SKELETON CONTROL OUTLINE --
<none>

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:sequential
BB_2:sequential
BB_3:sequential
BB_4:sequential

-- REAL SOURCE FUNCTION --
  103: {
  104:   blake2b_param P[1];
  105: 
  106:   if ( ( !outlen ) || ( outlen > BLAKE2B_OUTBYTES ) ) return -1;
  107: 
  108:   P->digest_length = (uint8_t)outlen;
  109:   P->key_length    = 0;
  110:   P->fanout        = 1;
  111:   P->depth         = 1;
  112:   store32( &P->leaf_length, 0 );
  113:   store32( &P->node_offset, 0 );
  114:   store32( &P->xof_length, 0 );
  115:   P->node_depth    = 0;
  116:   P->inner_length  = 0;
  117:   memset( P->reserved, 0, sizeof( P->reserved ) );
  118:   memset( P->salt,     0, sizeof( P->salt ) );
  119:   memset( P->personal, 0, sizeof( P->personal ) );
  120:   return blake2b_init_param( S, P );
  121: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (sequential)
}

========================================================================================================================
example = 71  input_line = 135  func = blake2b_init_param  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/blake2/blake2b-ref.c
source_bounds = 86-98
bb = 4  src_rows = 8  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L93

-- AB SKELETON CONTROL OUTLINE --
loop
  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_body
BB_3:sequential

-- REAL SOURCE FUNCTION --
   86: {
   87:   const uint8_t *p = ( const uint8_t * )( P );
   88:   size_t i;
   89: 
   90:   blake2b_init0( S );
   91: 
   92:   /* IV XOR ParamBlock */
   93:   for( i = 0; i < 8; ++i )
   94:     S->h[i] ^= load64( p + sizeof( S->h[i] ) * i );
   95: 
   96:   S->outlen = P->digest_length;
   97:   return 0;
   98: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (loop_body)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 }
 }
}

========================================================================================================================
example = 72  input_line = 137  func = compare_files  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/comm.c
source_bounds = 257-422
bb = 72  src_rows = 87  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 38/23  gaps(if,loop,switch)=(1,14,0)  sim(type,depth)=0.579/0.053
bb_cover = 72/72  reasons = ['extra_if:1', 'extra_loop:14']
10class_pattern_label_counts = sequential=16, if_header=18, if_then_body=6, if_else_body=13, loop_header=14, loop_body=5

-- SOURCE CONTROL OUTLINE --
loop L282
    loop L284
    if L293
    if L300
loop L304
    if L311
        if L313
              if L317
                    if L324
    if L331
          if L340
    if L356
    if L358
    loop L361
      if L362
          if L372
              if L378
          if L382
loop L389
  if L390
if L393
    if L399
if L417

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        if
          if
            if
              loop
                loop
                  loop
                    loop
                    loop
                    loop
                    loop
                    loop
                    if
                    loop
                    if
                    if
                    if
                    if
                    if
                    if
                    loop
                    if
                    loop
                    loop
                    loop
                    if
                    if
                    if
                    loop
                    loop
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:loop_body
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:if_header
BB_9:if_else_body
BB_10:if_header
BB_11:loop_header
BB_12:loop_header
BB_13:loop_body
BB_14:loop_header
BB_15:loop_header
BB_16:sequential
BB_17:loop_header
BB_18:sequential
BB_19:if_header
BB_20:if_else_body
BB_21:if_header
BB_22:if_else_body
BB_23:if_header
BB_24:if_else_body
BB_25:if_else_body
BB_26:sequential
BB_27:sequential
BB_28:if_header
BB_29:if_else_body
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:if_header
BB_34:if_then_body
BB_35:if_header
BB_36:if_then_body
BB_37:if_else_body
BB_38:sequential
BB_39:if_header
BB_40:if_else_body
BB_41:if_header
BB_42:loop_header
BB_43:loop_header
BB_44:loop_header
BB_45:if_header
BB_46:if_header
BB_47:if_else_body
BB_48:if_header
BB_49:if_else_body
BB_50:sequential
BB_51:if_header
BB_52:if_else_body
BB_53:sequential
BB_54:if_then_body
BB_55:loop_body
BB_56:loop_body
BB_57:loop_header
BB_58:loop_header
BB_59:if_header
BB_60:if_then_body
BB_61:if_then_body
BB_62:loop_body
BB_63:if_header
BB_64:if_header
BB_65:if_then_body
BB_66:if_else_body
BB_67:sequential
BB_68:if_header
BB_69:if_else_body
BB_70:sequential
BB_71:sequential

-- REAL SOURCE FUNCTION --
  257: {
  258:   /* For each file, we have four linebuffers in lba. */
  259:   struct linebuffer lba[2][4];
  260: 
  261:   /* thisline[i] points to the linebuffer holding the next available line
  262:      in file i, or is null if there are no lines left in that file.  */
  263:   struct linebuffer *thisline[2];
  264: 
  265:   /* all_line[i][alt[i][0]] also points to the linebuffer holding the
  266:      current line in file i. We keep two buffers of history around so we
  267:      can look two lines back when we get to the end of a file. */
  268:   struct linebuffer *all_line[2][4];
  269: 
  270:   /* This is used to rotate through the buffers for each input file. */
  271:   int alt[2][3];
  272: 
  273:   /* streams[i] holds the input stream for file i.  */
  274:   FILE *streams[2];
  275: 
  276:   /* Counters for the summary.  */
  277:   uintmax_t total[] = {0, 0, 0};
  278: 
  279:   int i, j;
  280: 
  281:   /* Initialize the storage. */
  282:   for (i = 0; i < 2; i++)
  283:     {
  284:       for (j = 0; j < 4; j++)
  285:         {
  286:           initbuffer (&lba[i][j]);
  287:           all_line[i][j] = &lba[i][j];
  288:         }
  289:       alt[i][0] = 0;
  290:       alt[i][1] = 0;
  291:       alt[i][2] = 0;
  292:       streams[i] = (STREQ (infiles[i], "-") ? stdin : fopen (infiles[i], "r"));
  293:       if (!streams[i])
  294:         error (EXIT_FAILURE, errno, "%s", quotef (infiles[i]));
  295: 
  296:       fadvise (streams[i], FADVISE_SEQUENTIAL);
  297: 
  298:       thisline[i] = readlinebuffer_delim (all_line[i][alt[i][0]], streams[i],
  299:                                           delim);
  300:       if (ferror (streams[i]))
  301:         error (EXIT_FAILURE, errno, "%s", quotef (infiles[i]));
  302:     }
  303: 
  304:   while (thisline[0] || thisline[1])
  305:     {
  306:       int order;
  307:       bool fill_up[2] = { false, false };
  308: 
  309:       /* Compare the next available lines of the two files.  */
  310: 
  311:       if (!thisline[0])
  312:         order = 1;
  313:       else if (!thisline[1])
  314:         order = -1;
  315:       else
  316:         {
  317:           if (hard_LC_COLLATE)
  318:             order = xmemcoll (thisline[0]->buffer, thisline[0]->length - 1,
  319:                               thisline[1]->buffer, thisline[1]->length - 1);
  320:           else
  321:             {
  322:               size_t len = min (thisline[0]->length, thisline[1]->length) - 1;
  323:               order = memcmp (thisline[0]->buffer, thisline[1]->buffer, len);
  324:               if (order == 0)
  325:                 order = ((thisline[0]->length > thisline[1]->length)
  326:                          - (thisline[0]->length < thisline[1]->length));
  327:             }
  328:         }
  329: 
  330:       /* Output the line that is lesser. */
  331:       if (order == 0)
  332:         {
  333:           /* Line is seen in both files.  */
  334:           total[2]++;
  335:           writeline (thisline[1], 3);
  336:         }
  337:       else
  338:         {
  339:           seen_unpairable = true;
  340:           if (order <= 0)
  341:             {
  342:               /* Line is seen in file 1 only.  */
  343:               total[0]++;
  344:               writeline (thisline[0], 1);
  345:             }
  346:           else
  347:             {
  348:               /* Line is seen in file 2 only.  */
  349:               total[1]++;
  350:               writeline (thisline[1], 2);
  351:             }
  352:         }
  353: 
  354:       /* Step the file the line came from.
  355:          If the files match, step both files.  */
  356:       if (0 <= order)
  357:         fill_up[1] = true;
  358:       if (order <= 0)
  359:         fill_up[0] = true;
  360: 
  361:       for (i = 0; i < 2; i++)
  362:         if (fill_up[i])
  363:           {
  364:             /* Rotate the buffers for this file. */
  365:             alt[i][2] = alt[i][1];
  366:             alt[i][1] = alt[i][0];
  367:             alt[i][0] = (alt[i][0] + 1) & 0x03;
  368: 
  369:             thisline[i] = readlinebuffer_delim (all_line[i][alt[i][0]],
  370:                                                 streams[i], delim);
  371: 
  372:             if (thisline[i])
  373:               check_order (all_line[i][alt[i][1]], thisline[i], i + 1);
  374: 
  375:             /* If this is the end of the file we may need to re-check
  376:                the order of the previous two lines, since we might have
  377:                discovered an unpairable match since we checked before. */
  378:             else if (all_line[i][alt[i][2]]->buffer)
  379:               check_order (all_line[i][alt[i][2]],
  380:                            all_line[i][alt[i][1]], i + 1);
  381: 
  382:             if (ferror (streams[i]))
  383:               error (EXIT_FAILURE, errno, "%s", quotef (infiles[i]));
  384: 
  385:             fill_up[i] = false;
  386:           }
  387:     }
  388: 
  389:   for (i = 0; i < 2; i++)
  390:     if (fclose (streams[i]) != 0)
  391:       error (EXIT_FAILURE, errno, "%s", quotef (infiles[i]));
  392: 
  393:   if (total_option)
  394:     {
  395:       /* Print the summary, minding the column and line delimiters.  */
  396:       char buf1[INT_BUFSIZE_BOUND (uintmax_t)];
  397:       char buf2[INT_BUFSIZE_BOUND (uintmax_t)];
  398:       char buf3[INT_BUFSIZE_BOUND (uintmax_t)];
  399:       if (col_sep_len == 1)
  400:         { /* Separate to handle NUL char.  */
  401:           printf ("%s%c%s%c%s%c%s%c",
  402:                   umaxtostr (total[0], buf1), *col_sep,
  403:                   umaxtostr (total[1], buf2), *col_sep,
  404:                   umaxtostr (total[2], buf3), *col_sep,
  405:                   _("total"), delim);
  406:         }
  407:       else
  408:         {
  409:           printf ("%s%s%s%s%s%s%s%c",
  410:                   umaxtostr (total[0], buf1), col_sep,
  411:                   umaxtostr (total[1], buf2), col_sep,
  412:                   umaxtostr (total[2], buf3), col_sep,
  413:                   _("total"), delim);
  414:         }
  415:     }
  416: 
  417:   if (issued_disorder_warning[0] || issued_disorder_warning[1])
  418:     error (EXIT_FAILURE, 0, _("input is not in sorted order"));
  419: 
  420:   /* Exit here to pacify gcc -fsanitizer=leak.  */
  421:   exit (EXIT_SUCCESS);
  422: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (loop_body)
 if (<FILL_COND:slot_7>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_8>;  // aligned to BB_6 (if_then_body)
 if (<FILL_COND:slot_9>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_10 (if_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_11 (loop_header)
 while (<FILL_COND:slot_13>) {  // aligned to BB_12 (loop_header)
 <FILL_STMT:slot_14>;  // aligned to BB_13 (loop_body)
 while (<FILL_COND:slot_15>) {  // aligned to BB_14 (loop_header)
 while (<FILL_COND:slot_16>) {  // aligned to BB_15 (loop_header)
 <FILL_STMT:slot_17>;  // aligned to BB_16 (sequential)
 while (<FILL_COND:slot_18>) {  // aligned to BB_17 (loop_header)
 while (<FILL_COND:slot_19>) {  // aligned to BB_18 (loop_header)
 while (<FILL_COND:slot_20>) {  // aligned to BB_57 (loop_header)
 while (<FILL_COND:slot_21>) {  // aligned to BB_58 (loop_header)
 if (<FILL_COND:slot_22>) {  // aligned to BB_59 (if_header)
 <FILL_STMT:slot_23>;  // aligned to BB_60 (if_then_body)
 while (<FILL_COND:slot_24>) {  // aligned to BB_61 (loop_header)
 <FILL_STMT:slot_25>;  // aligned to BB_62 (loop_body)
 if (<FILL_COND:slot_26>) {  // aligned to BB_63 (if_header)
 if (<FILL_COND:slot_27>) {  // aligned to BB_64 (if_header)
 <FILL_STMT:slot_28>;  // aligned to BB_65 (if_then_body)
 <FILL_STMT:slot_29>;  // aligned to BB_67 (sequential)
 if (<FILL_COND:slot_30>) {  // aligned to BB_68 (if_header)
 <FILL_STMT:slot_32>;  // aligned to BB_70 (sequential)
 <FILL_STMT:slot_33>;  // aligned to BB_71 (sequential)
 if (<FILL_COND:slot_35>) {  // aligned to BB_19 (if_header)
 <FILL_STMT:slot_36>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_37>) {  // aligned to BB_33 (if_header)
 <FILL_STMT:slot_38>;  // aligned to BB_34 (if_then_body)
 if (<FILL_COND:slot_39>) {  // aligned to BB_39 (if_header)
 while (<FILL_COND:slot_40>) {  // aligned to BB_40 (loop_header)
 if (<FILL_COND:slot_41>) {  // aligned to BB_41 (if_header)
 while (<FILL_COND:slot_42>) {  // aligned to BB_42 (loop_header)
 while (<FILL_COND:slot_43>) {  // aligned to BB_43 (loop_header)
 while (<FILL_COND:slot_44>) {  // aligned to BB_44 (loop_header)
 if (<FILL_COND:slot_45>) {  // aligned to BB_45 (if_header)
 if (<FILL_COND:slot_46>) {  // aligned to BB_46 (if_header)
 if (<FILL_COND:slot_48>) {  // aligned to BB_51 (if_header)
 while (<FILL_COND:slot_50>) {  // aligned to BB_53 (loop_header)
 while (<FILL_COND:slot_51>) {  // aligned to BB_54 (loop_header)
 <FILL_STMT:slot_52>;  // aligned to BB_55 (loop_body)
 if (<FILL_COND:slot_53>) {  // aligned to BB_48 (if_header)
 <FILL_STMT:slot_55>;  // aligned to BB_50 (sequential)
 if (<FILL_COND:slot_56>) {  // aligned to BB_35 (if_header)
 <FILL_STMT:slot_57>;  // aligned to BB_36 (if_then_body)
 <FILL_STMT:slot_58>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_60>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_62>;  // aligned to BB_32 (sequential)
 if (<FILL_COND:slot_63>) {  // aligned to BB_23 (if_header)
 <FILL_STMT:slot_64>;  // aligned to BB_24 (if_then_body)
 <FILL_STMT:slot_65>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_67>;  // aligned to BB_26 (sequential)
 if (<FILL_COND:slot_68>) {  // aligned to BB_28 (if_header)
 <FILL_STMT:slot_70>;  // aligned to BB_30 (sequential)
 <FILL_STMT:slot_71>;  // aligned to BB_27 (sequential)
 } else {
 <FILL_STMT:slot_69>;  // aligned to BB_29 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_66>;  // aligned to BB_25 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_61>;  // aligned to BB_22 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_59>;  // aligned to BB_37 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_54>;  // aligned to BB_49 (if_else_body)
 }
 <FILL_STMT:slot_72>;  // aligned to BB_56 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_49>;  // aligned to BB_52 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_47>;  // aligned to BB_47 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_31>;  // aligned to BB_69 (if_else_body)
 <FILL_STMT:slot_34>;  // aligned to BB_66 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_73>;  // aligned to BB_7 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_9 (if_else_body)
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 73  input_line = 138  func = writeline  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/comm.c
source_bounds = 173-202
bb = 24  src_rows = 24  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 10/8  gaps(if,loop,switch)=(0,0,2)  sim(type,depth)=0.800/0.200
bb_cover = 24/24  reasons = ['extra_switch:2']
10class_pattern_label_counts = sequential=6, if_header=7, if_then_body=1, if_else_body=7, switch_header=3

-- SOURCE CONTROL OUTLINE --
switch L174
      if L177
      if L182
      if L184
      if L189
      if L191
      if L193
if L200

-- AB SKELETON CONTROL OUTLINE --
switch
  switch
    switch
      if
        if
          if
            if
              if
                if
                  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:switch_header
BB_1:sequential
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:if_header
BB_7:if_then_body
BB_8:if_else_body
BB_9:if_header
BB_10:if_else_body
BB_11:if_header
BB_12:if_else_body
BB_13:sequential
BB_14:if_header
BB_15:if_else_body
BB_16:if_header
BB_17:if_else_body
BB_18:if_header
BB_19:if_else_body
BB_20:sequential
BB_21:if_header
BB_22:if_else_body
BB_23:sequential

-- REAL SOURCE FUNCTION --
  173: {
  174:   switch (class)
  175:     {
  176:     case 1:
  177:       if (!only_file_1)
  178:         return;
  179:       break;
  180: 
  181:     case 2:
  182:       if (!only_file_2)
  183:         return;
  184:       if (only_file_1)
  185:         fwrite (col_sep, 1, col_sep_len, stdout);
  186:       break;
  187: 
  188:     case 3:
  189:       if (!both)
  190:         return;
  191:       if (only_file_1)
  192:         fwrite (col_sep, 1, col_sep_len, stdout);
  193:       if (only_file_2)
  194:         fwrite (col_sep, 1, col_sep_len, stdout);
  195:       break;
  196:     }
  197: 
  198:   fwrite (line->buffer, sizeof (char), line->length, stdout);
  199: 
  200:   if (ferror (stdout))
  201:     write_error ();
  202: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 switch (<FILL_COND:slot_2>) {  // aligned to BB_0 (switch_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (sequential)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 if (<FILL_COND:slot_8>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_23 (sequential)
 if (<FILL_COND:slot_11>) {  // aligned to BB_14 (if_header)
 <FILL_STMT:slot_12>;  // aligned to BB_15 (if_then_body)
 if (<FILL_COND:slot_13>) {  // aligned to BB_16 (if_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_18 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_18>) {  // aligned to BB_9 (if_header)
 <FILL_STMT:slot_19>;  // aligned to BB_10 (if_then_body)
 if (<FILL_COND:slot_20>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_22>;  // aligned to BB_13 (sequential)
 if (<FILL_COND:slot_23>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_24>;  // aligned to BB_7 (if_then_body)
 } else {
 <FILL_STMT:slot_25>;  // aligned to BB_8 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_21>;  // aligned to BB_12 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_19 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_17 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_22 (if_else_body)
 }
 }
 }
 }
}

========================================================================================================================
example = 74  input_line = 139  func = check_order  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/comm.c
source_bounds = 219-247
bb = 15  src_rows = 13  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 6/4  gaps(if,loop,switch)=(2,0,0)  sim(type,depth)=0.667/0.500
bb_cover = 15/15  reasons = ['extra_if:2']
10class_pattern_label_counts = sequential=6, if_header=6, if_else_body=3

-- SOURCE CONTROL OUTLINE --
if L221
    if L224
        if L228
        if L235

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_header
BB_3:if_header
BB_4:if_header
BB_5:if_else_body
BB_6:if_else_body
BB_7:if_header
BB_8:if_else_body
BB_9:sequential
BB_10:sequential
BB_11:sequential
BB_12:sequential
BB_13:sequential
BB_14:sequential

-- REAL SOURCE FUNCTION --
  219: {
  220: 
  221:   if (check_input_order != CHECK_ORDER_DISABLED
  222:       && ((check_input_order == CHECK_ORDER_ENABLED) || seen_unpairable))
  223:     {
  224:       if (!issued_disorder_warning[whatfile - 1])
  225:         {
  226:           int order;
  227: 
  228:           if (hard_LC_COLLATE)
  229:             order = xmemcoll (prev->buffer, prev->length - 1,
  230:                               current->buffer, current->length - 1);
  231:           else
  232:             order = memcmp2 (prev->buffer, prev->length - 1,
  233:                              current->buffer, current->length - 1);
  234: 
  235:           if (0 < order)
  236:             {
  237:               error ((check_input_order == CHECK_ORDER_ENABLED
  238:                       ? EXIT_FAILURE : 0),
  239:                      0, _("file %d is not in sorted order"), whatfile);
  240: 
  241:               /* If we get to here, the message was just a warning, but we
  242:                  want only to issue it once. */
  243:               issued_disorder_warning[whatfile - 1] = true;
  244:             }
  245:         }
  246:     }
  247: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_4 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_then_body)
 if (<FILL_COND:slot_8>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_9 (sequential)
 <FILL_STMT:slot_11>;  // aligned to BB_10 (sequential)
 <FILL_STMT:slot_13>;  // aligned to BB_12 (sequential)
 <FILL_STMT:slot_14>;  // aligned to BB_13 (sequential)
 <FILL_STMT:slot_15>;  // aligned to BB_14 (sequential)
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_8 (if_else_body)
 <FILL_STMT:slot_12>;  // aligned to BB_11 (if_else_body)
 <FILL_STMT:slot_16>;  // aligned to BB_6 (if_else_body)
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 75  input_line = 140  func = main  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/comm.c
source_bounds = 426-512
bb = 50  src_rows = 63  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 18/7  gaps(if,loop,switch)=(0,2,9)  sim(type,depth)=0.389/0.056
bb_cover = 50/50  reasons = ['extra_loop:2', 'extra_switch:9', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=26, if_header=5, if_else_body=5, loop_header=3, loop_body=1, switch_header=10

-- SOURCE CONTROL OUTLINE --
loop L447
  switch L448
        if L475
if L493
if L496
    if L498
if L505

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      switch
        switch
          switch
            switch
              switch
                switch
                  switch
                    switch
                    switch
                    loop
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:switch_header
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:switch_header
BB_13:sequential
BB_14:switch_header
BB_15:sequential
BB_16:switch_header
BB_17:sequential
BB_18:switch_header
BB_19:sequential
BB_20:switch_header
BB_21:sequential
BB_22:sequential
BB_23:sequential
BB_24:sequential
BB_25:sequential
BB_26:sequential
BB_27:sequential
BB_28:if_header
BB_29:if_else_body
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:sequential
BB_35:sequential
BB_36:sequential
BB_37:sequential
BB_38:loop_header
BB_39:loop_body
BB_40:if_header
BB_41:if_else_body
BB_42:if_header
BB_43:if_header
BB_44:if_else_body
BB_45:if_else_body
BB_46:sequential
BB_47:if_header
BB_48:if_else_body
BB_49:sequential

-- REAL SOURCE FUNCTION --
  426: {
  427:   int c;
  428: 
  429:   initialize_main (&argc, &argv);
  430:   set_program_name (argv[0]);
  431:   setlocale (LC_ALL, "");
  432:   bindtextdomain (PACKAGE, LOCALEDIR);
  433:   textdomain (PACKAGE);
  434:   hard_LC_COLLATE = hard_locale (LC_COLLATE);
  435: 
  436:   atexit (close_stdout);
  437: 
  438:   only_file_1 = true;
  439:   only_file_2 = true;
  440:   both = true;
  441: 
  442:   seen_unpairable = false;
  443:   issued_disorder_warning[0] = issued_disorder_warning[1] = false;
  444:   check_input_order = CHECK_ORDER_DEFAULT;
  445:   total_option = false;
  446: 
  447:   while ((c = getopt_long (argc, argv, "123z", long_options, nullptr)) != -1)
  448:     switch (c)
  449:       {
  450:       case '1':
  451:         only_file_1 = false;
  452:         break;
  453: 
  454:       case '2':
  455:         only_file_2 = false;
  456:         break;
  457: 
  458:       case '3':
  459:         both = false;
  460:         break;
  461: 
  462:       case 'z':
  463:         delim = '\0';
  464:         break;
  465: 
  466:       case NOCHECK_ORDER_OPTION:
  467:         check_input_order = CHECK_ORDER_DISABLED;
  468:         break;
  469: 
  470:       case CHECK_ORDER_OPTION:
  471:         check_input_order = CHECK_ORDER_ENABLED;
  472:         break;
  473: 
  474:       case OUTPUT_DELIMITER_OPTION:
  475:         if (col_sep_len && !STREQ (col_sep, optarg))
  476:           error (EXIT_FAILURE, 0, _("multiple output delimiters specified"));
  477:         col_sep = optarg;
  478:         col_sep_len = *optarg ? strlen (optarg) : 1;
  479:         break;
  480: 
  481:       case TOTAL_OPTION:
  482:         total_option = true;
  483:         break;
  484: 
  485:       case_GETOPT_HELP_CHAR;
  486: 
  487:       case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
  488: 
  489:       default:
  490:         usage (EXIT_FAILURE);
  491:       }
  492: 
  493:   if (! col_sep_len)
  494:     col_sep_len = 1;
  495: 
  496:   if (argc - optind < 2)
  497:     {
  498:       if (argc <= optind)
  499:         error (0, 0, _("missing operand"));
  500:       else
  501:         error (0, 0, _("missing operand after %s"), quote (argv[argc - 1]));
  502:       usage (EXIT_FAILURE);
  503:     }
  504: 
  505:   if (2 < argc - optind)
  506:     {
  507:       error (0, 0, _("extra operand %s"), quote (argv[optind + 2]));
  508:       usage (EXIT_FAILURE);
  509:     }
  510: 
  511:   compare_files (argv + optind);
  512: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 switch (<FILL_COND:slot_8>) {  // aligned to BB_6 (switch_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 switch (<FILL_COND:slot_10>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_12>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 switch (<FILL_COND:slot_14>) {  // aligned to BB_12 (switch_header)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 switch (<FILL_COND:slot_16>) {  // aligned to BB_14 (switch_header)
 <FILL_STMT:slot_17>;  // aligned to BB_15 (sequential)
 switch (<FILL_COND:slot_18>) {  // aligned to BB_16 (switch_header)
 <FILL_STMT:slot_19>;  // aligned to BB_17 (sequential)
 switch (<FILL_COND:slot_20>) {  // aligned to BB_18 (switch_header)
 <FILL_STMT:slot_21>;  // aligned to BB_19 (sequential)
 switch (<FILL_COND:slot_22>) {  // aligned to BB_20 (switch_header)
 <FILL_STMT:slot_23>;  // aligned to BB_21 (sequential)
 while (<FILL_COND:slot_24>) {  // aligned to BB_38 (loop_header)
 <FILL_STMT:slot_25>;  // aligned to BB_39 (loop_body)
 <FILL_STMT:slot_26>;  // aligned to BB_35 (sequential)
 if (<FILL_COND:slot_27>) {  // aligned to BB_28 (if_header)
 <FILL_STMT:slot_30>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_32 (sequential)
 <FILL_STMT:slot_32>;  // aligned to BB_34 (sequential)
 <FILL_STMT:slot_33>;  // aligned to BB_33 (sequential)
 <FILL_STMT:slot_34>;  // aligned to BB_26 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_27 (sequential)
 <FILL_STMT:slot_36>;  // aligned to BB_25 (sequential)
 <FILL_STMT:slot_37>;  // aligned to BB_24 (sequential)
 <FILL_STMT:slot_38>;  // aligned to BB_23 (sequential)
 <FILL_STMT:slot_39>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_40>;  // aligned to BB_36 (sequential)
 <FILL_STMT:slot_41>;  // aligned to BB_37 (sequential)
 if (<FILL_COND:slot_42>) {  // aligned to BB_40 (if_header)
 if (<FILL_COND:slot_44>) {  // aligned to BB_42 (if_header)
 if (<FILL_COND:slot_45>) {  // aligned to BB_43 (if_header)
 <FILL_STMT:slot_47>;  // aligned to BB_46 (sequential)
 if (<FILL_COND:slot_48>) {  // aligned to BB_47 (if_header)
 <FILL_STMT:slot_49>;  // aligned to BB_48 (sequential)
 <FILL_STMT:slot_50>;  // aligned to BB_49 (sequential)
 } else {
 <FILL_STMT:slot_51>;  // aligned to BB_45 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_46>;  // aligned to BB_44 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_43>;  // aligned to BB_41 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_28>;  // aligned to BB_29 (if_else_body)
 <FILL_STMT:slot_29>;  // aligned to BB_30 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 76  input_line = 142  func = decode_preserve_arg  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cp.c
source_bounds = 877-965
bb = 17  src_rows = 50  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(-1,2,0)  sim(type,depth)=0.500/0.250
bb_cover = 17/17  reasons = ['missing_if:1', 'extra_loop:2']
10class_pattern_label_counts = sequential=12, if_header=1, if_else_body=1, loop_header=1, loop_body=1, switch_header=1

-- SOURCE CONTROL OUTLINE --
if L911
switch L917
      if L952

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    switch
      if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:sequential
BB_3:switch_header
BB_4:sequential
BB_5:sequential
BB_6:sequential
BB_7:sequential
BB_8:sequential
BB_9:sequential
BB_10:sequential
BB_11:if_header
BB_12:if_else_body
BB_13:sequential
BB_14:sequential
BB_15:loop_body
BB_16:sequential

-- REAL SOURCE FUNCTION --
  877: {
  878:   enum File_attribute
  879:     {
  880:       PRESERVE_MODE,
  881:       PRESERVE_TIMESTAMPS,
  882:       PRESERVE_OWNERSHIP,
  883:       PRESERVE_LINK,
  884:       PRESERVE_CONTEXT,
  885:       PRESERVE_XATTR,
  886:       PRESERVE_ALL
  887:     };
  888:   static enum File_attribute const preserve_vals[] =
  889:     {
  890:       PRESERVE_MODE, PRESERVE_TIMESTAMPS,
  891:       PRESERVE_OWNERSHIP, PRESERVE_LINK, PRESERVE_CONTEXT, PRESERVE_XATTR,
  892:       PRESERVE_ALL
  893:     };
  894:   /* Valid arguments to the '--preserve' option. */
  895:   static char const *const preserve_args[] =
  896:     {
  897:       "mode", "timestamps",
  898:       "ownership", "links", "context", "xattr", "all", nullptr
  899:     };
  900:   ARGMATCH_VERIFY (preserve_args, preserve_vals);
  901: 
  902:   char *arg_writable = xstrdup (arg);
  903:   char *s = arg_writable;
  904:   do
  905:     {
  906:       /* find next comma */
  907:       char *comma = strchr (s, ',');
  908:       enum File_attribute val;
  909: 
  910:       /* If we found a comma, put a NUL in its place and advance.  */
  911:       if (comma)
  912:         *comma++ = 0;
  913: 
  914:       /* process S.  */
  915:       val = XARGMATCH (on_off ? "--preserve" : "--no-preserve",
  916:                        s, preserve_args, preserve_vals);
  917:       switch (val)
  918:         {
  919:         case PRESERVE_MODE:
  920:           x->preserve_mode = on_off;
  921:           x->explicit_no_preserve_mode = !on_off;
  922:           break;
  923: 
  924:         case PRESERVE_TIMESTAMPS:
  925:           x->preserve_timestamps = on_off;
  926:           break;
  927: 
  928:         case PRESERVE_OWNERSHIP:
  929:           x->preserve_ownership = on_off;
  930:           break;
  931: 
  932:         case PRESERVE_LINK:
  933:           x->preserve_links = on_off;
  934:           break;
  935: 
  936:         case PRESERVE_CONTEXT:
  937:           x->require_preserve_context = on_off;
  938:           x->preserve_security_context = on_off;
  939:           break;
  940: 
  941:         case PRESERVE_XATTR:
  942:           x->preserve_xattr = on_off;
  943:           x->require_preserve_xattr = on_off;
  944:           break;
  945: 
  946:         case PRESERVE_ALL:
  947:           x->preserve_mode = on_off;
  948:           x->preserve_timestamps = on_off;
  949:           x->preserve_ownership = on_off;
  950:           x->preserve_links = on_off;
  951:           x->explicit_no_preserve_mode = !on_off;
  952:           if (selinux_enabled)
  953:             x->preserve_security_context = on_off;
  954:           x->preserve_xattr = on_off;
  955:           break;
  956: 
  957:         default:
  958:           affirm (false);
  959:         }
  960:       s = comma;
  961:     }
  962:   while (s);
  963: 
  964:   free (arg_writable);
  965: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 switch (<FILL_COND:slot_5>) {  // aligned to BB_3 (switch_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_16 (if_else_body)
 <FILL_STMT:slot_9>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_10>;  // aligned to BB_6 (sequential)
 <FILL_STMT:slot_11>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_12>;  // aligned to BB_8 (sequential)
 <FILL_STMT:slot_13>;  // aligned to BB_9 (sequential)
 <FILL_STMT:slot_14>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_15>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_13 (sequential)
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_12 (if_else_body)
 }
 }
 <FILL_STMT:slot_18>;  // aligned to BB_15 (loop_body)
 }
 }
}

========================================================================================================================
example = 77  input_line = 143  func = do_copy  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cp.c
source_bounds = 610-1202
bb = 64  src_rows = 361  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 34/21  gaps(if,loop,switch)=(7,6,0)  sim(type,depth)=0.559/0.059
bb_cover = 64/64  reasons = ['extra_if:7', 'extra_loop:6', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=13, if_header=24, if_then_body=2, if_else_body=15, loop_header=6, loop_body=3, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L615
    if L617
if L627
    if L629
    if L633
    if L639
        if L642
          if L650
                if L659
                if L671
if L679
    if L688
    loop L694
        if L704
        if L707
              loop L753
              if L760
        if L765
            loop L767
      if L784
      if L797

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if
              loop
                loop
                  if
                    if
                    loop
                    if
                    if
                    loop
                    loop
                    loop
                    loop
                    if
                    loop
                    loop
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:if_else_body
BB_4:sequential
BB_5:if_header
BB_6:if_header
BB_7:if_else_body
BB_8:if_header
BB_9:if_else_body
BB_10:sequential
BB_11:if_header
BB_12:if_header
BB_13:if_else_body
BB_14:sequential
BB_15:if_header
BB_16:if_else_body
BB_17:if_then_body
BB_18:if_header
BB_19:if_else_body
BB_20:if_header
BB_21:if_else_body
BB_22:sequential
BB_23:sequential
BB_24:sequential
BB_25:if_header
BB_26:if_header
BB_27:if_else_body
BB_28:sequential
BB_29:loop_header
BB_30:if_header
BB_31:if_else_body
BB_32:if_header
BB_33:if_else_body
BB_34:sequential
BB_35:if_else_body
BB_36:sequential
BB_37:if_header
BB_38:if_then_body
BB_39:loop_header
BB_40:loop_header
BB_41:loop_body
BB_42:if_header
BB_43:if_else_body
BB_44:sequential
BB_45:loop_header
BB_46:loop_header
BB_47:loop_header
BB_48:loop_body
BB_49:sequential
BB_50:loop_body
BB_51:sequential
BB_52:if_header
BB_53:if_else_body
BB_54:if_header
BB_55:if_header
BB_56:if_header
BB_57:if_header
BB_58:if_header
BB_59:if_header
BB_60:if_header
BB_61:if_else_body
BB_62:sequential
BB_63:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  610: {
  611:   struct stat sb;
  612:   bool new_dst = false;
  613:   bool ok = true;
  614: 
  615:   if (n_files <= !target_directory)
  616:     {
  617:       if (n_files <= 0)
  618:         error (0, 0, _("missing file operand"));
  619:       else
  620:         error (0, 0, _("missing destination file operand after %s"),
  621:                quoteaf (file[0]));
  622:       usage (EXIT_FAILURE);
  623:     }
  624: 
  625:   sb.st_mode = 0;
  626:   int target_dirfd = AT_FDCWD;
  627:   if (no_target_directory)
  628:     {
  629:       if (target_directory)
  630:         error (EXIT_FAILURE, 0,
  631:                _("cannot combine --target-directory (-t) "
  632:                  "and --no-target-directory (-T)"));
  633:       if (2 < n_files)
  634:         {
  635:           error (0, 0, _("extra operand %s"), quoteaf (file[2]));
  636:           usage (EXIT_FAILURE);
  637:         }
  638:     }
  639:   else if (target_directory)
  640:     {
  641:       target_dirfd = target_directory_operand (target_directory, &sb);
  642:       if (! target_dirfd_valid (target_dirfd))
  643:         error (EXIT_FAILURE, errno, _("target directory %s"),
  644:                quoteaf (target_directory));
  645:     }
  646:   else
  647:     {
  648:       char const *lastfile = file[n_files - 1];
  649:       int fd = target_directory_operand (lastfile, &sb);
  650:       if (target_dirfd_valid (fd))
  651:         {
  652:           target_dirfd = fd;
  653:           target_directory = lastfile;
  654:           n_files--;
  655:         }
  656:       else
  657:         {
  658:           int err = errno;
  659:           if (err == ENOENT)
  660:             new_dst = true;
  661: 
  662:           /* The last operand LASTFILE cannot be opened as a directory.
  663:              If there are more than two operands, report an error.
  664: 
  665:              Also, report an error if LASTFILE is known to be a directory
  666:              even though it could not be opened, which can happen if
  667:              opening failed with EACCES on a platform lacking O_PATH.
  668:              In this case use stat to test whether LASTFILE is a
  669:              directory, in case opening a non-directory with (O_SEARCH
  670:              | O_DIRECTORY) failed with EACCES not ENOTDIR.  */
  671:           if (2 < n_files
  672:               || (O_PATHSEARCH == O_SEARCH && err == EACCES
  673:                   && (sb.st_mode || stat (lastfile, &sb) == 0)
  674:                   && S_ISDIR (sb.st_mode)))
  675:             error (EXIT_FAILURE, err, _("target %s"), quoteaf (lastfile));
  676:         }
  677:     }
  678: 
  679:   if (target_directory)
  680:     {
  681:       /* cp file1...filen edir
  682:          Copy the files 'file1' through 'filen'
  683:          to the existing directory 'edir'. */
  684: 
  685:       /* Initialize these hash tables only if we'll need them.
  686:          The problems they're used to detect can arise only if
  687:          there are two or more files to copy.  */
  688:       if (2 <= n_files)
  689:         {
  690:           dest_info_init (x);
  691:           src_info_init (x);
  692:         }
  693: 
  694:       for (int i = 0; i < n_files; i++)
  695:         {
  696:           char *dst_name;
  697:           bool parent_exists = true;  /* True if dir_name (dst_name) exists. */
  698:           struct dir_attr *attr_list;
  699:           char *arg_in_concat;
  700:           char *arg = file[i];
  701: 
  702:           /* Trailing slashes are meaningful (i.e., maybe worth preserving)
  703:              only in the source file names.  */
  704:           if (remove_trailing_slashes)
  705:             strip_trailing_slashes (arg);
  706: 
  707:           if (parents_option)
  708:             {
  709:               char *arg_no_trailing_slash;
  710: 
  711:               /* Use 'arg' without trailing slashes in constructing destination
  712:                  file names.  Otherwise, we can end up trying to create a
  713:                  directory using a name with trailing slash, which fails on
  714:                  NetBSD 1.[34] systems.  */
  715:               ASSIGN_STRDUPA (arg_no_trailing_slash, arg);
  716:               strip_trailing_slashes (arg_no_trailing_slash);
  717: 
  718:               /* Append all of 'arg' (minus any trailing slash) to 'dest'.  */
  719:               dst_name = file_name_concat (target_directory,
  720:                                            arg_no_trailing_slash,
  721:                                            &arg_in_concat);
  722: 
  723:               /* For --parents, we have to make sure that the directory
  724:                  dir_name (dst_name) exists.  We may have to create a few
  725:                  leading directories. */
  726:               parent_exists =
  727:                 (make_dir_parents_private
  728:                  (dst_name, arg_in_concat - dst_name, target_dirfd,
  729:                   (x->verbose ? "%s -> %s\n" : nullptr),
  730:                   &attr_list, &new_dst, x));
  731:             }
  732:           else
  733:             {
  734:               char *arg_base;
  735:               /* Append the last component of 'arg' to 'target_directory'.  */
  736:               ASSIGN_STRDUPA (arg_base, last_component (arg));
  737:               strip_trailing_slashes (arg_base);
  738:               /* For 'cp -R source/.. dest', don't copy into 'dest/..'. */
  739:               arg_base += STREQ (arg_base, "..");
  740:               dst_name = file_name_concat (target_directory, arg_base,
  741:                                            &arg_in_concat);
  742:             }
  743: 
  744:           if (!parent_exists)
  745:             {
  746:               /* make_dir_parents_private failed, so don't even
  747:                  attempt the copy.  */
  748:               ok = false;
  749:             }
  750:           else
  751:             {
  752:               char const *dst_relname = arg_in_concat;
  753:               while (*dst_relname == '/')
  754:                 dst_relname++;
  755: 
  756:               bool copy_into_self;
  757:               ok &= copy (arg, dst_name, target_dirfd, dst_relname,
  758:                           new_dst, x, &copy_into_self, nullptr);
  759: 
  760:               if (parents_option)
  761:                 ok &= re_protect (dst_name, arg_in_concat, target_dirfd,
  762:                                   dst_relname, attr_list, x);
  763:             }
  764: 
  765:           if (parents_option)
  766:             {
  767:               while (attr_list)
  768:                 {
  769:                   struct dir_attr *p = attr_list;
  770:                   attr_list = attr_list->next;
  771:                   free (p);
  772:                 }
  773:             }
  774: 
  775:           free (dst_name);
  776:         }
  777:     }
  778:   else /* !target_directory */
  779:     {
  780:       char const *source = file[0];
  781:       char const *dest = file[1];
  782:       bool unused;
  783: 
  784:       if (parents_option)
  785:         {
  786:           error (0, 0,
  787:                  _("with --parents, the destination must be a directory"));
  788:           usage (EXIT_FAILURE);
  789:         }
  790: 
  791:       /* When the force and backup options have been specified and
  792:          the source and destination are the same name for an existing
  793:          regular file, convert the user's command, e.g.,
  794:          'cp --force --backup foo foo' to 'cp --force foo fooSUFFIX'
  795:          where SUFFIX is determined by any version control options used.  */
  796: 
  797:       if (x->unlink_dest_after_failed_open
  798:           && x->backup_type != no_backups
  799:           && STREQ (source, dest)
  800:           && !new_dst
  801:           && (sb.st_mode != 0 || stat (dest, &sb) == 0) && S_ISREG (sb.st_mode))
  802:         {
  803:           static struct cp_options x_tmp;
  804: 
  805:           dest = find_backup_file_name (AT_FDCWD, dest, x->backup_type);
  806:           /* Set x->backup_type to 'no_backups' so that the normal backup
  807:              mechanism is not used when performing the actual copy.
  808:              backup_type must be set to 'no_backups' only *after* the above
  809:              call to find_backup_file_name -- that function uses
  810:              backup_type to determine the suffix it applies.  */
  811:           x_tmp = *x;
  812:           x_tmp.backup_type = no_backups;
  813:           x = &x_tmp;
  814:         }
  815: 
  816:       ok = copy (source, dest, AT_FDCWD, dest, -new_dst, x, &unused, nullptr);
  817:     }
  818: 
  819:   return ok;
  820: }
  821: 
  822: static void
  823: cp_option_init (struct cp_options *x)
  824: {
  825:   cp_options_default (x);
  826:   x->copy_as_regular = true;
  827:   x->dereference = DEREF_UNDEFINED;
  828:   x->unlink_dest_before_opening = false;
  829:   x->unlink_dest_after_failed_open = false;
  830:   x->hard_link = false;
  831:   x->interactive = I_UNSPECIFIED;
  832:   x->move_mode = false;
  833:   x->install_mode = false;
  834:   x->one_file_system = false;
  835:   x->reflink_mode = REFLINK_AUTO;
  836: 
  837:   x->preserve_ownership = false;
  838:   x->preserve_links = false;
  839:   x->preserve_mode = false;
  840:   x->preserve_timestamps = false;
  841:   x->explicit_no_preserve_mode = false;
  842:   x->preserve_security_context = false; /* -a or --preserve=context.  */
  843:   x->require_preserve_context = false;  /* --preserve=context.  */
  844:   x->set_security_context = nullptr;       /* -Z, set sys default context. */
  845:   x->preserve_xattr = false;
  846:   x->reduce_diagnostics = false;
  847:   x->require_preserve_xattr = false;
  848: 
  849:   x->data_copy_required = true;
  850:   x->require_preserve = false;
  851:   x->recursive = false;
  852:   x->sparse_mode = SPARSE_AUTO;
  853:   x->symbolic_link = false;
  854:   x->set_mode = false;
  855:   x->mode = 0;
  856: 
  857:   /* Not used.  */
  858:   x->stdin_tty = false;
  859: 
  860:   x->update = false;
  861:   x->verbose = false;
  862: 
  863:   /* By default, refuse to open a dangling destination symlink, because
  864:      in general one cannot do that safely, give the current semantics of
  865:      open's O_EXCL flag, (which POSIX doesn't even allow cp to use, btw).
  866:      But POSIX requires it.  */
  867:   x->open_dangling_dest_symlink = getenv ("POSIXLY_CORRECT") != nullptr;
  868: 
  869:   x->dest_info = nullptr;
  870:   x->src_info = nullptr;
  871: }
  872: 
  873: /* Given a string, ARG, containing a comma-separated list of arguments
  874:    to the --preserve option, set the appropriate fields of X to ON_OFF.  */
  875: static void
  876: decode_preserve_arg (char const *arg, struct cp_options *x, bool on_off)
  877: {
  878:   enum File_attribute
  879:     {
  880:       PRESERVE_MODE,
  881:       PRESERVE_TIMESTAMPS,
  882:       PRESERVE_OWNERSHIP,
  883:       PRESERVE_LINK,
  884:       PRESERVE_CONTEXT,
  885:       PRESERVE_XATTR,
  886:       PRESERVE_ALL
  887:     };
  888:   static enum File_attribute const preserve_vals[] =
  889:     {
  890:       PRESERVE_MODE, PRESERVE_TIMESTAMPS,
  891:       PRESERVE_OWNERSHIP, PRESERVE_LINK, PRESERVE_CONTEXT, PRESERVE_XATTR,
  892:       PRESERVE_ALL
  893:     };
  894:   /* Valid arguments to the '--preserve' option. */
  895:   static char const *const preserve_args[] =
  896:     {
  897:       "mode", "timestamps",
  898:       "ownership", "links", "context", "xattr", "all", nullptr
  899:     };
  900:   ARGMATCH_VERIFY (preserve_args, preserve_vals);
  901: 
  902:   char *arg_writable = xstrdup (arg);
  903:   char *s = arg_writable;
  904:   do
  905:     {
  906:       /* find next comma */
  907:       char *comma = strchr (s, ',');
  908:       enum File_attribute val;
  909: 
  910:       /* If we found a comma, put a NUL in its place and advance.  */
  911:       if (comma)
  912:         *comma++ = 0;
  913: 
  914:       /* process S.  */
  915:       val = XARGMATCH (on_off ? "--preserve" : "--no-preserve",
  916:                        s, preserve_args, preserve_vals);
  917:       switch (val)
  918:         {
  919:         case PRESERVE_MODE:
  920:           x->preserve_mode = on_off;
  921:           x->explicit_no_preserve_mode = !on_off;
  922:           break;
  923: 
  924:         case PRESERVE_TIMESTAMPS:
  925:           x->preserve_timestamps = on_off;
  926:           break;
  927: 
  928:         case PRESERVE_OWNERSHIP:
  929:           x->preserve_ownership = on_off;
  930:           break;
  931: 
  932:         case PRESERVE_LINK:
  933:           x->preserve_links = on_off;
  934:           break;
  935: 
  936:         case PRESERVE_CONTEXT:
  937:           x->require_preserve_context = on_off;
  938:           x->preserve_security_context = on_off;
  939:           break;
  940: 
  941:         case PRESERVE_XATTR:
  942:           x->preserve_xattr = on_off;
  943:           x->require_preserve_xattr = on_off;
  944:           break;
  945: 
  946:         case PRESERVE_ALL:
  947:           x->preserve_mode = on_off;
  948:           x->preserve_timestamps = on_off;
  949:           x->preserve_ownership = on_off;
  950:           x->preserve_links = on_off;
  951:           x->explicit_no_preserve_mode = !on_off;
  952:           if (selinux_enabled)
  953:             x->preserve_security_context = on_off;
  954:           x->preserve_xattr = on_off;
  955:           break;
  956: 
  957:         default:
  958:           affirm (false);
  959:         }
  960:       s = comma;
  961:     }
  962:   while (s);
  963: 
  964:   free (arg_writable);
  965: }
  966: 
  967: int
  968: main (int argc, char **argv)
  969: {
  970:   int c;
  971:   bool ok;
  972:   bool make_backups = false;
  973:   char const *backup_suffix = nullptr;
  974:   char *version_control_string = nullptr;
  975:   struct cp_options x;
  976:   bool copy_contents = false;
  977:   char *target_directory = nullptr;
  978:   bool no_target_directory = false;
  979:   char const *scontext = nullptr;
  980: 
  981:   initialize_main (&argc, &argv);
  982:   set_program_name (argv[0]);
  983:   setlocale (LC_ALL, "");
  984:   bindtextdomain (PACKAGE, LOCALEDIR);
  985:   textdomain (PACKAGE);
  986: 
  987:   atexit (close_stdin);
  988: 
  989:   selinux_enabled = (0 < is_selinux_enabled ());
  990:   cp_option_init (&x);
  991: 
  992:   while ((c = getopt_long (argc, argv, "abdfHilLnprst:uvxPRS:TZ",
  993:                            long_opts, nullptr))
  994:          != -1)
  995:     {
  996:       switch (c)
  997:         {
  998:         case SPARSE_OPTION:
  999:           x.sparse_mode = XARGMATCH ("--sparse", optarg,
 1000:                                      sparse_type_string, sparse_type);
 1001:           break;
 1002: 
 1003:         case REFLINK_OPTION:
 1004:           if (optarg == nullptr)
 1005:             x.reflink_mode = REFLINK_ALWAYS;
 1006:           else
 1007:             x.reflink_mode = XARGMATCH ("--reflink", optarg,
 1008:                                        reflink_type_string, reflink_type);
 1009:           break;
 1010: 
 1011:         case 'a':
 1012:           /* Like -dR --preserve=all with reduced failure diagnostics.  */
 1013:           x.dereference = DEREF_NEVER;
 1014:           x.preserve_links = true;
 1015:           x.preserve_ownership = true;
 1016:           x.preserve_mode = true;
 1017:           x.preserve_timestamps = true;
 1018:           x.require_preserve = true;
 1019:           if (selinux_enabled)
 1020:              x.preserve_security_context = true;
 1021:           x.preserve_xattr = true;
 1022:           x.reduce_diagnostics = true;
 1023:           x.recursive = true;
 1024:           break;
 1025: 
 1026:         case 'b':
 1027:           make_backups = true;
 1028:           if (optarg)
 1029:             version_control_string = optarg;
 1030:           break;
 1031: 
 1032:         case ATTRIBUTES_ONLY_OPTION:
 1033:           x.data_copy_required = false;
 1034:           break;
 1035: 
 1036:         case DEBUG_OPTION:
 1037:           x.debug = x.verbose = true;
 1038:           break;
 1039: 
 1040:         case COPY_CONTENTS_OPTION:
 1041:           copy_contents = true;
 1042:           break;
 1043: 
 1044:         case 'd':
 1045:           x.preserve_links = true;
 1046:           x.dereference = DEREF_NEVER;
 1047:           break;
 1048: 
 1049:         case 'f':
 1050:           x.unlink_dest_after_failed_open = true;
 1051:           break;
 1052: 
 1053:         case 'H':
 1054:           x.dereference = DEREF_COMMAND_LINE_ARGUMENTS;
 1055:           break;
 1056: 
 1057:         case 'i':
 1058:           x.interactive = I_ASK_USER;
 1059:           break;
 1060: 
 1061:         case 'l':
 1062:           x.hard_link = true;
 1063:           break;
 1064: 
 1065:         case 'L':
 1066:           x.dereference = DEREF_ALWAYS;
 1067:           break;
 1068: 
 1069:         case 'n':
 1070:           x.interactive = I_ALWAYS_NO;
 1071:           break;
 1072: 
 1073:         case 'P':
 1074:           x.dereference = DEREF_NEVER;
 1075:           break;
 1076: 
 1077:         case NO_PRESERVE_ATTRIBUTES_OPTION:
 1078:           decode_preserve_arg (optarg, &x, false);
 1079:           break;
 1080: 
 1081:         case PRESERVE_ATTRIBUTES_OPTION:
 1082:           if (optarg == nullptr)
 1083:             {
 1084:               /* Fall through to the case for 'p' below.  */
 1085:             }
 1086:           else
 1087:             {
 1088:               decode_preserve_arg (optarg, &x, true);
 1089:               x.require_preserve = true;
 1090:               break;
 1091:             }
 1092:           FALLTHROUGH;
 1093: 
 1094:         case 'p':
 1095:           x.preserve_ownership = true;
 1096:           x.preserve_mode = true;
 1097:           x.preserve_timestamps = true;
 1098:           x.require_preserve = true;
 1099:           break;
 1100: 
 1101:         case PARENTS_OPTION:
 1102:           parents_option = true;
 1103:           break;
 1104: 
 1105:         case 'r':
 1106:         case 'R':
 1107:           x.recursive = true;
 1108:           break;
 1109: 
 1110:         case UNLINK_DEST_BEFORE_OPENING:
 1111:           x.unlink_dest_before_opening = true;
 1112:           break;
 1113: 
 1114:         case STRIP_TRAILING_SLASHES_OPTION:
 1115:           remove_trailing_slashes = true;
 1116:           break;
 1117: 
 1118:         case 's':
 1119:           x.symbolic_link = true;
 1120:           break;
 1121: 
 1122:         case 't':
 1123:           if (target_directory)
 1124:             error (EXIT_FAILURE, 0,
 1125:                    _("multiple target directories specified"));
 1126:           target_directory = optarg;
 1127:           break;
 1128: 
 1129:         case 'T':
 1130:           no_target_directory = true;
 1131:           break;
 1132: 
 1133:         case 'u':
 1134:           if (optarg == nullptr)
 1135:             x.update = true;
 1136:           else if (x.interactive != I_ALWAYS_NO)  /* -n takes precedence.  */
 1137:             {
 1138:               enum Update_type update_opt;
 1139:               update_opt = XARGMATCH ("--update", optarg,
 1140:                                       update_type_string, update_type);
 1141:               if (update_opt == UPDATE_ALL)
 1142:                 {
 1143:                   /* Default cp operation.  */
 1144:                   x.update = false;
 1145:                   x.interactive = I_UNSPECIFIED;
 1146:                 }
 1147:               else if (update_opt == UPDATE_NONE)
 1148:                 {
 1149:                   x.update = false;
 1150:                   x.interactive = I_ALWAYS_SKIP;
 1151:                 }
 1152:               else if (update_opt == UPDATE_OLDER)
 1153:                 {
 1154:                   x.update = true;
 1155:                   x.interactive = I_UNSPECIFIED;
 1156:                 }
 1157:             }
 1158:           break;
 1159: 
 1160:         case 'v':
 1161:           x.verbose = true;
 1162:           break;
 1163: 
 1164:         case 'x':
 1165:           x.one_file_system = true;
 1166:           break;
 1167: 
 1168:         case 'Z':
 1169:           /* politely decline if we're not on a selinux-enabled kernel.  */
 1170:           if (selinux_enabled)
 1171:             {
 1172:               if (optarg)
 1173:                 scontext = optarg;
 1174:               else
 1175:                 {
 1176:                   x.set_security_context = selabel_open (SELABEL_CTX_FILE,
 1177:                                                          nullptr, 0);
 1178:                   if (! x.set_security_context)
 1179:                     error (0, errno, _("warning: ignoring --context"));
 1180:                 }
 1181:             }
 1182:           else if (optarg)
 1183:             {
 1184:               error (0, 0,
 1185:                      _("warning: ignoring --context; "
 1186:                        "it requires an SELinux-enabled kernel"));
 1187:             }
 1188:           break;
 1189: 
 1190:         case 'S':
 1191:           make_backups = true;
 1192:           backup_suffix = optarg;
 1193:           break;
 1194: 
 1195:         case_GETOPT_HELP_CHAR;
 1196: 
 1197:         case_GETOPT_VERSION_CHAR (PROGRAM_NAME, AUTHORS);
 1198: 
 1199:         default:
 1200:           usage (EXIT_FAILURE);
 1201:         }
 1202:     }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 if (<FILL_COND:slot_6>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_11>;  // aligned to BB_10 (sequential)
 if (<FILL_COND:slot_12>) {  // aligned to BB_25 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_26 (if_header)
 while (<FILL_COND:slot_15>) {  // aligned to BB_28 (loop_header)
 while (<FILL_COND:slot_16>) {  // aligned to BB_29 (loop_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_30 (if_header)
 if (<FILL_COND:slot_19>) {  // aligned to BB_32 (if_header)
 <FILL_STMT:slot_20>;  // aligned to BB_33 (sequential)
 while (<FILL_COND:slot_21>) {  // aligned to BB_34 (loop_header)
 if (<FILL_COND:slot_22>) {  // aligned to BB_37 (if_header)
 <FILL_STMT:slot_23>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_24>) {  // aligned to BB_45 (if_header)
 while (<FILL_COND:slot_25>) {  // aligned to BB_46 (loop_header)
 while (<FILL_COND:slot_26>) {  // aligned to BB_47 (loop_header)
 <FILL_STMT:slot_27>;  // aligned to BB_48 (loop_body)
 <FILL_STMT:slot_28>;  // aligned to BB_49 (sequential)
 <FILL_STMT:slot_29>;  // aligned to BB_50 (loop_body)
 while (<FILL_COND:slot_30>) {  // aligned to BB_39 (loop_header)
 while (<FILL_COND:slot_31>) {  // aligned to BB_40 (loop_header)
 <FILL_STMT:slot_32>;  // aligned to BB_41 (loop_body)
 if (<FILL_COND:slot_33>) {  // aligned to BB_42 (if_header)
 while (<FILL_COND:slot_35>) {  // aligned to BB_44 (loop_header)
 <FILL_STMT:slot_36>;  // aligned to BB_35 (sequential)
 while (<FILL_COND:slot_37>) {  // aligned to BB_36 (loop_header)
 <FILL_STMT:slot_38>;  // aligned to BB_51 (sequential)
 <FILL_EXPR:slot_39>;  // aligned to BB_63 (func_prologue_epilogue)
 if (<FILL_COND:slot_40>) {  // aligned to BB_52 (if_header)
 <FILL_STMT:slot_41>;  // aligned to BB_53 (sequential)
 if (<FILL_COND:slot_42>) {  // aligned to BB_54 (if_header)
 if (<FILL_COND:slot_43>) {  // aligned to BB_55 (if_header)
 if (<FILL_COND:slot_44>) {  // aligned to BB_56 (if_header)
 if (<FILL_COND:slot_45>) {  // aligned to BB_57 (if_header)
 if (<FILL_COND:slot_46>) {  // aligned to BB_58 (if_header)
 if (<FILL_COND:slot_47>) {  // aligned to BB_59 (if_header)
 if (<FILL_COND:slot_48>) {  // aligned to BB_60 (if_header)
 <FILL_STMT:slot_50>;  // aligned to BB_62 (sequential)
 if (<FILL_COND:slot_51>) {  // aligned to BB_11 (if_header)
 if (<FILL_COND:slot_52>) {  // aligned to BB_12 (if_header)
 <FILL_STMT:slot_54>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_55>;  // aligned to BB_24 (sequential)
 if (<FILL_COND:slot_56>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_58>) {  // aligned to BB_18 (if_header)
 if (<FILL_COND:slot_60>) {  // aligned to BB_20 (if_header)
 <FILL_STMT:slot_62>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_63>;  // aligned to BB_23 (sequential)
 <FILL_STMT:slot_64>;  // aligned to BB_17 (sequential)
 } else {
 <FILL_STMT:slot_61>;  // aligned to BB_21 (if_else_body)
 <FILL_STMT:slot_65>;  // aligned to BB_3 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_59>;  // aligned to BB_19 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_57>;  // aligned to BB_16 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_53>;  // aligned to BB_13 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_49>;  // aligned to BB_61 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_34>;  // aligned to BB_43 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_31 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_14>;  // aligned to BB_27 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_9 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_8>;  // aligned to BB_7 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 78  input_line = 144  func = make_dir_parents_private  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cp.c
source_bounds = 404-602
bb = 73  src_rows = 101  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 35/22  gaps(if,loop,switch)=(7,6,0)  sim(type,depth)=0.629/0.086
bb_cover = 73/73  reasons = ['extra_if:7', 'extra_loop:6']
10class_pattern_label_counts = sequential=17, if_header=26, if_then_body=4, if_else_body=14, loop_header=8, loop_body=4

-- SOURCE CONTROL OUTLINE --
if L415
loop L426
if L431
    loop L438
    loop L442
        if L450
            if L461
        if L477
        if L482
            if L512
                  if L520
            if L528
            if L536
                if L538
                if L540
            if L549
                if L554
            if L562
        if L573
            if L576
        loop L585
    if L592

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        if
          loop
            loop
              loop
                loop
                  if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    if
                    loop
                    loop
                    loop
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:loop_header
BB_4:loop_header
BB_5:loop_body
BB_6:if_header
BB_7:loop_header
BB_8:loop_header
BB_9:loop_body
BB_10:loop_header
BB_11:loop_header
BB_12:if_header
BB_13:if_header
BB_14:if_header
BB_15:if_header
BB_16:sequential
BB_17:sequential
BB_18:sequential
BB_19:if_header
BB_20:if_then_body
BB_21:if_else_body
BB_22:if_header
BB_23:if_then_body
BB_24:sequential
BB_25:if_header
BB_26:if_else_body
BB_27:if_header
BB_28:if_else_body
BB_29:sequential
BB_30:sequential
BB_31:sequential
BB_32:sequential
BB_33:sequential
BB_34:if_header
BB_35:if_else_body
BB_36:if_header
BB_37:if_else_body
BB_38:sequential
BB_39:if_header
BB_40:if_else_body
BB_41:if_header
BB_42:if_header
BB_43:if_else_body
BB_44:if_header
BB_45:if_header
BB_46:sequential
BB_47:sequential
BB_48:if_header
BB_49:if_header
BB_50:if_else_body
BB_51:if_else_body
BB_52:sequential
BB_53:if_header
BB_54:if_then_body
BB_55:if_else_body
BB_56:if_header
BB_57:if_header
BB_58:if_header
BB_59:if_header
BB_60:if_header
BB_61:if_else_body
BB_62:sequential
BB_63:loop_header
BB_64:loop_header
BB_65:loop_body
BB_66:loop_body
BB_67:sequential
BB_68:if_header
BB_69:if_else_body
BB_70:if_else_body
BB_71:sequential
BB_72:sequential

-- REAL SOURCE FUNCTION --
  404: {
  405:   struct stat stats;
  406:   char *dir;		/* A copy of CONST_DIR we can change.  */
  407:   char *src;		/* Source name in DIR.  */
  408:   char *dst_dir;	/* Leading directory of DIR.  */
  409:   idx_t dirlen = dir_len (const_dir);
  410: 
  411:   *attr_list = nullptr;
  412: 
  413:   /* Succeed immediately if the parent of CONST_DIR must already exist,
  414:      as the target directory has already been checked.  */
  415:   if (dirlen <= src_offset)
  416:     return true;
  417: 
  418:   ASSIGN_STRDUPA (dir, const_dir);
  419: 
  420:   src = dir + src_offset;
  421: 
  422:   dst_dir = alloca (dirlen + 1);
  423:   memcpy (dst_dir, dir, dirlen);
  424:   dst_dir[dirlen] = '\0';
  425:   char const *dst_reldir = dst_dir + src_offset;
  426:   while (*dst_reldir == '/')
  427:     dst_reldir++;
  428: 
  429:   /* XXX: If all dirs are present at the destination,
  430:      no permissions or security contexts will be updated.  */
  431:   if (fstatat (dst_dirfd, dst_reldir, &stats, 0) != 0)
  432:     {
  433:       /* A parent of CONST_DIR does not exist.
  434:          Make all missing intermediate directories. */
  435:       char *slash;
  436: 
  437:       slash = src;
  438:       while (*slash == '/')
  439:         slash++;
  440:       dst_reldir = slash;
  441: 
  442:       while ((slash = strchr (slash, '/')))
  443:         {
  444:           struct dir_attr *new;
  445:           bool missing_dir;
  446: 
  447:           *slash = '\0';
  448:           missing_dir = fstatat (dst_dirfd, dst_reldir, &stats, 0) != 0;
  449: 
  450:           if (missing_dir || x->preserve_ownership || x->preserve_mode
  451:               || x->preserve_timestamps)
  452:             {
  453:               /* Add this directory to the list of directories whose
  454:                  modes might need fixing later. */
  455:               struct stat src_st;
  456:               int src_errno = (stat (src, &src_st) != 0
  457:                                ? errno
  458:                                : S_ISDIR (src_st.st_mode)
  459:                                ? 0
  460:                                : ENOTDIR);
  461:               if (src_errno)
  462:                 {
  463:                   error (0, src_errno, _("failed to get attributes of %s"),
  464:                          quoteaf (src));
  465:                   return false;
  466:                 }
  467: 
  468:               new = xmalloc (sizeof *new);
  469:               new->st = src_st;
  470:               new->slash_offset = slash - dir;
  471:               new->restore_mode = false;
  472:               new->next = *attr_list;
  473:               *attr_list = new;
  474:             }
  475: 
  476:           /* If required set the default context for created dirs.  */
  477:           if (! set_process_security_ctx (src, dir,
  478:                                           missing_dir ? new->st.st_mode : 0,
  479:                                           missing_dir, x))
  480:             return false;
  481: 
  482:           if (missing_dir)
  483:             {
  484:               mode_t src_mode;
  485:               mode_t omitted_permissions;
  486:               mode_t mkdir_mode;
  487: 
  488:               /* This component does not exist.  We must set
  489:                  *new_dst and new->st.st_mode inside this loop because,
  490:                  for example, in the command 'cp --parents ../a/../b/c e_dir',
  491:                  make_dir_parents_private creates only e_dir/../a if
  492:                  ./b already exists. */
  493:               *new_dst = true;
  494:               src_mode = new->st.st_mode;
  495: 
  496:               /* If the ownership or special mode bits might change,
  497:                  omit some permissions at first, so unauthorized users
  498:                  cannot nip in before the file is ready.  */
  499:               omitted_permissions = (src_mode
  500:                                      & (x->preserve_ownership
  501:                                         ? S_IRWXG | S_IRWXO
  502:                                         : x->preserve_mode
  503:                                         ? S_IWGRP | S_IWOTH
  504:                                         : 0));
  505: 
  506:               /* POSIX says mkdir's behavior is implementation-defined when
  507:                  (src_mode & ~S_IRWXUGO) != 0.  However, common practice is
  508:                  to ask mkdir to copy all the CHMOD_MODE_BITS, letting mkdir
  509:                  decide what to do with S_ISUID | S_ISGID | S_ISVTX.  */
  510:               mkdir_mode = x->explicit_no_preserve_mode ? S_IRWXUGO : src_mode;
  511:               mkdir_mode &= CHMOD_MODE_BITS & ~omitted_permissions;
  512:               if (mkdirat (dst_dirfd, dst_reldir, mkdir_mode) != 0)
  513:                 {
  514:                   error (0, errno, _("cannot make directory %s"),
  515:                          quoteaf (dir));
  516:                   return false;
  517:                 }
  518:               else
  519:                 {
  520:                   if (verbose_fmt_string != nullptr)
  521:                     printf (verbose_fmt_string, src, dir);
  522:                 }
  523: 
  524:               /* We need search and write permissions to the new directory
  525:                  for writing the directory's contents. Check if these
  526:                  permissions are there.  */
  527: 
  528:               if (fstatat (dst_dirfd, dst_reldir, &stats, AT_SYMLINK_NOFOLLOW))
  529:                 {
  530:                   error (0, errno, _("failed to get attributes of %s"),
  531:                          quoteaf (dir));
  532:                   return false;
  533:                 }
  534: 
  535: 
  536:               if (! x->preserve_mode)
  537:                 {
  538:                   if (omitted_permissions & ~stats.st_mode)
  539:                     omitted_permissions &= ~ cached_umask ();
  540:                   if (omitted_permissions & ~stats.st_mode
  541:                       || (stats.st_mode & S_IRWXU) != S_IRWXU)
  542:                     {
  543:                       new->st.st_mode = stats.st_mode | omitted_permissions;
  544:                       new->restore_mode = true;
  545:                     }
  546:                 }
  547: 
  548:               mode_t accessible = stats.st_mode | S_IRWXU;
  549:               if (stats.st_mode != accessible)
  550:                 {
  551:                   /* Make the new directory searchable and writable.
  552:                      The original permissions will be restored later.  */
  553: 
  554:                   if (lchmodat (dst_dirfd, dst_reldir, accessible) != 0)
  555:                     {
  556:                       error (0, errno, _("setting permissions for %s"),
  557:                              quoteaf (dir));
  558:                       return false;
  559:                     }
  560:                 }
  561:             }
  562:           else if (!S_ISDIR (stats.st_mode))
  563:             {
  564:               error (0, 0, _("%s exists but is not a directory"),
  565:                      quoteaf (dir));
  566:               return false;
  567:             }
  568:           else
  569:             *new_dst = false;
  570: 
  571:           /* For existing dirs, set the security context as per that already
  572:              set for the process global context.  */
  573:           if (! *new_dst
  574:               && (x->set_security_context || x->preserve_security_context))
  575:             {
  576:               if (! set_file_security_ctx (dir, false, x)
  577:                   && x->require_preserve_context)
  578:                 return false;
  579:             }
  580: 
  581:           *slash++ = '/';
  582: 
  583:           /* Avoid unnecessary calls to 'stat' when given
  584:              file names containing multiple adjacent slashes.  */
  585:           while (*slash == '/')
  586:             slash++;
  587:         }
  588:     }
  589: 
  590:   /* We get here if the parent of DIR already exists.  */
  591: 
  592:   else if (!S_ISDIR (stats.st_mode))
  593:     {
  594:       error (0, 0, _("%s exists but is not a directory"), quoteaf (dst_dir));
  595:       return false;
  596:     }
  597:   else
  598:     {
  599:       *new_dst = false;
  600:     }
  601:   return true;
  602: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_72 (sequential)
 while (<FILL_COND:slot_5>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_7>) {  // aligned to BB_4 (loop_header)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (loop_body)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_7 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_8 (loop_header)
 <FILL_STMT:slot_12>;  // aligned to BB_9 (loop_body)
 while (<FILL_COND:slot_13>) {  // aligned to BB_10 (loop_header)
 while (<FILL_COND:slot_14>) {  // aligned to BB_11 (loop_header)
 if (<FILL_COND:slot_15>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_13 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_14 (if_header)
 if (<FILL_COND:slot_18>) {  // aligned to BB_15 (if_header)
 <FILL_STMT:slot_19>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_17 (sequential)
 if (<FILL_COND:slot_21>) {  // aligned to BB_19 (if_header)
 <FILL_STMT:slot_24>;  // aligned to BB_22 (sequential)
 <FILL_STMT:slot_25>;  // aligned to BB_23 (sequential)
 if (<FILL_COND:slot_26>) {  // aligned to BB_25 (if_header)
 <FILL_STMT:slot_27>;  // aligned to BB_26 (if_then_body)
 if (<FILL_COND:slot_28>) {  // aligned to BB_27 (if_header)
 <FILL_STMT:slot_30>;  // aligned to BB_29 (sequential)
 <FILL_STMT:slot_31>;  // aligned to BB_31 (sequential)
 <FILL_STMT:slot_32>;  // aligned to BB_32 (sequential)
 if (<FILL_COND:slot_33>) {  // aligned to BB_34 (if_header)
 if (<FILL_COND:slot_35>) {  // aligned to BB_36 (if_header)
 <FILL_STMT:slot_37>;  // aligned to BB_38 (sequential)
 if (<FILL_COND:slot_38>) {  // aligned to BB_39 (if_header)
 if (<FILL_COND:slot_40>) {  // aligned to BB_41 (if_header)
 if (<FILL_COND:slot_41>) {  // aligned to BB_42 (if_header)
 if (<FILL_COND:slot_43>) {  // aligned to BB_44 (if_header)
 if (<FILL_COND:slot_44>) {  // aligned to BB_45 (if_header)
 <FILL_STMT:slot_45>;  // aligned to BB_46 (sequential)
 <FILL_STMT:slot_46>;  // aligned to BB_47 (sequential)
 if (<FILL_COND:slot_47>) {  // aligned to BB_48 (if_header)
 if (<FILL_COND:slot_48>) {  // aligned to BB_49 (if_header)
 <FILL_STMT:slot_50>;  // aligned to BB_51 (sequential)
 <FILL_STMT:slot_51>;  // aligned to BB_52 (sequential)
 if (<FILL_COND:slot_52>) {  // aligned to BB_56 (if_header)
 if (<FILL_COND:slot_53>) {  // aligned to BB_57 (if_header)
 if (<FILL_COND:slot_54>) {  // aligned to BB_58 (if_header)
 if (<FILL_COND:slot_55>) {  // aligned to BB_59 (if_header)
 if (<FILL_COND:slot_56>) {  // aligned to BB_60 (if_header)
 <FILL_STMT:slot_57>;  // aligned to BB_61 (if_then_body)
 while (<FILL_COND:slot_58>) {  // aligned to BB_62 (loop_header)
 while (<FILL_COND:slot_59>) {  // aligned to BB_63 (loop_header)
 while (<FILL_COND:slot_60>) {  // aligned to BB_64 (loop_header)
 <FILL_STMT:slot_61>;  // aligned to BB_65 (loop_body)
 <FILL_STMT:slot_62>;  // aligned to BB_66 (loop_body)
 <FILL_STMT:slot_63>;  // aligned to BB_33 (sequential)
 if (<FILL_COND:slot_64>) {  // aligned to BB_53 (if_header)
 <FILL_STMT:slot_67>;  // aligned to BB_24 (sequential)
 <FILL_STMT:slot_68>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_70>;  // aligned to BB_71 (sequential)
 if (<FILL_COND:slot_71>) {  // aligned to BB_68 (if_header)

 } else {
 <FILL_STMT:slot_72>;  // aligned to BB_69 (if_else_body)
 <FILL_STMT:slot_73>;  // aligned to BB_70 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_65>;  // aligned to BB_54 (if_else_body)
 <FILL_STMT:slot_66>;  // aligned to BB_55 (if_else_body)
 <FILL_STMT:slot_69>;  // aligned to BB_67 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_74>;  // aligned to BB_30 (if_else_body)
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_49>;  // aligned to BB_50 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_42>;  // aligned to BB_43 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_39>;  // aligned to BB_40 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_36>;  // aligned to BB_37 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_34>;  // aligned to BB_35 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_29>;  // aligned to BB_28 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_20 (if_else_body)
 <FILL_STMT:slot_23>;  // aligned to BB_21 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 79  input_line = 145  func = re_protect  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cp.c
source_bounds = 305-376
bb = 24  src_rows = 39  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 11/10  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.909/0.273
bb_cover = 24/24  reasons = ['extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=4, if_header=9, if_then_body=1, if_else_body=7, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L318
    if L326
        if L333
    if L341
        if L343
            if L346
    if L358
        if L360
        if L363
            if L365

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if
        if
          if
            if
              if
                if
                  if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:if_header
BB_3:if_header
BB_4:if_else_body
BB_5:if_else_body
BB_6:if_header
BB_7:if_header
BB_8:if_header
BB_9:if_then_body
BB_10:if_else_body
BB_11:sequential
BB_12:if_header
BB_13:if_header
BB_14:if_else_body
BB_15:if_else_body
BB_16:if_header
BB_17:if_header
BB_18:if_else_body
BB_19:if_else_body
BB_20:sequential
BB_21:loop_body
BB_22:sequential
BB_23:sequential

-- REAL SOURCE FUNCTION --
  305: {
  306:   struct dir_attr *p;
  307:   char *dst_name;		/* A copy of CONST_DST_NAME we can change. */
  308: 
  309:   ASSIGN_STRDUPA (dst_name, const_dst_name);
  310: 
  311:   /* The suffix of DST_NAME that is a copy of the source file name,
  312:      possibly truncated to name a parent directory.  */
  313:   char const *src_name = dst_name + (dst_src_name - const_dst_name);
  314: 
  315:   /* Likewise, but with any leading '/'s skipped.  */
  316:   char const *relname = dst_name + (dst_relname - const_dst_name);
  317: 
  318:   for (p = attr_list; p; p = p->next)
  319:     {
  320:       dst_name[p->slash_offset] = '\0';
  321: 
  322:       /* Adjust the times (and if possible, ownership) for the copy.
  323:          chown turns off set[ug]id bits for non-root,
  324:          so do the chmod last.  */
  325: 
  326:       if (x->preserve_timestamps)
  327:         {
  328:           struct timespec timespec[2];
  329: 
  330:           timespec[0] = get_stat_atime (&p->st);
  331:           timespec[1] = get_stat_mtime (&p->st);
  332: 
  333:           if (utimensat (dst_dirfd, relname, timespec, 0))
  334:             {
  335:               error (0, errno, _("failed to preserve times for %s"),
  336:                      quoteaf (dst_name));
  337:               return false;
  338:             }
  339:         }
  340: 
  341:       if (x->preserve_ownership)
  342:         {
  343:           if (lchownat (dst_dirfd, relname, p->st.st_uid, p->st.st_gid)
  344:               != 0)
  345:             {
  346:               if (! chown_failure_ok (x))
  347:                 {
  348:                   error (0, errno, _("failed to preserve ownership for %s"),
  349:                          quoteaf (dst_name));
  350:                   return false;
  351:                 }
  352:               /* Failing to preserve ownership is OK. Still, try to preserve
  353:                  the group, but ignore the possible error. */
  354:               ignore_value (lchownat (dst_dirfd, relname, -1, p->st.st_gid));
  355:             }
  356:         }
  357: 
  358:       if (x->preserve_mode)
  359:         {
  360:           if (copy_acl (src_name, -1, dst_name, -1, p->st.st_mode) != 0)
  361:             return false;
  362:         }
  363:       else if (p->restore_mode)
  364:         {
  365:           if (lchmodat (dst_dirfd, relname, p->st.st_mode) != 0)
  366:             {
  367:               error (0, errno, _("failed to preserve permissions for %s"),
  368:                      quoteaf (dst_name));
  369:               return false;
  370:             }
  371:         }
  372: 
  373:       dst_name[p->slash_offset] = '/';
  374:     }
  375:   return true;
  376: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_23 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 if (<FILL_COND:slot_9>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_7 (if_header)
 if (<FILL_COND:slot_11>) {  // aligned to BB_8 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_11 (sequential)
 if (<FILL_COND:slot_15>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_13 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_14 (if_then_body)
 if (<FILL_COND:slot_19>) {  // aligned to BB_16 (if_header)
 if (<FILL_COND:slot_20>) {  // aligned to BB_17 (if_header)
 <FILL_STMT:slot_22>;  // aligned to BB_19 (sequential)
 <FILL_STMT:slot_23>;  // aligned to BB_20 (sequential)
 <FILL_STMT:slot_24>;  // aligned to BB_22 (sequential)
 } else {
 <FILL_STMT:slot_21>;  // aligned to BB_18 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_15 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_12>;  // aligned to BB_9 (if_else_body)
 <FILL_STMT:slot_13>;  // aligned to BB_10 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 }
 }
 <FILL_STMT:slot_25>;  // aligned to BB_21 (loop_body)
 }
 }
}

========================================================================================================================
example = 80  input_line = 146  func = usage  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/cp.c
source_bounds = 152-279
bb = 5  src_rows = 32  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 5/5  reasons = []
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L153

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_else_body
BB_4:sequential

-- REAL SOURCE FUNCTION --
  152: {
  153:   if (status != EXIT_SUCCESS)
  154:     emit_try_help ();
  155:   else
  156:     {
  157:       printf (_("\
  158: Usage: %s [OPTION]... [-T] SOURCE DEST\n\
  159:   or:  %s [OPTION]... SOURCE... DIRECTORY\n\
  160:   or:  %s [OPTION]... -t DIRECTORY SOURCE...\n\
  161: "),
  162:               program_name, program_name, program_name);
  163:       fputs (_("\
  164: Copy SOURCE to DEST, or multiple SOURCE(s) to DIRECTORY.\n\
  165: "), stdout);
  166: 
  167:       emit_mandatory_arg_note ();
  168: 
  169:       fputs (_("\
  170:   -a, --archive                same as -dR --preserve=all\n\
  171:       --attributes-only        don't copy the file data, just the attributes\n\
  172:       --backup[=CONTROL]       make a backup of each existing destination file\
  173: \n\
  174:   -b                           like --backup but does not accept an argument\n\
  175:       --copy-contents          copy contents of special files when recursive\n\
  176:   -d                           same as --no-dereference --preserve=links\n\
  177: "), stdout);
  178:       fputs (_("\
  179:       --debug                  explain how a file is copied.  Implies -v\n\
  180: "), stdout);
  181:       fputs (_("\
  182:   -f, --force                  if an existing destination file cannot be\n\
  183:                                  opened, remove it and try again (this option\n\
  184:                                  is ignored when the -n option is also used)\n\
  185:   -i, --interactive            prompt before overwrite (overrides a previous -n\
  186: \n\
  187:                                   option)\n\
  188:   -H                           follow command-line symbolic links in SOURCE\n\
  189: "), stdout);
  190:       fputs (_("\
  191:   -l, --link                   hard link files instead of copying\n\
  192:   -L, --dereference            always follow symbolic links in SOURCE\n\
  193: "), stdout);
  194:       fputs (_("\
  195:   -n, --no-clobber             do not overwrite an existing file (overrides a\n\
  196:                                  -u or previous -i option). See also --update\n\
  197: "), stdout);
  198:       fputs (_("\
  199:   -P, --no-dereference         never follow symbolic links in SOURCE\n\
  200: "), stdout);
  201:       fputs (_("\
  202:   -p                           same as --preserve=mode,ownership,timestamps\n\
  203:       --preserve[=ATTR_LIST]   preserve the specified attributes\n\
  204: "), stdout);
  205:       fputs (_("\
  206:       --no-preserve=ATTR_LIST  don't preserve the specified attributes\n\
  207:       --parents                use full source file name under DIRECTORY\n\
  208: "), stdout);
  209:       fputs (_("\
  210:   -R, -r, --recursive          copy directories recursively\n\
  211:       --reflink[=WHEN]         control clone/CoW copies. See below\n\
  212:       --remove-destination     remove each existing destination file before\n\
  213:                                  attempting to open it (contrast with --force)\
  214: \n"), stdout);
  215:       fputs (_("\
  216:       --sparse=WHEN            control creation of sparse files. See below\n\
  217:       --strip-trailing-slashes  remove any trailing slashes from each SOURCE\n\
  218:                                  argument\n\
  219: "), stdout);
  220:       fputs (_("\
  221:   -s, --symbolic-link          make symbolic links instead of copying\n\
  222:   -S, --suffix=SUFFIX          override the usual backup suffix\n\
  223:   -t, --target-directory=DIRECTORY  copy all SOURCE arguments into DIRECTORY\n\
  224:   -T, --no-target-directory    treat DEST as a normal file\n\
  225: "), stdout);
  226:       fputs (_("\
  227:   --update[=UPDATE]            control which existing files are updated;\n\
  228:                                  UPDATE={all,none,older(default)}.  See below\n\
  229:   -u                           equivalent to --update[=older]\n\
  230: "), stdout);
  231:       fputs (_("\
  232:   -v, --verbose                explain what is being done\n\
  233: "), stdout);
  234:       fputs (_("\
  235:   -x, --one-file-system        stay on this file system\n\
  236: "), stdout);
  237:       fputs (_("\
  238:   -Z                           set SELinux security context of destination\n\
  239:                                  file to default type\n\
  240:       --context[=CTX]          like -Z, or if CTX is specified then set the\n\
  241:                                  SELinux or SMACK security context to CTX\n\
  242: "), stdout);
  243:       fputs (HELP_OPTION_DESCRIPTION, stdout);
  244:       fputs (VERSION_OPTION_DESCRIPTION, stdout);
  245:       fputs (_("\
  246: \n\
  247: ATTR_LIST is a comma-separated list of attributes. Attributes are 'mode' for\n\
  248: permissions (including any ACL and xattr permissions), 'ownership' for user\n\
  249: and group, 'timestamps' for file timestamps, 'links' for hard links, 'context'\
  250: \nfor security context, 'xattr' for extended attributes, and 'all' for all\n\
  251: attributes.\n\
  252: "), stdout);
  253:       fputs (_("\
  254: \n\
  255: By default, sparse SOURCE files are detected by a crude heuristic and the\n\
  256: corresponding DEST file is made sparse as well.  That is the behavior\n\
  257: selected by --sparse=auto.  Specify --sparse=always to create a sparse DEST\n\
  258: file whenever the SOURCE file contains a long enough sequence of zero bytes.\n\
  259: Use --sparse=never to inhibit creation of sparse files.\n\
  260: "), stdout);
  261:       emit_update_parameters_note ();
  262:       fputs (_("\
  263: \n\
  264: When --reflink[=always] is specified, perform a lightweight copy, where the\n\
  265: data blocks are copied only when modified.  If this is not possible the copy\n\
  266: fails, or if --reflink=auto is specified, fall back to a standard copy.\n\
  267: Use --reflink=never to ensure a standard copy is performed.\n\
  268: "), stdout);
  269:       emit_backup_suffix_note ();
  270:       fputs (_("\
  271: \n\
  272: As a special case, cp makes a backup of SOURCE when the force and backup\n\
  273: options are given and SOURCE and DEST are the same name for an existing,\n\
  274: regular file.\n\
  275: "), stdout);
  276:       emit_ancillary_info (PROGRAM_NAME);
  277:     }
  278:   exit (status);
  279: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_4 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (if_else_body)
 }
}

========================================================================================================================
example = 81  input_line = 150  func = max_out  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1265-1293
bb = 22  src_rows = 23  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 15/8  gaps(if,loop,switch)=(1,6,0)  sim(type,depth)=0.533/0.133
bb_cover = 22/22  reasons = ['extra_if:1', 'extra_loop:6', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=2, if_header=5, if_then_body=1, if_else_body=3, loop_header=7, loop_body=3, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L1268
  if L1269
      if L1271
      loop L1277
      if L1279
        loop L1280
if L1285
if L1290

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    if
      if
        if
          loop
            loop
              loop
                if
                  loop
                    loop
                    loop
                    loop
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:if_header
BB_3:if_header
BB_4:if_header
BB_5:if_else_body
BB_6:loop_header
BB_7:loop_header
BB_8:loop_body
BB_9:loop_header
BB_10:loop_header
BB_11:loop_header
BB_12:loop_body
BB_13:loop_header
BB_14:sequential
BB_15:if_then_body
BB_16:loop_body
BB_17:if_header
BB_18:if_else_body
BB_19:if_header
BB_20:if_else_body
BB_21:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
 1265: {
 1266:   bool percent = false;
 1267: 
 1268:   for (char *f = format; *f; f++)
 1269:     if (*f == '%' && *++f != '%')
 1270:       {
 1271:         if (percent)
 1272:           error (EXIT_FAILURE, 0,
 1273:                  _("too many %% conversion specifications in suffix"));
 1274:         percent = true;
 1275:         int flags;
 1276:         f += get_format_flags (f, &flags);
 1277:         while (ISDIGIT (*f))
 1278:           f++;
 1279:         if (*f == '.')
 1280:           while (ISDIGIT (*++f))
 1281:             continue;
 1282:         check_format_conv_type (f, flags);
 1283:       }
 1284: 
 1285:   if (! percent)
 1286:     error (EXIT_FAILURE, 0,
 1287:            _("missing %% conversion specification in suffix"));
 1288: 
 1289:   int maxlen = snprintf (nullptr, 0, format, INT_MAX);
 1290:   if (maxlen < 0)
 1291:     xalloc_die ();
 1292:   return maxlen;
 1293: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_4 (if_header)
 while (<FILL_COND:slot_7>) {  // aligned to BB_5 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_6 (loop_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_7 (loop_header)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (loop_body)
 if (<FILL_COND:slot_11>) {  // aligned to BB_9 (if_header)
 while (<FILL_COND:slot_12>) {  // aligned to BB_10 (loop_header)
 while (<FILL_COND:slot_13>) {  // aligned to BB_11 (loop_header)
 <FILL_STMT:slot_14>;  // aligned to BB_12 (loop_body)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 while (<FILL_COND:slot_16>) {  // aligned to BB_14 (loop_header)
 while (<FILL_COND:slot_17>) {  // aligned to BB_15 (loop_header)
 <FILL_STMT:slot_18>;  // aligned to BB_16 (loop_body)
 if (<FILL_COND:slot_19>) {  // aligned to BB_17 (if_header)
 if (<FILL_COND:slot_21>) {  // aligned to BB_19 (if_header)
 return <FILL_EXPR:slot_23>;  // aligned to BB_21 (func_prologue_epilogue)
 } else {
 <FILL_STMT:slot_22>;  // aligned to BB_20 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_20>;  // aligned to BB_18 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 82  input_line = 151  func = set_input_file  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 610-614
bb = 4  src_rows = 3  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_if:1']
10class_pattern_label_counts = sequential=2, if_header=1, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L611

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:sequential

-- REAL SOURCE FUNCTION --
  610: {
  611:   if (! STREQ (name, "-") && fd_reopen (STDIN_FILENO, name, O_RDONLY, 0) < 0)
  612:     error (EXIT_FAILURE, errno, _("cannot open %s for reading"),
  613:            quoteaf (name));
  614: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 }
 }
}

========================================================================================================================
example = 83  input_line = 152  func = parse_patterns  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1132-1180
bb = 21  src_rows = 29  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 13/7  gaps(if,loop,switch)=(3,2,1)  sim(type,depth)=0.538/0.154
bb_cover = 21/21  reasons = ['extra_if:3', 'extra_loop:2', 'extra_switch:1']
10class_pattern_label_counts = sequential=4, if_header=7, if_then_body=1, if_else_body=5, loop_header=3, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L1136
    if L1138
          if L1148
          if L1151
          if L1154
          if L1163
    if L1173

-- AB SKELETON CONTROL OUTLINE --
switch
  loop
    if
      if
        if
          if
            loop
              loop
                if
                  if
                    if
                    if
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:if_header
BB_3:if_else_body
BB_4:if_then_body
BB_5:if_header
BB_6:if_header
BB_7:sequential
BB_8:if_header
BB_9:if_else_body
BB_10:if_header
BB_11:if_else_body
BB_12:if_header
BB_13:if_else_body
BB_14:sequential
BB_15:if_header
BB_16:if_else_body
BB_17:loop_header
BB_18:loop_header
BB_19:loop_body
BB_20:sequential

-- REAL SOURCE FUNCTION --
 1132: {
 1133:   struct control *p;		/* New control record created. */
 1134:   static intmax_t last_val = 0;
 1135: 
 1136:   for (int i = start; i < argc; i++)
 1137:     {
 1138:       if (*argv[i] == '/' || *argv[i] == '%')
 1139:         {
 1140:           p = extract_regexp (i, *argv[i] == '%', argv[i]);
 1141:         }
 1142:       else
 1143:         {
 1144:           p = new_control_record ();
 1145:           p->argnum = i;
 1146: 
 1147:           uintmax_t val;
 1148:           if (xstrtoumax (argv[i], nullptr, 10, &val, "") != LONGINT_OK
 1149:               || INTMAX_MAX < val)
 1150:             error (EXIT_FAILURE, 0, _("%s: invalid pattern"), quote (argv[i]));
 1151:           if (val == 0)
 1152:             error (EXIT_FAILURE, 0,
 1153:                    _("%s: line number must be greater than zero"), argv[i]);
 1154:           if (val < last_val)
 1155:             {
 1156:               char buf[INT_BUFSIZE_BOUND (intmax_t)];
 1157:               error (EXIT_FAILURE, 0,
 1158:                      _("line number %s is smaller than preceding line number,"
 1159:                        " %s"),
 1160:                      quote (argv[i]), imaxtostr (last_val, buf));
 1161:             }
 1162: 
 1163:           if (val == last_val)
 1164:             error (0, 0,
 1165:            _("warning: line number %s is the same as preceding line number"),
 1166:                    quote (argv[i]));
 1167: 
 1168:           last_val = val;
 1169: 
 1170:           p->lines_required = val;
 1171:         }
 1172: 
 1173:       if (i + 1 < argc && *argv[i + 1] == '{')
 1174:         {
 1175:           /* We have a repeat count. */
 1176:           i++;
 1177:           parse_repeat_count (i, p, argv[i]);
 1178:         }
 1179:     }
 1180: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 switch (<FILL_COND:slot_2>) {  // aligned to BB_0 (switch_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_then_body)
 if (<FILL_COND:slot_7>) {  // aligned to BB_15 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_16 (if_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_17 (loop_header)
 while (<FILL_COND:slot_10>) {  // aligned to BB_18 (loop_header)
 <FILL_STMT:slot_11>;  // aligned to BB_19 (loop_body)
 if (<FILL_COND:slot_12>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_13>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_14>;  // aligned to BB_7 (sequential)
 if (<FILL_COND:slot_15>) {  // aligned to BB_8 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_19>) {  // aligned to BB_12 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_22>;  // aligned to BB_20 (sequential)
 } else {
 <FILL_STMT:slot_20>;  // aligned to BB_13 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_11 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_9 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 84  input_line = 154  func = split_file  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 856-877
bb = 20  src_rows = 15  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 13/4  gaps(if,loop,switch)=(-1,10,0)  sim(type,depth)=0.231/0.231
bb_cover = 20/20  reasons = ['missing_if:1', 'extra_loop:10']
10class_pattern_label_counts = sequential=4, loop_header=13, loop_body=3

-- SOURCE CONTROL OUTLINE --
loop L857
    if L860
        loop L862
          loop L868

-- AB SKELETON CONTROL OUTLINE --
loop
  loop
    loop
      loop
        loop
          loop
            loop
              loop
                loop
                  loop
                    loop
                    loop
                    loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:sequential
BB_1:loop_header
BB_2:loop_header
BB_3:loop_header
BB_4:loop_header
BB_5:loop_header
BB_6:loop_header
BB_7:sequential
BB_8:loop_body
BB_9:loop_header
BB_10:loop_header
BB_11:loop_header
BB_12:loop_header
BB_13:loop_header
BB_14:sequential
BB_15:loop_body
BB_16:loop_header
BB_17:loop_header
BB_18:loop_body
BB_19:sequential

-- REAL SOURCE FUNCTION --
  856: {
  857:   for (idx_t i = 0; i < control_used; i++)
  858:     {
  859:       intmax_t j;
  860:       if (controls[i].regexpr)
  861:         {
  862:           for (j = 0; (controls[i].repeat_forever
  863:                        || j <= controls[i].repeat); j++)
  864:             process_regexp (&controls[i], j);
  865:         }
  866:       else
  867:         {
  868:           for (j = 0; (controls[i].repeat_forever
  869:                        || j <= controls[i].repeat); j++)
  870:             process_line_count (&controls[i], j);
  871:         }
  872:     }
  873: 
  874:   create_output_file ();
  875:   dump_rest_of_file ();
  876:   close_output_file ();
  877: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 <FILL_STMT:slot_2>;  // aligned to BB_0 (sequential)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_7>) {  // aligned to BB_5 (loop_header)
 while (<FILL_COND:slot_8>) {  // aligned to BB_6 (loop_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (if_else_body)
 while (<FILL_COND:slot_10>) {  // aligned to BB_9 (loop_header)
 while (<FILL_COND:slot_11>) {  // aligned to BB_17 (loop_header)
 <FILL_STMT:slot_12>;  // aligned to BB_18 (loop_body)
 <FILL_STMT:slot_13>;  // aligned to BB_8 (loop_body)
 while (<FILL_COND:slot_14>) {  // aligned to BB_10 (loop_header)
 while (<FILL_COND:slot_15>) {  // aligned to BB_11 (loop_header)
 while (<FILL_COND:slot_16>) {  // aligned to BB_12 (loop_header)
 while (<FILL_COND:slot_17>) {  // aligned to BB_13 (loop_header)
 <FILL_STMT:slot_18>;  // aligned to BB_14 (sequential)
 while (<FILL_COND:slot_19>) {  // aligned to BB_16 (loop_header)
 <FILL_STMT:slot_20>;  // aligned to BB_15 (loop_body)
 <FILL_STMT:slot_21>;  // aligned to BB_19 (sequential)
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 85  input_line = 155  func = get_format_flags  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1191-1215
bb = 9  src_rows = 19  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/2  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.500
bb_cover = 9/9  reasons = ['filled_terminal_count_mismatch', 'same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=4, loop_header=2, switch_header=2, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
loop L1194
    switch L1196

-- AB SKELETON CONTROL OUTLINE --
loop
  switch

-- 10CLASS BB LABEL SEQUENCE --
BB_0:switch_header
BB_1:switch_header
BB_2:function_prologue_epilogue
BB_3:sequential
BB_4:sequential
BB_5:sequential
BB_6:loop_header
BB_7:loop_header
BB_8:sequential

-- REAL SOURCE FUNCTION --
 1191: {
 1192:   int flags = 0;
 1193: 
 1194:   for (idx_t count = 0; ; count++)
 1195:     {
 1196:       switch (format[count])
 1197:         {
 1198:         case '-':
 1199:         case '0':
 1200:           break;
 1201: 
 1202:         case '\'':
 1203:           flags |= FLAG_THOUSANDS;
 1204:           break;
 1205: 
 1206:         case '#':
 1207:           flags |= FLAG_ALTERNATIVE;
 1208:           break;
 1209: 
 1210:         default:
 1211:           *flags_ptr = flags;
 1212:           return count;
 1213:         }
 1214:     }
 1215: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 switch (<FILL_COND:slot_3>) {  // aligned to BB_1 (switch_header)
 <FILL_EXPR:slot_4>;  // aligned to BB_2 (func_prologue_epilogue)
 <FILL_STMT:slot_5>;  // aligned to BB_6 (if_else_body)
 <FILL_STMT:slot_6>;  // aligned to BB_3 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_8>;  // aligned to BB_5 (sequential)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (sequential)
 }
 }
}

========================================================================================================================
example = 86  input_line = 156  func = check_format_conv_type  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1222-1258
bb = 24  src_rows = 24  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 9/3  gaps(if,loop,switch)=(0,0,6)  sim(type,depth)=0.333/0.111
bb_cover = 24/24  reasons = ['extra_switch:6', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=13, if_header=2, if_else_body=2, switch_header=7

-- SOURCE CONTROL OUTLINE --
switch L1226
      if L1246
if L1254

-- AB SKELETON CONTROL OUTLINE --
switch
  switch
    switch
      switch
        switch
          switch
            switch
              if
                if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:switch_header
BB_1:sequential
BB_2:switch_header
BB_3:sequential
BB_4:switch_header
BB_5:sequential
BB_6:switch_header
BB_7:sequential
BB_8:switch_header
BB_9:sequential
BB_10:switch_header
BB_11:sequential
BB_12:switch_header
BB_13:sequential
BB_14:sequential
BB_15:sequential
BB_16:sequential
BB_17:sequential
BB_18:if_header
BB_19:if_else_body
BB_20:sequential
BB_21:if_header
BB_22:if_else_body
BB_23:sequential

-- REAL SOURCE FUNCTION --
 1222: {
 1223:   unsigned char ch = *format;
 1224:   int compatible_flags = FLAG_THOUSANDS;
 1225: 
 1226:   switch (ch)
 1227:     {
 1228:     case 'd':
 1229:     case 'i':
 1230:       break;
 1231: 
 1232:     case 'u':
 1233:       *format = 'd';
 1234:       break;
 1235: 
 1236:     case 'o':
 1237:     case 'x':
 1238:     case 'X':
 1239:       compatible_flags = FLAG_ALTERNATIVE;
 1240:       break;
 1241: 
 1242:     case 0:
 1243:       error (EXIT_FAILURE, 0, _("missing conversion specifier in suffix"));
 1244: 
 1245:     default:
 1246:       if (isprint (ch))
 1247:         error (EXIT_FAILURE, 0,
 1248:                _("invalid conversion specifier in suffix: %c"), ch);
 1249:       else
 1250:         error (EXIT_FAILURE, 0,
 1251:                _("invalid conversion specifier in suffix: \\%.3o"), ch);
 1252:     }
 1253: 
 1254:   if (flags & ~ compatible_flags)
 1255:     error (EXIT_FAILURE, 0,
 1256:            _("invalid flags in conversion specification: %%%c%c"),
 1257:            (flags & ~ compatible_flags & FLAG_ALTERNATIVE ? '#' : '\''), ch);
 1258: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 switch (<FILL_COND:slot_2>) {  // aligned to BB_0 (switch_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (sequential)
 switch (<FILL_COND:slot_4>) {  // aligned to BB_2 (switch_header)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 switch (<FILL_COND:slot_6>) {  // aligned to BB_4 (switch_header)
 <FILL_STMT:slot_7>;  // aligned to BB_5 (sequential)
 switch (<FILL_COND:slot_8>) {  // aligned to BB_6 (switch_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (sequential)
 switch (<FILL_COND:slot_10>) {  // aligned to BB_8 (switch_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 switch (<FILL_COND:slot_12>) {  // aligned to BB_10 (switch_header)
 <FILL_STMT:slot_13>;  // aligned to BB_11 (sequential)
 switch (<FILL_COND:slot_14>) {  // aligned to BB_12 (switch_header)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (sequential)
 if (<FILL_COND:slot_16>) {  // aligned to BB_18 (if_header)
 <FILL_STMT:slot_18>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_19>) {  // aligned to BB_21 (if_header)
 <FILL_STMT:slot_21>;  // aligned to BB_23 (sequential)
 <FILL_STMT:slot_22>;  // aligned to BB_16 (sequential)
 <FILL_STMT:slot_24>;  // aligned to BB_14 (sequential)
 } else {
 <FILL_STMT:slot_20>;  // aligned to BB_22 (if_else_body)
 <FILL_STMT:slot_23>;  // aligned to BB_15 (if_else_body)
 <FILL_STMT:slot_25>;  // aligned to BB_17 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_17>;  // aligned to BB_19 (if_else_body)
 }
 }
 }
 }
 }
 }
 }
 }
}

========================================================================================================================
example = 87  input_line = 157  func = extract_regexp  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1090-1125
bb = 7  src_rows = 27  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 3/3  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.333
bb_cover = 7/7  reasons = ['filled_terminal_count_mismatch', 'same_control_sequence_different_depth']
10class_pattern_label_counts = if_header=3, if_else_body=3, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L1098
if L1115
if L1121

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_else_body
BB_4:if_header
BB_5:if_else_body
BB_6:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
 1090: {
 1091:   idx_t len;			/* Number of bytes in this regexp. */
 1092:   char delim = *str;
 1093:   char const *closing_delim;
 1094:   struct control *p;
 1095:   char const *err;
 1096: 
 1097:   closing_delim = strrchr (str + 1, delim);
 1098:   if (closing_delim == nullptr)
 1099:     error (EXIT_FAILURE, 0,
 1100:            _("%s: closing delimiter '%c' missing"), str, delim);
 1101: 
 1102:   len = closing_delim - str - 1;
 1103:   p = new_control_record ();
 1104:   p->argnum = argnum;
 1105:   p->ignore = ignore;
 1106: 
 1107:   p->regexpr = true;
 1108:   p->re_compiled.buffer = nullptr;
 1109:   p->re_compiled.allocated = 0;
 1110:   p->re_compiled.fastmap = xmalloc (UCHAR_MAX + 1);
 1111:   p->re_compiled.translate = nullptr;
 1112:   re_syntax_options =
 1113:     RE_SYNTAX_POSIX_BASIC & ~RE_CONTEXT_INVALID_DUP & ~RE_NO_EMPTY_RANGES;
 1114:   err = re_compile_pattern (str + 1, len, &p->re_compiled);
 1115:   if (err)
 1116:     {
 1117:       error (0, 0, _("%s: invalid regular expression: %s"), quote (str), err);
 1118:       cleanup_fatal ();
 1119:     }
 1120: 
 1121:   if (closing_delim[1])
 1122:     check_for_offset (p, str, closing_delim + 1);
 1123: 
 1124:   return p;
 1125: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_4 (if_header)
 return <FILL_EXPR:slot_8>;  // aligned to BB_6 (func_prologue_epilogue)
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 88  input_line = 159  func = parse_repeat_count  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1055-1080
bb = 10  src_rows = 15  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 3/3  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.333
bb_cover = 10/10  reasons = ['same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=4, if_header=4, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L1059
if L1064
      if L1069

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_header
BB_3:if_else_body
BB_4:sequential
BB_5:if_header
BB_6:if_header
BB_7:sequential
BB_8:sequential
BB_9:sequential

-- REAL SOURCE FUNCTION --
 1055: {
 1056:   char *end;
 1057: 
 1058:   end = str + strlen (str) - 1;
 1059:   if (*end != '}')
 1060:     error (EXIT_FAILURE, 0, _("%s: '}' is required in repeat count"),
 1061:            quote (str));
 1062:   *end = '\0';
 1063: 
 1064:   if (str + 1 == end - 1 && *(str + 1) == '*')
 1065:     p->repeat_forever = true;
 1066:   else
 1067:     {
 1068:       uintmax_t val;
 1069:       if (xstrtoumax (str + 1, nullptr, 10, &val, "") != LONGINT_OK
 1070:           || INTMAX_MAX < val)
 1071:         {
 1072:           error (EXIT_FAILURE, 0,
 1073:                  _("%s}: integer required between '{' and '}'"),
 1074:                  quote (global_argv[argnum]));
 1075:         }
 1076:       p->repeat = val;
 1077:     }
 1078: 
 1079:   *end = '}';
 1080: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_2 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_4 (sequential)
 <FILL_STMT:slot_7>;  // aligned to BB_9 (sequential)
 if (<FILL_COND:slot_8>) {  // aligned to BB_5 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (sequential)
 <FILL_STMT:slot_11>;  // aligned to BB_8 (sequential)
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_6 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_3 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 89  input_line = 160  func = delete_all_files  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 938-950
bb = 11  src_rows = 10  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 4/3  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.750/0.250
bb_cover = 11/11  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=3, if_header=3, if_else_body=3, loop_header=1, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L939
loop L942
    if L945

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_else_body
BB_3:loop_header
BB_4:if_header
BB_5:if_header
BB_6:if_else_body
BB_7:sequential
BB_8:loop_body
BB_9:sequential
BB_10:sequential

-- REAL SOURCE FUNCTION --
  938: {
  939:   if (! remove_files)
  940:     return;
  941: 
  942:   for (int i = files_created; 0 <= --i; )
  943:     {
  944:       char const *name = make_filename (i);
  945:       if (unlink (name) != 0 && errno != ENOENT && !in_signal_handler)
  946:         error (0, errno, "%s", quotef (name));
  947:     }
  948: 
  949:   files_created = 0;
  950: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_then_body)
 <FILL_STMT:slot_4>;  // aligned to BB_10 (sequential)
 while (<FILL_COND:slot_5>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_3 (loop_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_4 (if_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 } else {
 <FILL_STMT:slot_8>;  // aligned to BB_5 (if_else_body)
 <FILL_STMT:slot_9>;  // aligned to BB_6 (if_else_body)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (if_else_body)
 }
 <FILL_STMT:slot_12>;  // aligned to BB_8 (loop_body)
 }
 }
 }
}

========================================================================================================================
example = 90  input_line = 161  func = process_regexp  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 750-850
bb = 43  src_rows = 69  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 22/17  gaps(if,loop,switch)=(0,5,0)  sim(type,depth)=0.682/0.182
bb_cover = 43/43  reasons = ['extra_loop:5', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=7, if_header=15, if_else_body=11, loop_header=8, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L757
if L763
    loop L765
        if L768
            if L770
        if L783
        if L787
        if L792
            if L795
      loop L805
          if L808
              if L810
          if L823
          if L827
if L842
if L845
if L848

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      loop
        if
          if
            if
              if
                if
                  if
                    if
                    if
                    if
                    loop
                    loop
                    if
                    if
                    if
                    loop
                    if
                    loop
                    if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:loop_header
BB_3:loop_header
BB_4:loop_header
BB_5:if_header
BB_6:if_header
BB_7:if_else_body
BB_8:sequential
BB_9:sequential
BB_10:if_header
BB_11:if_else_body
BB_12:if_header
BB_13:if_else_body
BB_14:if_header
BB_15:if_header
BB_16:if_else_body
BB_17:sequential
BB_18:loop_header
BB_19:loop_body
BB_20:loop_header
BB_21:loop_header
BB_22:loop_header
BB_23:if_header
BB_24:if_header
BB_25:if_else_body
BB_26:sequential
BB_27:sequential
BB_28:if_header
BB_29:if_else_body
BB_30:if_header
BB_31:if_else_body
BB_32:if_header
BB_33:loop_header
BB_34:loop_body
BB_35:sequential
BB_36:if_header
BB_37:if_else_body
BB_38:if_header
BB_39:if_else_body
BB_40:if_header
BB_41:if_else_body
BB_42:sequential

-- REAL SOURCE FUNCTION --
  750: {
  751:   struct cstring *line;		/* From input file. */
  752:   idx_t line_len;		/* To make "$" in regexps work. */
  753:   intmax_t break_line;		/* First line number of next file. */
  754:   bool ignore = p->ignore;	/* If true, skip this section. */
  755:   regoff_t ret;
  756: 
  757:   if (!ignore)
  758:     create_output_file ();
  759: 
  760:   /* If there is no offset for the regular expression, or
  761:      it is positive, then it is not necessary to buffer the lines. */
  762: 
  763:   if (p->offset >= 0)
  764:     {
  765:       while (true)
  766:         {
  767:           line = find_line (++current_line);
  768:           if (line == nullptr)
  769:             {
  770:               if (p->repeat_forever)
  771:                 {
  772:                   if (!ignore)
  773:                     {
  774:                       dump_rest_of_file ();
  775:                       close_output_file ();
  776:                     }
  777:                   exit (EXIT_SUCCESS);
  778:                 }
  779:               else
  780:                 regexp_error (p, repetition, ignore);
  781:             }
  782:           line_len = line->len;
  783:           if (line->str[line_len - 1] == '\n')
  784:             line_len--;
  785:           ret = re_search (&p->re_compiled, line->str, line_len,
  786:                            0, line_len, nullptr);
  787:           if (ret == -2)
  788:             {
  789:               error (0, 0, _("error in regular expression search"));
  790:               cleanup_fatal ();
  791:             }
  792:           if (ret == -1)
  793:             {
  794:               line = remove_line ();
  795:               if (!ignore)
  796:                 save_line_to_file (line);
  797:             }
  798:           else
  799:             break;
  800:         }
  801:     }
  802:   else
  803:     {
  804:       /* Buffer the lines. */
  805:       while (true)
  806:         {
  807:           line = find_line (++current_line);
  808:           if (line == nullptr)
  809:             {
  810:               if (p->repeat_forever)
  811:                 {
  812:                   if (!ignore)
  813:                     {
  814:                       dump_rest_of_file ();
  815:                       close_output_file ();
  816:                     }
  817:                   exit (EXIT_SUCCESS);
  818:                 }
  819:               else
  820:                 regexp_error (p, repetition, ignore);
  821:             }
  822:           line_len = line->len;
  823:           if (line->str[line_len - 1] == '\n')
  824:             line_len--;
  825:           ret = re_search (&p->re_compiled, line->str, line_len,
  826:                            0, line_len, nullptr);
  827:           if (ret == -2)
  828:             {
  829:               error (0, 0, _("error in regular expression search"));
  830:               cleanup_fatal ();
  831:             }
  832:           if (ret != -1)
  833:             break;
  834:         }
  835:     }
  836: 
  837:   /* Account for any offset from this regexp. */
  838:   break_line = current_line + p->offset;
  839: 
  840:   write_to_file (break_line, ignore, p->argnum);
  841: 
  842:   if (!ignore)
  843:     close_output_file ();
  844: 
  845:   if (p->offset > 0)
  846:     current_line = break_line;
  847: 
  848:   if (suppress_matched)
  849:     remove_line ();
  850: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 while (<FILL_COND:slot_6>) {  // aligned to BB_4 (loop_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_10>;  // aligned to BB_8 (sequential)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (sequential)
 if (<FILL_COND:slot_12>) {  // aligned to BB_10 (if_header)
 if (<FILL_COND:slot_14>) {  // aligned to BB_12 (if_header)
 if (<FILL_COND:slot_16>) {  // aligned to BB_14 (if_header)
 if (<FILL_COND:slot_17>) {  // aligned to BB_15 (if_header)
 <FILL_STMT:slot_19>;  // aligned to BB_17 (sequential)
 <FILL_STMT:slot_20>;  // aligned to BB_18 (sequential)
 <FILL_STMT:slot_21>;  // aligned to BB_20 (sequential)
 if (<FILL_COND:slot_22>) {  // aligned to BB_36 (if_header)
 if (<FILL_COND:slot_24>) {  // aligned to BB_38 (if_header)
 if (<FILL_COND:slot_26>) {  // aligned to BB_40 (if_header)
 <FILL_STMT:slot_28>;  // aligned to BB_42 (sequential)
 while (<FILL_COND:slot_29>) {  // aligned to BB_21 (loop_header)
 while (<FILL_COND:slot_30>) {  // aligned to BB_22 (loop_header)
 if (<FILL_COND:slot_31>) {  // aligned to BB_23 (if_header)
 if (<FILL_COND:slot_32>) {  // aligned to BB_24 (if_header)
 <FILL_STMT:slot_34>;  // aligned to BB_26 (sequential)
 <FILL_STMT:slot_35>;  // aligned to BB_27 (sequential)
 if (<FILL_COND:slot_36>) {  // aligned to BB_28 (if_header)
 while (<FILL_COND:slot_37>) {  // aligned to BB_29 (loop_header)
 if (<FILL_COND:slot_38>) {  // aligned to BB_30 (if_header)
 while (<FILL_COND:slot_39>) {  // aligned to BB_31 (loop_header)
 if (<FILL_COND:slot_40>) {  // aligned to BB_32 (if_header)
 <FILL_STMT:slot_41>;  // aligned to BB_33 (sequential)
 <FILL_STMT:slot_42>;  // aligned to BB_35 (sequential)
 }
 <FILL_STMT:slot_43>;  // aligned to BB_34 (loop_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_33>;  // aligned to BB_25 (if_else_body)
 }
 }
 }
 }
 } else {
 <FILL_STMT:slot_27>;  // aligned to BB_41 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_25>;  // aligned to BB_39 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_23>;  // aligned to BB_37 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_18>;  // aligned to BB_16 (if_else_body)
 }
 }
 } else {
 <FILL_STMT:slot_15>;  // aligned to BB_13 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_13>;  // aligned to BB_11 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_7 (if_else_body)
 }
 }
 <FILL_STMT:slot_44>;  // aligned to BB_19 (loop_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 91  input_line = 162  func = process_line_count  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 690-720
bb = 16  src_rows = 18  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 6/5  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.833/0.167
bb_cover = 16/16  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=3, if_header=4, if_then_body=2, if_else_body=4, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L699
loop L703
    if L706
if L713
if L718

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      if
        if
          if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:sequential
BB_4:loop_header
BB_5:loop_header
BB_6:if_header
BB_7:if_else_body
BB_8:loop_body
BB_9:if_header
BB_10:if_else_body
BB_11:if_header
BB_12:if_then_body
BB_13:if_else_body
BB_14:sequential
BB_15:sequential

-- REAL SOURCE FUNCTION --
  690: {
  691:   intmax_t linenum;
  692:   intmax_t last_line_to_save = p->lines_required * (repetition + 1);
  693: 
  694:   create_output_file ();
  695: 
  696:   /* Ensure that the line number specified is not 1 greater than
  697:      the number of lines in the file.
  698:      When suppressing matched lines, check before the loop. */
  699:   if (no_more_lines () && suppress_matched)
  700:     handle_line_error (p, repetition);
  701: 
  702:   linenum = get_first_line_in_buffer ();
  703:   while (linenum++ < last_line_to_save)
  704:     {
  705:       struct cstring *line = remove_line ();
  706:       if (line == nullptr)
  707:         handle_line_error (p, repetition);
  708:       save_line_to_file (line);
  709:     }
  710: 
  711:   close_output_file ();
  712: 
  713:   if (suppress_matched)
  714:     remove_line ();
  715: 
  716:   /* Ensure that the line number specified is not 1 greater than
  717:      the number of lines in the file. */
  718:   if (no_more_lines () && !suppress_matched)
  719:     handle_line_error (p, repetition);
  720: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_4 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_5 (loop_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_9 (if_header)
 if (<FILL_COND:slot_10>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_11>;  // aligned to BB_12 (if_then_body)
 <FILL_STMT:slot_12>;  // aligned to BB_15 (sequential)
 } else {
 <FILL_STMT:slot_13>;  // aligned to BB_13 (if_else_body)
 <FILL_STMT:slot_14>;  // aligned to BB_14 (if_else_body)
 <FILL_STMT:slot_15>;  // aligned to BB_2 (if_else_body)
 <FILL_STMT:slot_16>;  // aligned to BB_3 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_9>;  // aligned to BB_10 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_7 (if_else_body)
 }
 <FILL_STMT:slot_17>;  // aligned to BB_8 (loop_body)
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 92  input_line = 163  func = create_output_file  verdict = SURFACE_DIFF_PROBABLY_EQUIV
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 901-931
bb = 6  src_rows = 22  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/2  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/0.500
bb_cover = 6/6  reasons = ['same_control_sequence_different_depth']
10class_pattern_label_counts = sequential=1, if_header=2, if_then_body=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L908
if L925

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:if_header
BB_4:if_else_body
BB_5:sequential

-- REAL SOURCE FUNCTION --
  901: {
  902:   int nfiles = files_created;
  903:   bool fopen_ok;
  904:   int fopen_errno;
  905: 
  906:   output_filename = make_filename (nfiles);
  907: 
  908:   if (nfiles == INT_MAX)
  909:     {
  910:       fopen_ok = false;
  911:       fopen_errno = EOVERFLOW;
  912:     }
  913:   else
  914:     {
  915:       /* Create the output file in a critical section, to avoid races.  */
  916:       sigset_t oldset;
  917:       sigprocmask (SIG_BLOCK, &caught_signals, &oldset);
  918:       output_stream = fopen (output_filename, "w");
  919:       fopen_ok = (output_stream != nullptr);
  920:       fopen_errno = errno;
  921:       files_created = nfiles + fopen_ok;
  922:       sigprocmask (SIG_SETMASK, &oldset, nullptr);
  923:     }
  924: 
  925:   if (! fopen_ok)
  926:     {
  927:       error (0, fopen_errno, "%s", quotef (output_filename));
  928:       cleanup_fatal ();
  929:     }
  930:   bytes_written = 0;
  931: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_4>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_5 (sequential)
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_4 (if_else_body)
 <FILL_STMT:slot_7>;  // aligned to BB_2 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 93  input_line = 164  func = dump_rest_of_file  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 658-663
bb = 4  src_rows = 4  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.500/0.500
bb_cover = 4/4  reasons = ['extra_loop:1']
10class_pattern_label_counts = sequential=1, loop_header=2, loop_body=1

-- SOURCE CONTROL OUTLINE --
loop L661

-- AB SKELETON CONTROL OUTLINE --
loop
  loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:loop_header
BB_1:loop_header
BB_2:loop_body
BB_3:sequential

-- REAL SOURCE FUNCTION --
  658: {
  659:   struct cstring *line;
  660: 
  661:   while ((line = remove_line ()) != nullptr)
  662:     save_line_to_file (line);
  663: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 while (<FILL_COND:slot_2>) {  // aligned to BB_0 (loop_header)
 while (<FILL_COND:slot_3>) {  // aligned to BB_1 (loop_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (loop_body)
 <FILL_STMT:slot_5>;  // aligned to BB_3 (sequential)
 }
 }
}

========================================================================================================================
example = 94  input_line = 165  func = close_output_file  verdict = NEEDS_PATH_AUDIT
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 957-998
bb = 16  src_rows = 31  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 7/6  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.857/0.571
bb_cover = 16/16  reasons = ['extra_if:1', 'small_if_count_gap']
10class_pattern_label_counts = sequential=5, if_header=6, if_else_body=5

-- SOURCE CONTROL OUTLINE --
if L958
    if L960
    if L966
    if L972
        if L985
          if L990

-- AB SKELETON CONTROL OUTLINE --
if
  if
    if
      if
        if
          if
            if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_header
BB_2:if_else_body
BB_3:if_header
BB_4:if_else_body
BB_5:if_header
BB_6:if_else_body
BB_7:if_header
BB_8:if_else_body
BB_9:sequential
BB_10:sequential
BB_11:if_header
BB_12:if_else_body
BB_13:sequential
BB_14:sequential
BB_15:sequential

-- REAL SOURCE FUNCTION --
  957: {
  958:   if (output_stream)
  959:     {
  960:       if (ferror (output_stream))
  961:         {
  962:           error (0, 0, _("write error for %s"), quoteaf (output_filename));
  963:           output_stream = nullptr;
  964:           cleanup_fatal ();
  965:         }
  966:       if (fclose (output_stream) != 0)
  967:         {
  968:           error (0, errno, "%s", quotef (output_filename));
  969:           output_stream = nullptr;
  970:           cleanup_fatal ();
  971:         }
  972:       if (bytes_written == 0 && elide_empty_files)
  973:         {
  974:           sigset_t oldset;
  975:           bool unlink_ok;
  976:           int unlink_errno;
  977: 
  978:           /* Remove the output file in a critical section, to avoid races.  */
  979:           sigprocmask (SIG_BLOCK, &caught_signals, &oldset);
  980:           unlink_ok = (unlink (output_filename) == 0);
  981:           unlink_errno = errno;
  982:           files_created--;
  983:           sigprocmask (SIG_SETMASK, &oldset, nullptr);
  984: 
  985:           if (! unlink_ok && unlink_errno != ENOENT)
  986:             error (0, unlink_errno, "%s", quotef (output_filename));
  987:         }
  988:       else
  989:         {
  990:           if (!suppress_count)
  991:             {
  992:               char buf[INT_BUFSIZE_BOUND (intmax_t)];
  993:               fprintf (stdout, "%s\n", imaxtostr (bytes_written, buf));
  994:             }
  995:         }
  996:       output_stream = nullptr;
  997:     }
  998: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 if (<FILL_COND:slot_3>) {  // aligned to BB_1 (if_header)
 if (<FILL_COND:slot_5>) {  // aligned to BB_3 (if_header)
 if (<FILL_COND:slot_7>) {  // aligned to BB_5 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_6 (if_header)
 if (<FILL_COND:slot_9>) {  // aligned to BB_7 (if_header)
 <FILL_STMT:slot_12>;  // aligned to BB_10 (sequential)
 <FILL_STMT:slot_13>;  // aligned to BB_14 (sequential)
 <FILL_STMT:slot_14>;  // aligned to BB_15 (sequential)
 if (<FILL_COND:slot_15>) {  // aligned to BB_11 (if_header)
 <FILL_STMT:slot_17>;  // aligned to BB_13 (sequential)
 } else {
 <FILL_STMT:slot_16>;  // aligned to BB_12 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_10>;  // aligned to BB_8 (if_else_body)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_6>;  // aligned to BB_4 (if_else_body)
 }
 } else {
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 95  input_line = 166  func = make_filename  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 888-895
bb = 4  src_rows = 6  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 4/4  reasons = []
10class_pattern_label_counts = if_header=1, if_else_body=2, function_prologue_epilogue=1

-- SOURCE CONTROL OUTLINE --
if L890

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:if_else_body
BB_3:function_prologue_epilogue

-- REAL SOURCE FUNCTION --
  888: {
  889:   strcpy (filename_space, prefix);
  890:   if (suffix)
  891:     sprintf (filename_space + strlen (prefix), suffix, num);
  892:   else
  893:     sprintf (filename_space + strlen (prefix), "%0*d", digits, num);
  894:   return filename_space;
  895: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_3 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 <FILL_STMT:slot_5>;  // aligned to BB_2 (if_else_body)
 }
}

========================================================================================================================
example = 96  input_line = 168  func = find_line  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 565-596
bb = 16  src_rows = 22  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 7/6  gaps(if,loop,switch)=(0,1,0)  sim(type,depth)=0.714/0.286
bb_cover = 16/16  reasons = ['extra_loop:1', 'filled_terminal_count_mismatch']
10class_pattern_label_counts = sequential=4, if_header=3, if_else_body=3, loop_header=4, loop_body=2

-- SOURCE CONTROL OUTLINE --
if L568
if L571
loop L574
    if L576
        loop L585
    if L592

-- AB SKELETON CONTROL OUTLINE --
if
  if
    loop
      loop
        loop
          if
            if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential
BB_3:if_header
BB_4:loop_header
BB_5:if_else_body
BB_6:loop_header
BB_7:loop_header
BB_8:loop_header
BB_9:loop_body
BB_10:sequential
BB_11:if_header
BB_12:if_else_body
BB_13:sequential
BB_14:loop_body
BB_15:sequential

-- REAL SOURCE FUNCTION --
  565: {
  566:   struct buffer_record *b;
  567: 
  568:   if (head == nullptr && !load_buffer ())
  569:     return nullptr;
  570: 
  571:   if (linenum < head->start_line)
  572:     return nullptr;
  573: 
  574:   for (b = head;;)
  575:     {
  576:       if (linenum < b->start_line + b->num_lines)
  577:         {
  578:           /* The line is in this buffer. */
  579:           struct line *l;
  580:           idx_t offset;	/* How far into the buffer the line is. */
  581: 
  582:           l = b->line_start;
  583:           offset = linenum - b->start_line;
  584:           /* Find the control record. */
  585:           while (offset >= CTRL_SIZE)
  586:             {
  587:               l = l->next;
  588:               offset -= CTRL_SIZE;
  589:             }
  590:           return &l->starts[offset];
  591:         }
  592:       if (b->next == nullptr && !load_buffer ())
  593:         return nullptr;
  594:       b = b->next;		/* Try the next data block. */
  595:     }
  596: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (if_then_body)
 <FILL_EXPR:slot_5>;  // aligned to BB_15 (func_prologue_epilogue)
 if (<FILL_COND:slot_6>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_7>;  // aligned to BB_4 (if_then_body)
 while (<FILL_COND:slot_8>) {  // aligned to BB_5 (loop_header)
 while (<FILL_COND:slot_9>) {  // aligned to BB_6 (loop_header)
 <FILL_STMT:slot_10>;  // aligned to BB_7 (sequential)
 while (<FILL_COND:slot_11>) {  // aligned to BB_8 (loop_header)
 <FILL_STMT:slot_12>;  // aligned to BB_9 (loop_body)
 if (<FILL_COND:slot_13>) {  // aligned to BB_11 (if_header)
 if (<FILL_COND:slot_14>) {  // aligned to BB_12 (if_header)
 <FILL_STMT:slot_15>;  // aligned to BB_13 (if_then_body)
 }
 }
 <FILL_STMT:slot_16>;  // aligned to BB_14 (loop_body)
 }
 }
 }
 <FILL_STMT:slot_17>;  // aligned to BB_10 (if_then_body)
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 97  input_line = 169  func = regexp_error  verdict = CONTROL_COUNT_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 724-742
bb = 6  src_rows = 12  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 2/1  gaps(if,loop,switch)=(1,0,0)  sim(type,depth)=0.500/0.500
bb_cover = 6/6  reasons = ['extra_if:1']
10class_pattern_label_counts = sequential=1, if_header=2, if_then_body=1, if_else_body=2

-- SOURCE CONTROL OUTLINE --
if L728

-- AB SKELETON CONTROL OUTLINE --
if
  if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_then_body
BB_2:if_else_body
BB_3:if_header
BB_4:if_else_body
BB_5:sequential

-- REAL SOURCE FUNCTION --
  724: {
  725:   fprintf (stderr, _("%s: %s: match not found"),
  726:            program_name, quote (global_argv[p->argnum]));
  727: 
  728:   if (repetition)
  729:     {
  730:       char buf[INT_BUFSIZE_BOUND (intmax_t)];
  731:       fprintf (stderr, _(" on repetition %s\n"), imaxtostr (repetition, buf));
  732:     }
  733:   else
  734:     fprintf (stderr, "\n");
  735: 
  736:   if (!ignore)
  737:     {
  738:       dump_rest_of_file ();
  739:       close_output_file ();
  740:     }
  741:   cleanup_fatal ();
  742: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_then_body)
 if (<FILL_COND:slot_4>) {  // aligned to BB_3 (if_header)
 <FILL_STMT:slot_6>;  // aligned to BB_5 (sequential)
 } else {
 <FILL_STMT:slot_5>;  // aligned to BB_4 (if_else_body)
 <FILL_STMT:slot_7>;  // aligned to BB_2 (if_else_body)
 }
 }
}

========================================================================================================================
example = 98  input_line = 171  func = save_line_to_file  verdict = SURFACE_MATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 1005-1014
bb = 3  src_rows = 8  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 1/1  gaps(if,loop,switch)=(0,0,0)  sim(type,depth)=1.000/1.000
bb_cover = 3/3  reasons = []
10class_pattern_label_counts = sequential=1, if_header=1, if_else_body=1

-- SOURCE CONTROL OUTLINE --
if L1007

-- AB SKELETON CONTROL OUTLINE --
if

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:sequential

-- REAL SOURCE FUNCTION --
 1005: {
 1006:   idx_t l = fwrite (line->str, sizeof (char), line->len, output_stream);
 1007:   if (l != line->len)
 1008:     {
 1009:       error (0, errno, _("write error for %s"), quoteaf (output_filename));
 1010:       output_stream = nullptr;
 1011:       cleanup_fatal ();
 1012:     }
 1013:   bytes_written += line->len;
 1014: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 <FILL_STMT:slot_4>;  // aligned to BB_2 (sequential)
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}

========================================================================================================================
example = 99  input_line = 172  func = write_to_file  verdict = LOOP_SWITCH_MISMATCH
source_file = /data/chaoni/WQM/source_datasets/coreutils/coreutils-9.4/src/csplit.c
source_bounds = 623-652
bb = 11  src_rows = 20  src_ref = statement_rows  parse = statement_rows_ok
ctrl(pred/source) = 6/4  gaps(if,loop,switch)=(0,2,0)  sim(type,depth)=0.667/0.167
bb_cover = 11/11  reasons = ['extra_loop:2']
10class_pattern_label_counts = sequential=1, if_header=3, if_else_body=2, loop_header=4, loop_body=1

-- SOURCE CONTROL OUTLINE --
if L631
loop L640
    if L643
    if L649

-- AB SKELETON CONTROL OUTLINE --
if
  loop
    loop
      if
        if
          loop

-- 10CLASS BB LABEL SEQUENCE --
BB_0:if_header
BB_1:if_else_body
BB_2:loop_header
BB_3:loop_header
BB_4:if_header
BB_5:if_else_body
BB_6:if_header
BB_7:loop_header
BB_8:loop_header
BB_9:loop_body
BB_10:sequential

-- REAL SOURCE FUNCTION --
  623: {
  624:   struct cstring *line;
  625:   intmax_t first_line;		/* First available input line. */
  626:   intmax_t lines;		/* Number of lines to output. */
  627:   intmax_t i;
  628: 
  629:   first_line = get_first_line_in_buffer ();
  630: 
  631:   if (first_line > last_line)
  632:     {
  633:       error (0, 0, _("%s: line number out of range"),
  634:              quote (global_argv[argnum]));
  635:       cleanup_fatal ();
  636:     }
  637: 
  638:   lines = last_line - first_line;
  639: 
  640:   for (i = 0; i < lines; i++)
  641:     {
  642:       line = remove_line ();
  643:       if (line == nullptr)
  644:         {
  645:           error (0, 0, _("%s: line number out of range"),
  646:                  quote (global_argv[argnum]));
  647:           cleanup_fatal ();
  648:         }
  649:       if (!ignore)
  650:         save_line_to_file (line);
  651:     }
  652: }

-- AB GENERATED FULL SKELETON --
<FILL_TYPE:slot_0> func(<FILL_PARAM:slot_1>) {

 if (<FILL_COND:slot_2>) {  // aligned to BB_0 (if_header)
 while (<FILL_COND:slot_4>) {  // aligned to BB_2 (loop_header)
 while (<FILL_COND:slot_5>) {  // aligned to BB_3 (loop_header)
 if (<FILL_COND:slot_6>) {  // aligned to BB_4 (if_header)
 if (<FILL_COND:slot_8>) {  // aligned to BB_6 (if_header)
 <FILL_STMT:slot_9>;  // aligned to BB_7 (if_then_body)
 while (<FILL_COND:slot_10>) {  // aligned to BB_8 (loop_header)
 <FILL_STMT:slot_11>;  // aligned to BB_9 (loop_body)
 <FILL_STMT:slot_12>;  // aligned to BB_10 (sequential)
 }
 }
 } else {
 <FILL_STMT:slot_7>;  // aligned to BB_5 (if_else_body)
 }
 }
 }
 } else {
 <FILL_STMT:slot_3>;  // aligned to BB_1 (if_else_body)
 }
}
